
Here is the adversarial critique output (valid JSON only):

```json
{
  "score": 6.2,
  "adversarial_verdict": "This plan will execute in a clean single-worker dev environment and pass verification, but contains several latent failures: backfill will silently score non-MIG sessions with wrong thresholds; get_all_thresholds implementation is unspecified; process_type=None bypasses the 'mig' default; and the test_put_threshold_invalidates_cache fixture setup is undefined for an empty weld_thresholds table.",
  "sabotage_vectors": [
    {
      "step": "1.2",
      "attack": "Migration UPDATE locks the entire sessions table. On a DB with 100k+ sessions, the UPDATE runs without batching. Long-running transaction blocks other writes; migration times out or causes connection pool exhaustion in staging.",
      "detection_gap": "Verification uses psql on small dev DB. No verification for migration duration or lock contention.",
      "blast_radius": "Deployment blocked; rollback leaves schema half-applied if timeout occurs mid-migration."
    },
    {
      "step": "1.3",
      "attack": "from_pydantic uses getattr(session, 'process_type', 'mig'). If Session has process_type=None explicitly (e.g. from old client), getattr returns None, not 'mig'. Storing None violates NOT NULL or falls through to DB default unpredictably.",
      "detection_gap": "Verification tests round-trip with valid process_type. No test for process_type=None or missing attribute.",
      "blast_radius": "Sessions created with process_type=None get wrong thresholds (key = 'none'.lower() not in cache, falls back to mig) or insert failure."
    },
    {
      "step": "1.4",
      "attack": "get_all_thresholds is never implemented in the plan. The routes call it. Implementer guesses return list(_threshold_cache.values()) but forgets to trigger _load_all when cache is cold. First GET /api/thresholds returns [] or raises.",
      "detection_gap": "Verification assumes get_all_thresholds exists and works. The plan's code block only shows get_thresholds and _load_all.",
      "blast_radius": "Admin UI cannot load thresholds; Phase 3 blocked."
    },
    {
      "step": "1.4",
      "attack": "Two concurrent requests call get_thresholds before _cache_loaded is True. Both see _cache_loaded=False, both call _load_all. Last writer wins. With a slow DB, interleaved _load_all calls could overwrite cache with partial data.",
      "detection_gap": "Verification is single-threaded. No concurrent-load test.",
      "blast_radius": "Rare race; wrong thresholds served until next cache invalidation."
    },
    {
      "step": "1.5",
      "attack": "PUT succeeds, commit completes. Between invalidate_cache() and get_thresholds(db, weld_type), the DB connection is closed (e.g. connection pool timeout). get_thresholds raises. Route returns 500. Cache is invalidated. User sees error but DB has correct data. Next GET score still refetches.",
      "detection_gap": "Verification checks 200 and single object. No test for commit-succeeds-then-get_thresholds-fails path.",
      "blast_radius": "Admin confusion; no data corruption."
    },
    {
      "step": "2.3",
      "attack": "If process_type column exists but was never backfilled due to migration partial failure, session_model.process_type could be NULL. getattr returns None. We use 'mig'. TIG sessions scored as MIG.",
      "detection_gap": "Verification assumes SessionModel has process_type after migration. No assertion that process_type is never NULL in DB.",
      "blast_radius": "TIG sessions scored with MIG thresholds until manually fixed."
    },
    {
      "step": "3.4",
      "attack": "User clears angle_target_degrees. +e.target.value yields NaN. typeof NaN === 'number' so isCompleteForm passes. Save sends NaN. Pydantic may reject (422) or coerce unpredictably.",
      "detection_gap": "Verification tests warning > critical. No test for NaN, empty string, or non-numeric input.",
      "blast_radius": "422 or 500 on save; possible edge-case corruption if NaN passes validation."
    },
    {
      "step": "4.6",
      "attack": "test_put_threshold_invalidates_cache uses client. Existing fixtures use SQLite in-memory with Base.metadata.create_all. weld_thresholds is created empty. PUT /api/thresholds/mig returns 404 (row not found). Test fails.",
      "detection_gap": "Plan says 'Fixtures for db, client'. No fixture seeds weld_thresholds. Test assumes table is populated.",
      "blast_radius": "test_put_threshold_invalidates_cache never passes; cache invalidation not verified in CI."
    }
  ],
  "precondition_gaps": [
    "Step 1.4: get_all_thresholds implementation is not specified. Routes and verification depend on it.",
    "Step 1.3: from_pydantic should use (getattr(session, 'process_type', 'mig') or 'mig') to handle process_type=None.",
    "backfill_score_total.py: Plan says 'No code change needed'. If backfill runs after new TIG/stick/flux_core sessions exist with score_total=NULL, it scores them with default 45 and module constants. Wrong scores.",
    "Step 4.6: test_put_threshold_invalidates_cache requires weld_thresholds seeded. In-memory test DB has empty table. No fixture populates it."
  ],
  "untestable_verifications": [
    "Step 3.2: 'Arc spans correct angle (visual: 45 deg arc)' - no DOM assertion. A degenerate SVG that draws a line would pass.",
    "Step 3.3: document.querySelector checks link existence. Layout with no children or broken CSS would pass.",
    "Step 4.3, 4.4: 'Callout visible when spec present' - no assertion that callout text matches active_threshold_spec values."
  ],
  "cascade_points": [
    "Step 1.2 is a single point of failure for Steps 1.3, 1.4, 1.5, 1.7, and all of Phase 2.",
    "Step 1.4 get_thresholds is required by 1.5, 2.3. If get_all_thresholds is broken, Phase 3 Admin UI cannot load."
  ],
  "phase_boundary_gaps": [
    "Phase 1 to Phase 2: No step verifies that ALL sessions have process_type set (no NULLs from manual inserts or race).",
    "Phase 2 to Phase 4: If Step 2.3 omits thermal/amps/volts/heat_diss from active_threshold_spec, Step 4.2 fallbacks (20, 40, 5, 1, 40) are used. TIG thermal would show 20 instead of 60. No Phase 4 verification asserts spec completeness."
  ],
  "pre_mortem": {
    "partial_execution": "We got through Phase 1 and Phase 2 but had to stop. The state of the codebase was: weld_thresholds exists, GET/PUT work, scoring uses thresholds. But get_all_thresholds was never implemented. Admin UI fetch fails. Root cause: Step 1.4 code block omitted get_all_thresholds.",
    "silent_corruption": "Everything appeared to work. Users reported issues 3 days later. The root cause was backfill_score_total.py running nightly. New TIG sessions were scored with angle_target=45 and module constants. Backfill was never updated to use process_type and get_thresholds.",
    "prod_vs_local": "Staging deployment revealed migration timing out. Sessions table had 80k rows. The UPDATE locked the table for 12 seconds. Health checks failed. Root cause: Step 1.2 has no batching for large tables.",
    "dependency_drift": "SQLAlchemy 2.0 deprecation of db.query() would affect routes/thresholds.py. Plan uses db.query(WeldThresholdModel). Latent; not immediate."
  },
  "critical_issues": [
    "Step 1.4: get_all_thresholds is required by routes and verification but has no implementation in the plan. Phase 3 Admin UI will fail to load."
  ],
  "latent_issues": [
    "backfill_score_total.py scores non-MIG sessions with MIG thresholds when score_total is NULL. Trigger: run backfill after any non-MIG session completes.",
    "Step 1.2: Unbounded UPDATE on sessions; at 100k+ rows migration may timeout. Trigger: deploy to staging with large session count.",
    "Step 1.4: Multi-worker cache staleness. Trigger: Gunicorn with multiple workers; admin saves; another worker serves stale until restart.",
    "Step 3.4: NaN from number inputs can pass isCompleteForm. Trigger: user clears field."
  ],
  "blind_spots": [
    "Plan assumes backfill only runs on sessions that existed at migration time. Any backfill run after new non-MIG sessions exist will score them wrong.",
    "Plan assumes test DB can be used for test_put_threshold_invalidates_cache. weld_thresholds is empty; PUT returns 404. No fixture seeds it.",
    "Plan assumes prototype_arc_scoring.py and prototype_aggregate_perf.py need no updates. They use extract_features(session) and score_session(session, features). If they create TIG sessions, scores would be wrong."
  ],
  "minor_issues": [
    "Step 1.1: Field validation test uses ellipsis for other fields. Unclear if all required fields are provided.",
    "Step 4.1: Plan says 'angle + thermal + amps + volts' but micro-feedback only has angle and thermal. Amps/volts thresholds passed but unused.",
    "Step 4.5: Demo uses hardcoded MIG spec. If mock score later has different spec, callout would be wrong."
  ],
  "what_the_author_cant_see": "The plan was written with a sequential, single-worker mental model. get_all_thresholds was never written because list_thresholds 'obviously' needs it. The backfill was 'no change needed' because migration backfills mig; the author did not consider backfill running again later. The test fixture gap exists because the author envisioned a real PostgreSQL test DB or assumed migrations populate tables.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Add explicit get_all_thresholds to Step 1.4: if not _cache_loaded: _load_all(db); return list(_threshold_cache.values())",
    "Fix from_pydantic: process_type=(getattr(session, 'process_type', None) or 'mig')",
    "Document or update backfill_score_total.py to use get_thresholds and extract_features with angle_target from thresholds",
    "Add test fixture to seed weld_thresholds with at least mig row before test_put_threshold_invalidates_cache"
  ]
}
```
