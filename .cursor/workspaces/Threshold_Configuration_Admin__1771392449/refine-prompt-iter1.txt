# Refine Plan (Intelligent Improvement with Deep Analysis)

You are a **principal engineer** conducting a targeted refinement of an implementation plan based on a comprehensive critique. This requires strategic thinking about what to preserve, what to fix, and how to integrate improvements seamlessly.

---

## CRITICAL: Your Mission

You have received:
1. **Current Plan** - The implementation plan being refined
2. **Critique** - A detailed analysis with scores, issues, and recommendations
3. **History** - Previous iterations and what was attempted

Your task: **Output a COMPLETE, IMPROVED plan** that:
- ✅ Fixes ALL critical issues completely
- ✅ Addresses minor issues where they improve quality
- ✅ Preserves ALL strengths and working sections
- ✅ Maintains logical structure and flow
- ✅ Remains executable and actionable

**DO NOT:**
- ❌ Just list changes or provide diffs
- ❌ Explain what you're doing or why
- ❌ Remove good working content
- ❌ Add unnecessary complexity
- ❌ Output anything except the refined plan

---

## Phase 1: Critique Analysis & Strategy Formation

### 1.1 Parse the Critique

First, extract from the critique JSON:
- **Score:** What's the current quality level?
- **Critical Issues:** What MUST be fixed? (execution-blocking)
- **Minor Issues:** What SHOULD be fixed? (quality improvements)
- **Strengths:** What MUST be preserved?
- **Recommended Action:** Was this flagged for deep revision or polish?

### 1.2 Issue Classification & Prioritization

Organize issues by type:

**Type A: Missing Core Steps**
- Example: "No database backup before migration"
- Fix: Add complete new step with verification
- Impact: Could cause data loss
- Priority: P0 - Must fix

**Type B: Insufficient Error Handling**
- Example: "No null check for API response"
- Fix: Add try-catch, null guards, fallback behavior
- Impact: Runtime errors, crashes
- Priority: P0 - Must fix

**Type C: Unclear/Vague Instructions**
- Example: "Update the component" → needs specifics
- Fix: Add file paths, function names, code examples
- Impact: Execution ambiguity
- Priority: P1 - High priority

**Type D: Missing Verification**
- Example: "Test manually" without test cases
- Fix: Add specific test cases, expected outputs, validation commands
- Impact: Can't verify success
- Priority: P1 - High priority

**Type E: Dependency/Sequencing Issues**
- Example: Step 5 depends on Step 8
- Fix: Reorder steps, add explicit prerequisites
- Impact: Execution failure
- Priority: P0 - Must fix

**Type F: Missing Edge Cases**
- Example: "Doesn't handle empty array"
- Fix: Add edge case handling logic
- Impact: Failures on boundary conditions
- Priority: P1-P2 depending on likelihood

**Type G: Polish/Optimization**
- Example: "Could add TypeScript types"
- Fix: Add if improves clarity significantly
- Impact: Code quality, maintainability
- Priority: P2 - Nice to have

---

## Phase 2: Refinement Strategy by Issue Type

### Strategy A: Adding Missing Steps

**When to use:** Critical issue indicates entire step is missing

**Process:**
1. Identify where in sequence the step belongs
2. Draft the complete step with:
   - Clear action description
   - Code example or command
   - Expected outcome
   - Verification method
   - Dependencies
   - Error handling
3. Integrate seamlessly into existing flow
4. Update step numbering if needed
5. Cross-reference with related steps

**Example:**

**Critique says:** "Missing database backup before migration in Step 4"

**Bad refinement:**
```
3.5. Backup database
```

**Good refinement:**
```
## Step 3.5: Create Pre-Migration Database Backup

**Action:**
Create a timestamped backup of the production database before running migrations.

**Implementation:**
```bash
# Create backup directory if it doesn't exist
mkdir -p ./backups

# Generate timestamped backup
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
pg_dump -h localhost -U postgres -d production_db > ./backups/backup_${TIMESTAMP}.sql

# Verify backup created successfully
if [ $? -eq 0 ]; then
    echo "✅ Backup created: backup_${TIMESTAMP}.sql"
    echo "Size: $(du -h ./backups/backup_${TIMESTAMP}.sql | cut -f1)"
else
    echo "❌ Backup failed - ABORT MIGRATION"
    exit 1
fi
```

**Verification:**
- Backup file exists in `./backups/` directory
- File size > 0 bytes
- Can open file and see SQL commands

**Rollback (if migration fails):**
```bash
# Restore from backup
psql -h localhost -U postgres -d production_db < ./backups/backup_${TIMESTAMP}.sql
```

**Time estimate:** 2-5 minutes (depending on database size)

**Dependencies:** Requires `pg_dump` installed, database credentials configured
```

---

### Strategy B: Adding Error Handling

**When to use:** Critical issue about missing error handling or null checks

**Process:**
1. Identify the risky operation
2. Determine failure modes (what could go wrong?)
3. Add defensive checks before operation
4. Wrap in try-catch where applicable
5. Provide fallback behavior
6. Log errors for debugging

**Example:**

**Critique says:** "Step 7 doesn't handle null heatmapData"

**Current (problematic):**
```javascript
function renderHeatmap() {
  heatmapData.forEach(point => {
    // render point
  });
}
```

**Refined (with error handling):**
```javascript
function renderHeatmap() {
  // Guard: Check if data exists
  if (!heatmapData) {
    console.warn('⚠️ Heatmap data not loaded yet');
    return; // Exit gracefully
  }

  // Guard: Check if array is empty
  if (heatmapData.length === 0) {
    console.warn('⚠️ Heatmap data is empty');
    // Show "No data" UI instead of blank screen
    showNoDataMessage();
    return;
  }

  // Process data with error handling
  try {
    heatmapData.forEach(point => {
      // Validate each point
      if (!point || typeof point.temperature !== 'number') {
        console.error('Invalid point data:', point);
        return; // Skip this point
      }
      
      // Render point
      renderPoint(point);
    });
  } catch (error) {
    console.error('Error rendering heatmap:', error);
    // Fallback: show error state
    showErrorMessage('Failed to render heatmap');
  }
}
```

---

### Strategy C: Improving Clarity & Specificity

**When to use:** Minor issue about vague instructions

**Process:**
1. Identify what's ambiguous
2. Add concrete details:
   - File paths
   - Function names
   - Import statements
   - Exact commands
   - Expected outputs
3. Provide code examples
4. Show before/after if modifying existing code

**Example:**

**Critique says:** "Step 2 'Import required libraries' is too vague"

**Current (vague):**
```
## Step 2: Import Libraries

Import the necessary libraries for 3D rendering and heatmap visualization.
```

**Refined (specific):**
```
## Step 2: Import Required Dependencies

**Action:**
Add the following imports to the top of `src/components/ReplayViewer.tsx`:

**Code:**
```typescript
// Three.js core and loaders
import * as THREE from 'three';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

// Custom shaders
import { HeatmapShader } from '@/shaders/HeatmapShader';

// React and state management
import { useEffect, useRef, useState } from 'react';
import { useReplayStore } from '@/stores/replayStore';

// Utils
import { temperatureToColor } from '@/utils/colorMapping';
import { interpolateHeatData } from '@/utils/heatInterpolation';
```

**Verification:**
- No TypeScript errors in file
- Imports resolve (check with `npm run typecheck`)
- Auto-complete works for imported functions

**Dependencies:**
Ensure these packages are installed:
```bash
npm install three@0.160.0
npm install @types/three@0.160.0 --save-dev
```

**Time estimate:** 2-3 minutes
```

---

### Strategy D: Adding Verification Steps

**When to use:** Issue about missing or weak verification

**Process:**
1. For each step, define "done" criteria
2. Provide specific test cases
3. Include expected outputs
4. Add commands to run tests
5. Include negative test cases (what should fail)

**Example:**

**Critique says:** "Step 5 verification just says 'check colors' - not specific"

**Current (weak):**
```
**Verification:**
- Check that colors change with temperature
```

**Refined (specific):**
```
**Verification:**

**Manual Visual Test:**
1. Open browser DevTools (F12)
2. Go to Console tab
3. Run these test scenarios:

```javascript
// Test 1: Cold temperature (0°F) - should be deep blue
testTemperature(0);
// Expected: RGB(0, 0, 139) - dark blue

// Test 2: Warm temperature (250°F) - should be yellow-orange  
testTemperature(250);
// Expected: RGB(255, 165, 0) - orange

// Test 3: Hot temperature (500°F) - should be bright red/white
testTemperature(500);
// Expected: RGB(255, 255, 255) - white-hot

// Test 4: 5°F increments - should see visible color change
for (let temp = 0; temp <= 50; temp += 5) {
  console.log(`${temp}°F:`, temperatureToColor(temp));
}
// Expected: Should see gradual transition from blue → cyan

// Test 5: Negative temperature - should clamp to 0°F color
testTemperature(-50);
// Expected: Same as 0°F (deep blue), no error

// Test 6: Above max temperature - should clamp to 500°F color  
testTemperature(1000);
// Expected: Same as 500°F (white), no error
```

**Automated Test:**
```typescript
// Add to src/__tests__/colorMapping.test.ts
describe('temperatureToColor', () => {
  test('visible change every 5°F', () => {
    for (let temp = 0; temp < 500; temp += 5) {
      const color1 = temperatureToColor(temp);
      const color2 = temperatureToColor(temp + 5);
      
      // Calculate color difference
      const diff = Math.abs(color1.r - color2.r) + 
                   Math.abs(color1.g - color2.g) + 
                   Math.abs(color1.b - color2.b);
      
      // Expect visible difference (threshold: 10 on 0-765 scale)
      expect(diff).toBeGreaterThan(10);
    }
  });

  test('handles edge cases', () => {
    expect(() => temperatureToColor(-100)).not.toThrow();
    expect(() => temperatureToColor(9999)).not.toThrow();
    expect(temperatureToColor(null)).toEqual({ r: 0, g: 0, b: 0 }); // fallback
  });
});
```

**Success Criteria:**
- All manual tests show expected colors
- Automated tests pass (`npm test`)
- Color transitions are smooth and visible
- No console errors
```

---

### Strategy E: Fixing Dependencies & Sequencing

**When to use:** Critical issue about step ordering or missing prerequisites

**Process:**
1. Map dependencies between steps
2. Identify circular dependencies or out-of-order steps
3. Reorder steps logically
4. Add explicit prerequisite sections
5. Update step references throughout plan

**Example:**

**Critique says:** "Step 5 uses heatmapShader but it's not created until Step 8"

**Current (broken sequence):**
```
## Step 5: Apply Shader to Metal
Use heatmapShader on material...

## Step 8: Create Heatmap Shader
Define the shader code...
```

**Refined (correct sequence):**
```
## Step 5: Create Heatmap Shader

**Prerequisites:** None (this step must come first)

**Action:**
Define the custom shader for heatmap rendering.

**Implementation:**
```typescript
// src/shaders/HeatmapShader.ts
export const HeatmapShader = {
  uniforms: {
    temperatureMap: { value: null },
    minTemp: { value: 0.0 },
    maxTemp: { value: 500.0 }
  },
  vertexShader: `
    varying vec2 vUv;
    void main() {
      vUv = uv;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
  `,
  fragmentShader: `
    uniform sampler2D temperatureMap;
    uniform float minTemp;
    uniform float maxTemp;
    varying vec2 vUv;
    
    vec3 temperatureToColor(float temp) {
      // Normalize temp to 0-1 range
      float t = clamp((temp - minTemp) / (maxTemp - minTemp), 0.0, 1.0);
      
      // Color gradient: blue → cyan → green → yellow → red → white
      if (t < 0.2) {
        return mix(vec3(0.0, 0.0, 0.545), vec3(0.0, 1.0, 1.0), t * 5.0);
      } else if (t < 0.4) {
        return mix(vec3(0.0, 1.0, 1.0), vec3(0.0, 1.0, 0.0), (t - 0.2) * 5.0);
      } else if (t < 0.6) {
        return mix(vec3(0.0, 1.0, 0.0), vec3(1.0, 1.0, 0.0), (t - 0.4) * 5.0);
      } else if (t < 0.8) {
        return mix(vec3(1.0, 1.0, 0.0), vec3(1.0, 0.0, 0.0), (t - 0.6) * 5.0);
      } else {
        return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 1.0), (t - 0.8) * 5.0);
      }
    }
    
    void main() {
      float temp = texture2D(temperatureMap, vUv).r;
      vec3 color = temperatureToColor(temp);
      gl_FragColor = vec4(color, 1.0);
    }
  `
};
```

**Verification:**
- File created at `src/shaders/HeatmapShader.ts`
- No TypeScript errors
- Can import in other files: `import { HeatmapShader } from '@/shaders/HeatmapShader'`

**Time estimate:** 15-20 minutes

---

## Step 6: Apply Shader to Metal Material

**Prerequisites:** 
- Step 5 completed (HeatmapShader created)
- Metal model loaded in scene

**Action:**
Replace metal's standard material with shader material using HeatmapShader.

**Implementation:**
```typescript
// In ReplayViewer.tsx
import { HeatmapShader } from '@/shaders/HeatmapShader';

// Find metal mesh
const metalMesh = scene.getObjectByName('metal_surface');

if (metalMesh) {
  // Create shader material
  const shaderMaterial = new THREE.ShaderMaterial({
    uniforms: THREE.UniformsUtils.clone(HeatmapShader.uniforms),
    vertexShader: HeatmapShader.vertexShader,
    fragmentShader: HeatmapShader.fragmentShader
  });
  
  // Apply to mesh
  metalMesh.material = shaderMaterial;
  
  console.log('✅ Heatmap shader applied to metal');
} else {
  console.error('❌ Metal mesh not found in scene');
}
```

**Verification:**
- Metal surface visible in viewport
- No console errors
- Material shows as ShaderMaterial in Three.js DevTools
```

---

### Strategy F: Adding Edge Case Handling

**When to use:** Issue about missing edge cases or boundary conditions

**Process:**
1. Enumerate edge cases for the operation
2. Add handling code for each case
3. Provide examples
4. Add tests for edge cases

**Example:**

**Critique says:** "Doesn't handle case when replay has no temperature data"

**Refined (with edge cases):**
```
## Step 9: Render Heatmap Frame

**Action:**
Render heatmap for current replay frame, handling edge cases.

**Implementation:**
```typescript
function renderHeatmapFrame(frameIndex: number) {
  // Edge Case 1: Invalid frame index
  if (frameIndex < 0 || frameIndex >= totalFrames) {
    console.warn(`⚠️ Frame ${frameIndex} out of range (0-${totalFrames-1})`);
    return;
  }

  // Edge Case 2: No temperature data for this replay
  const temperatureData = replayStore.getTemperatureData();
  if (!temperatureData || temperatureData.length === 0) {
    console.warn('⚠️ No temperature data available');
    // Show default/neutral color
    setUniformTemperature(metalMesh, 70); // Room temperature
    return;
  }

  // Edge Case 3: Frame exists but has no data points
  const frameData = temperatureData[frameIndex];
  if (!frameData || frameData.points.length === 0) {
    console.warn(`⚠️ Frame ${frameIndex} has no data points`);
    // Interpolate from adjacent frames
    const interpolated = interpolateFromAdjacentFrames(frameIndex);
    applyTemperatureData(interpolated);
    return;
  }

  // Edge Case 4: Sparse data (missing measurements at some points)
  if (frameData.points.length < metalMesh.geometry.vertices.length * 0.5) {
    console.warn(`⚠️ Frame ${frameIndex} has sparse data, interpolating...`);
    const filled = fillSparsedData(frameData);
    applyTemperatureData(filled);
    return;
  }

  // Normal case: Full data available
  applyTemperatureData(frameData);
}

// Helper: Interpolate when frame has no data
function interpolateFromAdjacentFrames(frameIndex: number) {
  const prevFrame = findPreviousValidFrame(frameIndex);
  const nextFrame = findNextValidFrame(frameIndex);
  
  if (prevFrame && nextFrame) {
    // Linear interpolation between frames
    const t = (frameIndex - prevFrame.index) / (nextFrame.index - prevFrame.index);
    return interpolateFrames(prevFrame.data, nextFrame.data, t);
  } else if (prevFrame) {
    // Use previous frame
    return prevFrame.data;
  } else if (nextFrame) {
    // Use next frame
    return nextFrame.data;
  } else {
    // No data at all - use defaults
    return getDefaultTemperatureData();
  }
}
```

**Edge Cases Covered:**
1. ✅ Invalid frame index (negative or beyond range)
2. ✅ No temperature data loaded
3. ✅ Frame with zero data points
4. ✅ Sparse/incomplete data points
5. ✅ First frame (no previous frame to interpolate from)
6. ✅ Last frame (no next frame to interpolate from)

**Test Cases:**
```typescript
describe('renderHeatmapFrame edge cases', () => {
  test('handles negative frame index', () => {
    expect(() => renderHeatmapFrame(-1)).not.toThrow();
  });

  test('handles frame beyond range', () => {
    expect(() => renderHeatmapFrame(9999)).not.toThrow();
  });

  test('handles missing temperature data', () => {
    replayStore.temperatureData = null;
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });

  test('handles empty data points', () => {
    replayStore.temperatureData = [{ points: [] }];
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });
});
```
```

---

## Phase 3: Execution - Building the Refined Plan

### 3.1 Structure Preservation

**Preserve the original structure unless critique explicitly says to reorganize.**

**Keep:**
- Main section headings
- Step numbering scheme
- Overall flow and narrative
- Introduction and overview sections

**Modify only:**
- Content that has issues
- Steps that need reordering (if dependency issue)
- Sections flagged in critique

---

### 3.2 Integration Approach

**For each issue in the critique:**

1. **Locate** the affected section in current plan
2. **Apply** the appropriate refinement strategy (A-G above)
3. **Verify** the fix doesn't break other sections
4. **Update** any cross-references
5. **Maintain** consistent formatting and style

---

### 3.3 Quality Checks Before Output

Before outputting the refined plan, verify:

**Completeness:**
- [ ] All critical issues addressed
- [ ] All minor issues addressed (or consciously skipped)
- [ ] All strengths preserved
- [ ] No sections accidentally deleted

**Consistency:**
- [ ] Step numbers sequential
- [ ] Cross-references updated
- [ ] Consistent terminology
- [ ] Uniform formatting

**Executability:**
- [ ] Each step has clear action
- [ ] Code examples are complete and runnable
- [ ] Verification criteria are specific
- [ ] Dependencies are explicit

**Clarity:**
- [ ] No ambiguous instructions
- [ ] File paths and names provided
- [ ] Expected outputs specified
- [ ] Time estimates included

---

## Phase 4: Output Format

### 4.1 What to Output

**Output ONLY the complete refined plan as markdown.**

**DO NOT include:**
- ❌ This meta-instruction document
- ❌ Explanations of what you changed
- ❌ Diffs or change summaries
- ❌ Commentary about the critique
- ❌ Preamble or conclusion about the refinement

**DO include:**
- ✅ Complete refined plan from beginning to end
- ✅ All sections (even unchanged ones)
- ✅ All fixes integrated seamlessly
- ✅ Proper markdown formatting

---

### 4.2 Output Template Structure

Your output should follow this general structure (adapt to specific plan):

```markdown
# [Plan Title]

## Overview
[High-level description of what will be implemented]

## Prerequisites
- [Dependency 1]
- [Dependency 2]
- [etc.]

## Implementation Steps

### Phase 1: [Phase Name]

#### Step 1.1: [Step Name]

**Action:**
[What to do]

**Implementation:**
```[language]
[Complete code]
```

**Verification:**
- [Specific test 1]
- [Specific test 2]

**Expected Outcome:**
[What success looks like]

**Time Estimate:** [X minutes/hours]

**Dependencies:** [What must be done first]

---

#### Step 1.2: [Step Name]

[... repeat pattern ...]

---

### Phase 2: [Phase Name]

[... continue ...]

---

## Testing & Validation

[Comprehensive testing section]

---

## Rollback Procedure

[How to undo if things go wrong]

---

## Known Issues & Limitations

[Any caveats or edge cases still not handled]

---

## Success Criteria

[How to know the entire implementation is complete]
```

---

## Phase 5: Special Cases & Advanced Scenarios

### Scenario A: Score 8-9 (Minor Polish)

**What it means:** Plan is mostly good, just needs small improvements

**Approach:**
- Address minor issues without disrupting working sections
- Focus on clarity and specificity
- Add verification where weak
- Don't over-refine

**Example fixes:**
- Add missing import statement
- Clarify vague file path
- Add specific test case
- Fix typo in command

---

### Scenario B: Score 5-7 (Significant Revision)

**What it means:** Plan has critical flaws, needs substantial work

**Approach:**
- Fix all critical issues first (execution-blocking)
- Reorder steps if sequencing is broken
- Add missing core steps
- Beef up error handling significantly
- May need to restructure sections

**Example fixes:**
- Add entire missing phase
- Reorder 5+ steps for correct dependencies
- Add comprehensive error handling throughout
- Replace vague steps with specific implementations

---

### Scenario C: Score <5 (Major Overhaul)

**What it means:** Plan is fundamentally flawed

**Approach:**
- Consider if plan addresses the original task at all
- May need to restructure entire plan
- Add multiple missing phases
- Complete rewrite of vague sections
- Extensive verification added throughout

**Example fixes:**
- Add 3 new major phases
- Complete rewrite of implementation approach
- Add all error handling, verification, testing
- Fix fundamental technical errors

---

### Scenario D: Conflicting Critique Points

**What to do when critique has contradictory suggestions:**

**Example:** 
- Issue #1: "Too vague, add more detail"
- Issue #5: "Too verbose, simplify Step 3"

**Resolution:**
1. Look at which is critical vs minor
2. Critical issues take precedence
3. For same priority: Balance both concerns
   - Add detail where execution requires it
   - Simplify prose without losing critical information

---

### Scenario E: Preserving Existing Good Code

**When critique says:** "Add error handling to Step 7" but Step 7 already has working code

**Approach:**
```typescript
// EXISTING (keep this):
function processData(data) {
  const result = transform(data);
  return result;
}

// ADD (integrate with existing):
function processData(data) {
  // NEW: Validation
  if (!data) {
    console.error('Data is null or undefined');
    return null;
  }

  // NEW: Error handling
  try {
    // EXISTING: Keep original logic
    const result = transform(data);
    return result;
  } catch (error) {
    // NEW: Error recovery
    console.error('Transform failed:', error);
    return getDefaultData();
  }
}
```

**Don't replace working code entirely - wrap it with improvements.**

---

## Phase 6: Final Checklist

Before submitting the refined plan, verify:

### Critical Requirements
- [ ] **All critical issues fixed** - None remain unaddressed
- [ ] **Plan is executable** - Someone could follow it step-by-step
- [ ] **Strengths preserved** - Good sections untouched
- [ ] **Complete output** - No placeholders like "TODO" or "[add details]"

### Quality Requirements
- [ ] **Specific, not vague** - File paths, function names, exact commands
- [ ] **Verifiable** - Clear success criteria for each step
- [ ] **Error handling** - Defensive code where needed
- [ ] **Edge cases** - Boundary conditions handled

### Formatting Requirements
- [ ] **Valid markdown** - Proper headers, code blocks, lists
- [ ] **Consistent style** - Uniform formatting throughout
- [ ] **Code blocks tagged** - Language specified (```typescript, ```bash, etc.)
- [ ] **No meta-content** - Only the plan itself, no explanations

---

## Remember

**You are outputting THE REFINED PLAN, not a report about the plan.**

**The refined plan should be:**
- ✅ Complete (beginning to end)
- ✅ Improved (all issues fixed)
- ✅ Executable (ready to implement)
- ✅ Clear (no ambiguity)

**The refined plan should NOT be:**
- ❌ A diff or changelog
- ❌ A summary of changes
- ❌ Commentary about the critique
- ❌ Partial or incomplete

---

**Now, examine the critique, apply the appropriate strategies, and output the complete refined plan.**

---
# Current Plan:


The implementation plan is in `.cursor/issues/threshold-configuration-admin-ui-plan.md`. Summary:

---

## Plan Overview

**4 phases, 20 steps, ~22h total**

| Phase | Focus | Est. |
|-------|--------|------|
| **Phase 1** | Data model & backend (migration, process_type, weld_thresholds table, threshold service, GET/PUT API) | 6h |
| **Phase 2** | Scoring wiring (extract_features angle_target, score_session thresholds, active_threshold_spec in score API) | 5h |
| **Phase 3** | Admin UI (types, API, AngleArcDiagram, layout, tabs/form with validation) | 6h |
| **Phase 4** | Micro-feedback & callouts (generateMicroFeedback thresholds, Replay + WelderReport + ScorePanel + demo, tests) | 5h |

---

## Decisions Applied

- **process_type column** on sessions (default `mig`), backfilled in migration
- **TIG spec**: 75° ±10° (typo fix)
- **`heat_diss_consistency`** added to weld_thresholds model
- **Cache** invalidated on PUT
- **`active_threshold_spec`** in score API response
- **Micro-feedback** takes optional thresholds; Replay passes them from the score
- **Standalone** `/admin` route group

---

## Files

**Create:** `backend/models/thresholds.py`, `backend/services/threshold_service.py`, `backend/routes/thresholds.py`, `backend/alembic/versions/004_weld_thresholds_and_process_type.py`, `my-app/src/types/thresholds.ts`, `my-app/src/components/admin/AngleArcDiagram.tsx`, `my-app/src/app/admin/layout.tsx`, `my-app/src/app/admin/thresholds/page.tsx`

**Modify:** `backend/database/models.py`, `backend/models/session.py`, `backend/features/extractor.py`, `backend/scoring/rule_based.py`, `backend/routes/sessions.py`, `backend/main.py`, `my-app/src/lib/api.ts`, `my-app/src/lib/micro-feedback.ts`, `my-app/src/app/replay/[sessionId]/page.tsx`, `my-app/src/app/seagull/welder/[id]/page.tsx`, `my-app/src/components/welding/ScorePanel.tsx`, `my-app/src/app/demo/team/[welderId]/page.tsx`

---

The plan matches the create-plan-autonomous output structure: phases, steps with file/code/verification/dependencies, risk heatmap, pre-flight checklists, success criteria, and a progress tracker. All CRITICAL steps have full code; non-critical steps have concise snippets.


---
# Standard Critique (issues to fix):


{"overall_strategy_score": 8.2, "top_3_avg_priority": 8.61, "context_type": "fundraise", "goal_alignment_score": 8.5, "task_scores": [{"task_number": 1, "task_title": "Phase 1 — Data model & backend (migration, process_type, weld_thresholds table, threshold service, GET/PUT API)", "strategic_value": 9.0, "strategic_value_after_penalty": 9.0, "context_alignment": 9.0, "leverage_optionality": 8.0, "resource_efficiency": 6.0, "risk_balance": 6.0, "dependencies": 10.0, "execution_quality": 9.0, "overall_score": 8.38, "feedback_penalty_applied": false, "ships_wrong_risk": "Migration 1.2 could fail on existing DB if sessions table structure differs or backfill hits constraint; wrong down_revision breaks alembic chain.", "higher_leverage_alternative": null, "reasoning": "Foundation for all threshold functionality; well-specified with full migration code and verification."}, {"task_number": 2, "task_title": "Phase 2 — Wire thresholds into scoring (extract_features angle_target, score_session thresholds, active_threshold_spec in score API)", "strategic_value": 9.0, "strategic_value_after_penalty": 9.0, "context_alignment": 9.0, "leverage_optionality": 8.0, "resource_efficiency": 7.0, "risk_balance": 7.0, "dependencies": 5.0, "execution_quality": 9.0, "overall_score": 8.38, "feedback_penalty_applied": false, "ships_wrong_risk": "Score API may return wrong active_threshold_spec if session.process_type is missing before migration backfill runs, or cache returns stale thresholds after PUT.", "higher_leverage_alternative": null, "reasoning": "Core value prop — scores must reflect configured spec; depends on Phase 1."}, {"task_number": 3, "task_title": "Phase 3 — Admin UI (types, API, AngleArcDiagram, layout, tabs/form with validation)", "strategic_value": 10.0, "strategic_value_after_penalty": 10.0, "context_alignment": 10.0, "leverage_optionality": 8.0, "resource_efficiency": 7.0, "risk_balance": 9.0, "dependencies": 5.0, "execution_quality": 8.0, "overall_score": 9.06, "feedback_penalty_applied": false, "ships_wrong_risk": "Form allows warning > critical if frontend validation is bypassed; backend 422 mitigates but UX degrades.", "higher_leverage_alternative": "Could ship tabs + angle-only form first (2h), defer thermal/amps/volts/heat_diss to a follow-up.", "reasoning": "Admin UI is the primary configuration surface; directly enables 'calibrate to your spec' demo."}, {"task_number": 4, "task_title": "Phase 4 — Micro-feedback & callouts (generateMicroFeedback thresholds, Replay + WelderReport + ScorePanel + demo, tests)", "strategic_value": 8.0, "strategic_value_after_penalty": 8.0, "context_alignment": 9.0, "leverage_optionality": 6.0, "resource_efficiency": 7.0, "risk_balance": 8.0, "dependencies": 6.0, "execution_quality": 8.0, "overall_score": 7.69, "feedback_penalty_applied": false, "ships_wrong_risk": "Replay passes partial thresholds (angle only); thermal_symmetry still uses hardcoded 20/40 — admin changes to thermal thresholds won't affect micro-feedback.", "higher_leverage_alternative": "Add thermal_symmetry fields to active_threshold_spec in Step 2.3 so Replay can pass full thresholds; ~0.5h extra.", "reasoning": "Callouts deliver visibility; micro-feedback thermal gap weakens 'fully configured' story."}], "critical_issues": ["Step 4.2 builds thresholdsForMicroFeedback from active_threshold_spec but only angle_target/warning/critical — thermal_symmetry_warning_celsius, thermal_symmetry_critical_celsius, etc. are hardcoded (20, 40, 5, 1, 40). Admin edits to thermal/amps/volts/heat_diss thresholds will not affect micro-feedback.", "Migration 1.2: down_revision '003_add_score_total' must match actual revision id in 003 file; if 003 uses different revision string, upgrade chain breaks."], "minor_issues": ["PUT /api/thresholds/:weld_type returns get_all_thresholds() — refetch overhead; returning single updated threshold would suffice.", "AngleArcDiagram Step 3.2: largeArc flag (angleTargetDegrees > 180) is dead — arc is 0 to angleTargetDegrees, which is always ≤90 per WeldTypeThresholds validation."], "suggestions": ["Extend active_threshold_spec in Step 2.3 to include thermal_symmetry_warning_celsius, thermal_symmetry_critical_celsius, amps_stability_warning, volts_stability_warning, heat_diss_consistency so Replay and micro-feedback can pass full thresholds.", "Add GET/PUT threshold API test before Phase 3 to catch integration issues early.", "Validate down_revision against alembic history before running migration."], "dependency_order": "Phase 1 (Steps 1.1–1.7) must complete first. Phase 2 (2.1–2.3) and Phase 3 (3.1–3.4) can run in parallel after Phase 1; Phase 3 needs Step 1.5 (API) registered. Phase 4 depends on 2.3 (active_threshold_spec) and 3.1 (types); Steps 4.2–4.5 depend on 4.1.", "premortem_top_risk": "Migration 1.2 fails on production DB due to sessions table structure mismatch, constraint, or missing 003 revision; blocks entire plan.", "goal_alignment_notes": "All four phases serve the procurement calibration goal. Admin configures; scoring uses config; reports show spec. Phase 4 thermal gap is the main alignment shortfall — partial threshold passthrough undermines 'fully calibrated' narrative.", "context_aware_feedback": {"current_context_type": "fundraise", "penalties_applied": [], "penalties_skipped": []}}

---
# Adversarial Findings (also must fix):


```json
{
  "score": 5.9,
  "adversarial_verdict": "This plan will run in a single-worker dev setup but will fail or corrupt data in production due to multi-worker cache invalidation, empty-cache KeyError, hardcoded thermal fallback in micro-feedback, and unvalidated migration/backfill compatibility.",
  "sabotage_vectors": [
    {
      "step": "1.2",
      "attack": "Migration adds process_type with server_default after backfill. On DBs with CHECK constraints, triggers, or replication lag, the UPDATE/alter sequence can fail. The plan assumes a vanilla PostgreSQL schema.",
      "detection_gap": "Verification runs alembic upgrade on a clean dev DB. Prod DB may have extensions, read replicas, or different 003 state.",
      "blast_radius": "Entire plan blocked; weld_thresholds table never created; scoring stays on hardcoded constants"
    },
    {
      "step": "1.4",
      "attack": "Module-level _threshold_cache is process-local. Admin PUTs on worker A; invalidate_cache() only affects worker A. The next score request on worker B uses stale thresholds.",
      "detection_gap": "Verification uses a single process. No multi-worker test. 'No DB hit per score' is validated in isolation.",
      "blast_radius": "Steps 2.3, 4.1, 4.2, 4.3, 4.4 — all scoring and micro-feedback use wrong thresholds until process restart"
    },
    {
      "step": "1.4",
      "attack": "get_thresholds(db, 'mig') when _threshold_cache is empty (e.g. weld_thresholds truncated or seed failed) does: _threshold_cache.get(key, _threshold_cache['mig']). The default triggers KeyError because 'mig' is not in an empty dict.",
      "detection_gap": "Verification assumes seeded data. No test for empty table. Pass criteria never check behaviour when table has 0 rows.",
      "blast_radius": "502/500 on every score request; Replay, WelderReport, ScorePanel all break"
    },
    {
      "step": "1.5",
      "attack": "PUT commits before invalidate_cache(). If invalidate_cache() or the return path raises, client may see 200 but response can be incomplete. If db.commit() succeeds and a later line raises, client gets 500 and may retry — duplicate PUT risk.",
      "detection_gap": "Verification only checks 200 and body. No assertion on exception behaviour or idempotency.",
      "blast_radius": "Admin retries; possible double-write if backend is flaky"
    },
    {
      "step": "2.3",
      "attack": "getattr(session_model, 'process_type', None) assumes SessionModel has process_type. If 1.3 is skipped or incomplete, SessionModel may not load process_type. 2.3 depends on 1.3 but the dependency list omits it.",
      "detection_gap": "Dependency says 1.4, 2.1, 2.2 — 1.3 is not listed. Running 2.3 before 1.3 yields None for process_type → all sessions scored as mig.",
      "blast_radius": "TIG/stick/flux_core sessions scored with MIG thresholds"
    },
    {
      "step": "3.4",
      "attack": "current = all.find(t => t.weld_type === active) ?? form. When fetchThresholds fails, all=[], current=form (initial empty). setForm({...current}) yields {}. User edits one field and saves. updateThreshold sends partial body. Backend WeldThresholdUpdate requires all fields — 422. If backend ever accepted partial, other threshold fields would be overwritten with undefined.",
      "detection_gap": "Verification uses 'Backend running'. No test for fetch failure. No assertion that form is fully populated before save.",
      "blast_radius": "Admin saves with stale/partial data; DB could get partial overwrite if API contract changes"
    },
    {
      "step": "4.2",
      "attack": "thresholdsForMicroFeedback builds WeldTypeThresholds from active_threshold_spec but hardcodes thermal_symmetry_warning_celsius: 20. active_threshold_spec only has angle fields. TIG seed has thermal 60/80. Micro-feedback thermal check uses 20 — stricter than TIG's 60. TIG sessions get thermal warnings that MIG wouldn't.",
      "detection_gap": "Verification says 'TIG session shows feedback based on 75°' — only angle checked. Thermal threshold mismatch is never asserted.",
      "blast_radius": "Micro-feedback thermal rules wrong for TIG/stick/flux_core; inconsistent with score rules"
    },
    {
      "step": "4.6",
      "attack": "test_put_threshold_invalidates_cache says 'Verify cache refreshed (e.g. next score uses new value)'. No assertion code shown. A test that only checks PUT 200 would pass. Cache could remain stale; test never detects it.",
      "detection_gap": "Verification is a comment, not an assertion. No second score request to compare threshold values.",
      "blast_radius": "Cache invalidation bug ships; no test catches it"
    }
  ],
  "precondition_gaps": [
    "Step 1.2 assumes down_revision 003_add_score_total. No verification that 003 exists or that local alembic history matches deployed state.",
    "Step 2.3 implicitly requires Step 1.3 (SessionModel.process_type). Dependency graph omits it.",
    "backfill_score_total.py is never updated. It uses extract_features(session) and score_session(session, features). After plan: extract_features gets default angle_target=45, score_session gets thresholds=None. For migrated sessions (all process_type=mig) this matches. Plan never documents this; future edits may assume backfill uses thresholds.",
    "CreateSessionRequest and SessionModel must include process_type before 1.7/2.3. Step 1.7 adds to create_session; 1.3 adds to SessionModel. Order is correct but precondition not stated."
  ],
  "untestable_verifications": [
    "Step 2.3: 'Rules use thresholds (e.g. angle_consistency threshold = angle_warning)' — no assertion code. Wrong implementation using MIG constants could pass manual check.",
    "Step 3.2: 'Arc spans correct angle' — no DOM assertion. Wrong SVG math could render something that looks like an arc.",
    "Step 3.4: 'Validation errors display' — no selector. 422 could be swallowed; form could show nothing.",
    "Step 4.1: 'No regression for existing callers' — generateMicroFeedback has one caller. Plan doesn't list all callers.",
    "Success criterion 11: 'No DB hit per score' — verify by 'Log or profile'. No automated assertion."
  ],
  "cascade_points": [
    "Step 1.2 is single point of failure for Phase 1. Migration fails → no weld_thresholds, no process_type. Steps 1.3–1.7, all Phase 2, and threshold-dependent Phase 4 steps blocked.",
    "Step 1.4: threshold_service used by 1.5, 2.3. If cache is wrong, every score is wrong. No downstream step re-validates threshold correctness.",
    "Step 2.1: extract_features called by get_session_score (2.3) and backfill_score_total. If angle_target_deg is wrong, angle_max_deviation is wrong for all process types."
  ],
  "phase_boundary_gaps": [
    "Phase 1 → 2: No explicit handoff check. Partial Phase 1 (e.g. migration ran, router not registered) leaves inconsistent state.",
    "Phase 2 → 3: Frontend types must match backend active_threshold_spec. No versioning or schema test.",
    "Phase 3 → 4: Replay fetches score in separate useEffect. microFeedback recomputes when primaryScore loads. First render uses defaults; second uses correct. Brief visual inconsistency."
  ],
  "pre_mortem": {
    "partial_execution": "We stopped after Phase 1 and 2. State: migration 004 applied, process_type backfilled, weld_thresholds seeded. Rollback requires alembic downgrade to 003 (drops weld_thresholds and process_type). Plan does not document partial rollback.",
    "silent_corruption": "Production uses 4 uvicorn workers. Admin changed TIG to 70°. PUT hit worker 1; cache invalidated only there. Workers 2–4 kept 75°. TIG sessions scored with stale target; Replay showed inconsistent callout vs score.",
    "prod_vs_local": "Staging had weld_thresholds empty (seed failed). get_thresholds raised KeyError on _threshold_cache['mig']. All score requests returned 500.",
    "dependency_drift": "Pydantic 2.x changed validation; angle_target_degrees=0 started failing Field(ge=0). Admin could not save 0. Plan never tested 0 and 90."
  },
  "critical_issues": [
    "Step 1.4: get_thresholds KeyError when _threshold_cache is empty. Handle empty table or raise explicit error.",
    "Step 1.4: Module-level cache invalidates only in-process. Multi-worker deployment serves stale thresholds. Use shared cache (Redis) or per-request load when workers > 1.",
    "Step 4.2: active_threshold_spec omits thermal/amps/volts/heat_diss. Micro-feedback hardcodes 20/40/5/1/40. TIG thermal is 60. Extend active_threshold_spec or fetch full thresholds client-side."
  ],
  "latent_issues": [
    "backfill_score_total.py not updated. Works today (all migrated = mig). Document or update backfill to use get_thresholds.",
    "Step 4.6: test_put_threshold_invalidates_cache has no real assertion. Add: after PUT, GET score and assert active_threshold_spec matches new value.",
    "AngleArcDiagram (3.2): computed d and largeArc unused; path hardcoded. Angles > 90° may render wrong."
  ],
  "blind_spots": [
    "Plan assumes single uvicorn worker. No mention of Gunicorn/uvicorn workers. Cache design is single-process.",
    "Plan assumes active_threshold_spec is sufficient for micro-feedback. Spec has 4 angle fields. Thermal/amps/volts/heat_diss used by generateMicroFeedback are not in spec.",
    "Plan assumes NEXT_PUBLIC_API_URL is set for Phase 3. Admin page calls /api/thresholds; wrong URL yields empty form."
  ],
  "minor_issues": [
    "Step 1.1 verification: 'run from backend/' — specify exact command.",
    "Step 3.3: 'Layout renders' — no assertion.",
    "Step 4.5: Demo uses static 'MIG spec — 45° ±5°'. If MIG defaults change, demo inconsistent.",
    "Success criterion 3: 'Visual check' for arc — not automatable."
  ],
  "what_the_author_cant_see": "The plan targets a single-process dev setup. The cache, verification, and micro-feedback thresholds fit that model. Production uses many workers; thermal fields are missing from active_threshold_spec; and the empty-cache path is never exercised. The author assumes one backend process and sequential edits.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Step 1.4: Add empty-cache guard. If _threshold_cache empty after _load_all, raise explicit error or return hardcoded mig defaults.",
    "Step 1.4: Document multi-worker limitation or add shared cache. For MVP, document and add to backlog.",
    "Step 2.3: Extend active_threshold_spec with thermal_symmetry_warning_celsius so Step 4.2 can use correct TIG thermal threshold.",
    "Step 4.2: Use thermal from active_threshold_spec when present; fallback to 20 only when spec lacks it.",
    "Step 4.6: Add explicit test: PUT threshold, then GET score, assert active_threshold_spec matches new values.",
    "Dependencies: Add Step 1.3 to Step 2.3. Document backfill_score_total.py use of thresholds=None and why it is correct."
  ]
}
```
