
Here is the adversarial critique as valid JSON:

```json
{
  "score": 6.2,
  "adversarial_verdict": "This plan will execute in a clean dev environment but contains several undetected failure modes: SQLite CI migration will likely fail on op.alter_column; existing test_get_session_score requires weld_thresholds seeding that the plan does not specify; THERMAL_VARIANCE_THRESHOLD_CELSIUS (20) contradicts seed/fallback (60); and Step 2.3 does not mandate how the route loads frames, so a future refactor could break the <10 frames check.",
  "sabotage_vectors": [
    {
      "step": "1.2",
      "attack": "Run alembic upgrade head against SQLite (as CI suggests). op.alter_column with nullable=False and server_default on SQLite triggers 'SQLite does not support ALTER COLUMN'. Migration fails; CI red or CI skips migration entirely.",
      "detection_gap": "CI checkpoint says 'Run migration against SQLite' but migration uses PostgreSQL-specific ALTER. No batch_alter_table for SQLite. Plan assumes 'syntax errors' will be caught—ALTER failure is runtime, not syntax.",
      "blast_radius": "CI either fails or team removes migration from CI; first prod run is first ever; prod-specific failure surfaces late"
    },
    {
      "step": "1.2",
      "attack": "preflight_migration.py passes (DB writable, sessions <= 1000) but alembic is at 001 or 002, not 003. Operator runs upgrade; migration 004's down_revision chain runs 002→003→004. If 002/003 have schema assumptions that conflict, migration fails mid-chain.",
      "detection_gap": "Pre-flight never verifies alembic current revision. Operator can run pre-flight then upgrade from wrong state.",
      "blast_radius": "Partial migration; sessions or weld_thresholds in inconsistent state"
    },
    {
      "step": "1.4",
      "attack": "Worker A handles PUT, invalidates its cache. Worker B serves GET /score. Worker B's cache remains loaded. User sees old thresholds for one request. In Gunicorn/uvicorn multi-worker, this is guaranteed under load.",
      "detection_gap": "test_put_threshold_invalidates_cache runs in single-process TestClient. No multi-worker test. Verification passes while prod serves stale data.",
      "blast_radius": "Admin saves TIG=80°; welder opens replay; sees 75° in callout; confusion or wrong micro-feedback thresholds"
    },
    {
      "step": "2.3",
      "attack": "Implementer changes get_session_score to use a different query pattern (e.g. separate frames query, or selectinload removed during refactor). Frames not eager-loaded. frames = getattr(session_model, 'frames', None) returns lazy loader; len(frames) triggers load but relationship could be unloaded/empty in edge case. Or: session loaded without options(joinedload) in a code path; frames list is empty; valid 20-frame session returns 400.",
      "detection_gap": "Plan says 'Ensure session loaded with frames' but code snippet does not show the query. No test asserts that 10-frame session returns 200 vs 9-frame returns 400. Implementer could omit selectinload.",
      "blast_radius": "Valid sessions falsely rejected with 400; WelderReport blank for some sessions"
    },
    {
      "step": "2.3",
      "attack": "get_thresholds(db, process_type) raises ValueError when tig row missing. Route does not catch it. FastAPI returns 500. User sees generic error; no distinction from 'session not found' or 'insufficient frames'.",
      "detection_gap": "Verification checks that get_thresholds raises when tig missing. It does not verify that the route catches ValueError and returns 503 or structured error.",
      "blast_radius": "TIG sessions fail with 500 when DB corrupted; no actionable message"
    },
    {
      "step": "4.1",
      "attack": "generateMicroFeedback(frames, undefined) uses THERMAL_VARIANCE_THRESHOLD_CELSIUS = 20 (current constant). Seed and Step 4.2 fallback use 60/80. Legacy callers get thermal feedback with 20°C threshold. WelderReport callout says 'MIG spec' but micro-feedback uses 20.",
      "detection_gap": "Step 4.1 does not instruct changing THERMAL_VARIANCE_THRESHOLD_CELSIUS from 20 to 60. Verification uses thresholds object; no verification for thresholds=undefined using 60.",
      "blast_radius": "Micro-feedback over-fires on thermal; mismatch with reported spec"
    },
    {
      "step": "4.6",
      "attack": "test_get_session_score.py has its own db_session fixture with Base.metadata.create_all. When Step 2.3 adds get_thresholds, that test hits the DB. If weld_thresholds is not created (test isolates models differently), get_thresholds fails. Plan updates conftest for new tests; does not audit existing tests that hit get_session_score.",
      "detection_gap": "Plan adds WeldThresholdModel import to conftest. test_get_session_score may not use that conftest; has own fixtures. Dependency unclear.",
      "blast_radius": "test_get_session_score fails after Step 2.3; developer fixes by adding seed; plan's conftest and test fixture drift"
    }
  ],
  "precondition_gaps": [
    "Step 1.2: No verification that DATABASE_URL is set before alembic upgrade. preflight_migration.py checks it, but pre-flight is optional. Operator can run alembic directly; fails with opaque error.",
    "Step 1.2: Post-migration seed check uses psql. Windows or container-only env may not have psql. Pre-flight script uses Python; seed verification does not.",
    "Step 2.1: extract_features has no guard for angle_target_deg=0. Caller can pass 0; angle_max_deviation produces wrong metric. No step validates that extract_features rejects or normalizes 0.",
    "Step 4.2: thresholdsForMicroFeedback uses spec.thermal_symmetry_warning_celsius ?? 60. If active_threshold_spec is legacy 4-field, thermal fields are undefined. Fallback 60 is correct. generateMicroFeedback(undefined) uses THERMAL_VARIANCE_THRESHOLD_CELSIUS=20.",
    "Step 4.6: session_factory uses generate_expert_session. Plan assumes it has 10+ frames. No explicit lock. If mock_sessions is refactored, frame count could drop."
  ],
  "untestable_verifications": [
    {
      "step": "4.2",
      "issue": "'Verify markers don't visibly re-render when score arrives'—no DOM selector, no timing assertion. CI cannot automate this. Plan acknowledges in Known Issues but does not replace with automatable check."
    },
    {
      "step": "3.4",
      "issue": "'Save disabled until loaded'—no assertion that Save is disabled. Need data-testid and explicit assert that button has disabled=true when thresholdsLoaded is false."
    },
    {
      "step": "2.3",
      "issue": "'weld_type matches session process_type'—verification is manual. No automated test that creates TIG session, calls GET score, asserts active_threshold_spec.weld_type === 'tig'."
    }
  ],
  "cascade_points": [
    "Step 1.2 is single point of failure for Phase 2, 3, 4. If migration produces wrong seed or partial failure, threshold_service, scoring, admin UI, micro-feedback all operate on wrong data.",
    "Step 1.4: If _load_all throws before _cache_loaded=True, cache stays empty. Every get_thresholds retries. No circuit breaker.",
    "Step 2.2: If thresholds has angle_warning_margin > angle_critical_margin (raw SQL), rules use them as-is. Cascade: angle feedback severity inverted."
  ],
  "phase_boundary_gaps": [
    "Phase 1 to 2: No automated 'Phase 1 complete' gate. Operator could start Phase 2 with migration applied but threshold_service untested.",
    "Phase 2 to 3: Frontend assumes GET /api/thresholds returns 4 items. No handoff verification.",
    "Phase 1 rollback: Reverse case not documented: migration reverted but code still expects process_type. All reads fail."
  ],
  "pre_mortem": {
    "partial_execution": "We got through Phase 1 and Phase 2 but had to stop. The state: weld_thresholds exists; process_type on sessions; get_session_score uses thresholds. If we revert Phase 3, backend returns active_threshold_spec but no UI consumes it. Rollback of Phase 2 would require removing threshold loading from get_session_score—high blast radius.",
    "silent_corruption": "Users reported issues 3 days later. Root cause: multi-worker deployment. Admin changed TIG target 75°→80°. Worker A invalidated cache. Worker B served 75° for GET /score. WelderReports showed 'Target 80°' but replay micro-feedback used 75°. No test caught it.",
    "prod_vs_local": "Staging revealed: CI runs alembic against SQLite. Migration 004 uses op.alter_column. SQLite threw 'ALTER COLUMN not supported'. CI had been green because migration was run against Postgres elsewhere. Staging migration failed.",
    "dependency_drift": "Three months later, SQLAlchemy updated relationship lazy-loading. get_session_score's session_model.frames access changed. len(frames) on detached session returned 0. Sessions with 20 frames returned 400. A refactor had removed joinedload. No regression test enforced it."
  },
  "critical_issues": [
    "Step 1.2: Migration uses op.alter_column which fails on SQLite. Use batch_alter_table for SQLite or run CI migration against PostgreSQL only.",
    "Step 2.3: Plan does not explicitly require joinedload/selectinload in the route. Add: 'Route MUST use .options(joinedload(SessionModel.frames))'.",
    "Step 4.1: THERMAL_VARIANCE_THRESHOLD_CELSIUS (20) must be changed to 60 to match seed and Step 4.2 fallback."
  ],
  "latent_issues": [
    "Step 1.4: Multi-worker cache staleness. Trigger: Gunicorn with multiple workers; admin saves; Worker B serves stale thresholds.",
    "Step 4.6: test_get_session_score fixture does not use conftest; has own create_all. Needs weld_thresholds when Step 2.3 adds get_thresholds.",
    "Step 2.2: Raw SQL could insert row with angle_warning_margin > angle_critical_margin. PUT validates; load path does not."
  ],
  "blind_spots": [
    "Plan assumes generate_expert_session produces 10+ frames. No explicit lock.",
    "Plan assumes THERMAL_VARIANCE_THRESHOLD_CELSIUS is updated. Current constant is 20; seed is 60.",
    "Plan assumes production uses single worker or stale cache is acceptable.",
    "Plan assumes psql is available for post-migration verification."
  ],
  "minor_issues": [
    "Step 1.2 pre-flight: conn.commit() after CREATE/DROP is redundant on PostgreSQL but harmless.",
    "Step 3.2: Unit test could use 90 and assert endAngle === Math.PI/2.",
    "Step 4.5: fetchThresholds().catch(() => null)—on failure we omit callout. Consistent."
  ],
  "what_the_author_cant_see": "The plan assumes sequential execution and single-process dev. Multi-worker cache gap, SQLite ALTER failure, and THERMAL_VARIANCE vs seed mismatch are invisible locally. The author verified seed and cache invalidation in isolation; production runs N workers and CI may use SQLite.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Add batch_alter_table for SQLite in migration 004, or change CI to run alembic against ephemeral PostgreSQL only.",
    "In Step 4.1, add: set THERMAL_VARIANCE_THRESHOLD_CELSIUS = 60 to match seed.",
    "In Step 2.3, add: 'Route MUST use .options(joinedload(SessionModel.frames)) when loading SessionModel'.",
    "Add automated test: TIG session returns active_threshold_spec.weld_type === 'tig'.",
    "Audit test_get_session_score: ensure it seeds weld_thresholds or shares conftest that does."
  ]
}
```
