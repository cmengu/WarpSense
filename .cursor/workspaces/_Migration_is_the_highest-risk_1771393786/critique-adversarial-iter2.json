
Here is the adversarial critique as valid JSON only, with no extra wrapper:

```json
{
  "score": 7.2,
  "adversarial_verdict": "This refined plan will execute successfully in dev and staging. Remaining vulnerabilities: no verification that all four weld_types in weld_thresholds have correct seed values; threshold_service silently falls back to MIG when tig/stick/flux_core row is missing; session with zero frames passes score endpoint with all-zero features; and SQLite migration in CI may hide PostgreSQL-specific failures.",
  "sabotage_vectors": [
    {
      "step": "1.2",
      "attack": "Migration runs successfully but SEED_DATA has a typo (e.g. tig row has angle_target_degrees=45 instead of 75). Post-migration verification only checks '4 rows exist' and 'process_type backfilled', not that each weld_type has the correct numeric values.",
      "detection_gap": "No assertion that weld_thresholds.tig has angle_target_degrees=75. TIG sessions would score with wrong angle target until admin notices.",
      "blast_radius": "All scoring for TIG (or whichever row is wrong); WelderReport shows wrong spec"
    },
    {
      "step": "1.4",
      "attack": "Delete the tig row from weld_thresholds via direct SQL. get_thresholds('tig') falls back to mig via 'if key not in _threshold_cache: key = mig'. TIG sessions score with MIG thresholds.",
      "detection_gap": "Verification only tests get_thresholds('mig') and get_thresholds('unknown'). No test that get_thresholds('tig') returns TIG-specific values when tig row exists, or fails loudly when it does not.",
      "blast_radius": "Phase 2 and 4 scoring, backfill, micro-feedback all use wrong thresholds for non-MIG process types"
    },
    {
      "step": "2.3",
      "attack": "Session exists with frames=[] (e.g. created but add_frames never called, or frames relationship not loaded). extract_features returns all zeros; score_session returns total based on zeros. No 400/404; response looks valid.",
      "detection_gap": "get_session_score does not assert len(session.frames) >= N. Verification does not test session with zero frames. A 'works but meaningless' score is returned.",
      "blast_radius": "WelderReport and Replay show 100/100 for empty session; users trust incorrect data"
    },
    {
      "step": "1.5",
      "attack": "PUT succeeds, db.commit() succeeds, but invalidate_cache() is in finally. If a second PUT from another request interleaves—request A commits, B commits, both invalidate—cache reload could race. Low probability but not impossible with async.",
      "detection_gap": "test_put_threshold_invalidates_cache asserts PUT then GET score returns new value. Does not assert that concurrent PUTs preserve consistency.",
      "blast_radius": "Stale or inconsistent cache under load"
    },
    {
      "step": "3.4",
      "attack": "User opens /admin/thresholds, fetch succeeds, but network drops before Save. User edits, clicks Save. Request fails (timeout, 5xx). No retry logic; form still shows edited values. User believes save succeeded.",
      "detection_gap": "Verification does not require Save button to show loading/error state or disable on submit. No assertion that failed PUT surfaces error to user.",
      "blast_radius": "Admin thinks thresholds updated; production still uses old values"
    },
    {
      "step": "4.6",
      "attack": "Test imports SessionModel and WeldThresholdModel but conftest or fixture ordering loads models after another test's engine. weld_thresholds table never created because WeldThresholdModel not in Base.metadata when create_all ran.",
      "detection_gap": "Plan says 'import WeldThresholdModel before Base.metadata.create_all' but fixture order is test-order dependent. test_thresholds_api could run before any test that imported WeldThresholdModel.",
      "blast_radius": "Tests fail with 'no such table weld_thresholds' in some orderings; flaky CI"
    }
  ],
  "precondition_gaps": [
    "Step 1.2 assumes down_revision '003_add_score_total' exists. No verification that alembic/versions/003*.py has revision = '003_add_score_total'. If 003 file was renamed or revision string differs, migration chain breaks.",
    "Step 4.2 assumes active_threshold_spec includes thermal_symmetry_warning_celsius. If backend ever returns legacy 4-field spec without these, ?? 20 fallback applies. Seed has 60—micro-feedback would use wrong thermal threshold.",
    "Step 4.6 session_factory uses generate_expert_session which returns Pydantic Session. SessionModel.from_pydantic requires Session to have process_type (added in 1.3). Fixture order: session_factory depends on seeded_weld_thresholds; seeded_weld_thresholds must create weld_thresholds table. Conftest must ensure WeldThresholdModel is imported before any create_all."
  ],
  "untestable_verifications": [
    "Step 3.4: 'verify markers don't visibly re-render when score arrives' is subjective. Throttle test is manual. No DOM assertion for CI. Passes even if markers flash.",
    "Step 3.2: 'Arc spans correct angle (visual: 45° arc)' — no numeric assertion. A wrong formula (e.g. endAngle = angleTargetDegrees * Math.PI) could render a full semicircle; visual check is human-only.",
    "Step 1.3: 'SessionModel round-trip preserves process_type' — no concrete test command. 'Run existing session tests' could pass if tests don't assert process_type."
  ],
  "cascade_points": [
    "Step 1.2 is single point of failure for Phase 2, 3, 4. If migration partially fails (e.g. ALTER to NOT NULL fails because UPDATE didn't reach all rows in a race), schema is inconsistent. No automated rollback beyond alembic downgrade -1.",
    "Step 1.4 get_thresholds is used by 1.8, 2.3, 1.9, 4.2. If _load_all throws (e.g. corrupt DB row with angle_target_degrees=0 triggers Pydantic ValidationError), every caller fails. Plan does not handle corrupt rows in _load_all."
  ],
  "phase_boundary_gaps": [
    "Phase 1 to 2: Phase 2 assumes extract_features and score_session accept new params. If Step 2.1 is applied before 2.2, callers passing old signature break. No explicit ordering in plan.",
    "Phase 3 to 4: Admin thresholds page (3.4) and Replay (4.2) both consume active_threshold_spec. If backend returns optional thermal/amps/volts as undefined, frontend fallbacks may diverge (20 vs 60 for thermal)."
  ],
  "pre_mortem": {
    "partial_execution": "Stopping after Phase 1 leaves weld_thresholds table and process_type column in place. If code is reverted but downgrade is not run, next deployment has DB schema ahead of code. SessionModel.from_pydantic would fail if code expects process_type but column was dropped. Rollback procedure says run downgrade -1 but does not cover 'code reverted, migration not reverted' scenario.",
    "silent_corruption": "If tig row is deleted from weld_thresholds (manual SQL, bug in a future migration), get_thresholds('tig') returns MIG thresholds. TIG sessions score as if MIG. WelderReport would show 'Evaluated against MIG spec' for TIG sessions. Users might not notice for days.",
    "prod_vs_local": "CI runs alembic upgrade head against SQLite. PostgreSQL-specific behaviour (e.g. concurrent UPDATE behaviour, locking) is never exercised. First production run could fail in ways CI never saw. Migration preview is manual.",
    "dependency_drift": "Pydantic v2 Field(gt=0) behaviour is stable. If a future Pydantic minor changes ValidationError handling for NaN or Inf, model validation could change. Plan does not pin Pydantic. Low probability."
  },
  "critical_issues": [
    "Step 2.3: get_session_score does not reject sessions with 0 frames. Returns valid-looking score with all-zero features. Add explicit check: if not session.frames or len(session.frames) < 10: raise HTTPException(400, 'Session has insufficient frames for scoring')."
  ],
  "latent_issues": [
    "Step 1.4: Missing weld_thresholds row for tig/stick/flux_core causes silent fallback to mig. No alert. Add integrity check or fail loudly when requested process_type not in cache.",
    "Step 1.2: CI migration uses SQLite. PostgreSQL-specific DDL or locking differences will not surface until production.",
    "Step 1.4: When _load_all builds WeldTypeThresholds from a corrupt row (angle_target_degrees=0), Pydantic raises ValidationError. No try/except; entire request fails. Not silent but ungraceful."
  ],
  "blind_spots": [
    "Plan assumes DATABASE_URL is set and points to primary. No step verifies this before migration. Pre-flight has manual check; automation could proceed with wrong env.",
    "Plan assumes generate_expert_session produces Session with 10+ frames. It does (~1500). But session_factory could be reused with a different generator; assert len(session.frames) >= 10 is correct.",
    "Plan assumes ScorePanel and WelderReport receive full score object with active_threshold_spec. If a different code path supplies score without the spec (e.g. cached aggregate), callout would not render. No audit of all score sources."
  ],
  "minor_issues": [
    "Step 1.7 verification: 'GET returns process_type' — no example response snippet. Could add expected JSON key.",
    "Step 3.3: document.querySelector('nav a[href=\"/admin/thresholds\"]') — href might be relative. Next.js Link could render differently.",
    "Step 1.9: 'Known Issues documents limitation' is acceptable pass criteria. No automated check that Known Issues was updated."
  ],
  "what_the_author_cant_see": "The plan was written assuming the happy path: all four seed rows exist, sessions have frames, backend always returns full active_threshold_spec. The fallbacks (mig when tig missing, default thresholds when score fails, zeros when no frames) are correct individually but create a landscape where multiple things can be wrong and the system still 'works'. The author's mental model is 'if it breaks, we'll see it'. Production will have sessions with 0 frames, manual DB edits, and legacy API responses. Those paths are not exercised.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Add to Step 2.3: assert len(session.frames) >= 10 before scoring; return 400 with clear message if insufficient frames",
    "Add to Step 1.2 verification: assert weld_thresholds has correct angle_target_degrees for tig (75), stick (20), flux_core (45)",
    "Add to Step 1.4: when key not in _threshold_cache for a known process_type (mig/tig/stick/flux_core), log error and consider raising instead of silent mig fallback",
    "Add to conftest or test_thresholds_api: explicit import of WeldThresholdModel before Base.metadata.create_all in fixture that creates engine"
  ]
}
```
