
```json
{
  "phase_completed": "Plan Critique Fixes — Exploration",
  "phase_goal_achieved": true,
  "goal_delta": "Plan fixes are validated and documented; implementation not started.",

  "decisions_locked": [
    {
      "topic": "invalidate_cache threading",
      "chosen": "invalidate_cache acquires _load_lock before setting _cache_loaded = False",
      "reverting_this_requires": "Reintroduces cache correctness bug under concurrent requests"
    },
    {
      "topic": "Step 3.4 form parseFloat",
      "chosen": "parseFloat(e.target.value) with no || 0 fallback; angle_target_degrees > 0 required; backend 422 on 0",
      "reverting_this_requires": "Silent NaN/0 bug; cleared fields submit 0"
    },
    {
      "topic": "Step 4.6 session_factory fixture",
      "chosen": "Use generate_expert_session + model_copy(update={\"process_type\":\"mig\"}); explicit fixture code in exploration",
      "reverting_this_requires": "test_put_threshold_invalidates_cache fails or passes for wrong reason"
    },
    {
      "topic": "Step 4.2 micro-feedback gating",
      "chosen": "Gate micro-feedback on sessionData?.frames AND primaryScore; wait for both before computing",
      "reverting_this_requires": "Visual flicker when score loads after frames"
    },
    {
      "topic": "Step 1.2 pre-flight",
      "chosen": "If sessions >1000 rows: run UPDATE manual first, verify row count; alembic upgrade head --sql > migration_preview.sql before upgrade",
      "reverting_this_requires": "Partial migration failure risk on large DBs"
    },
    {
      "topic": "ActiveThresholdSpec optional fields",
      "chosen": "thermal/amps/volts/heat_diss optional for legacy API; document in Step 3.1",
      "reverting_this_requires": "Breaks backward compat if made required"
    },
    {
      "topic": "PUT validation asymmetry",
      "chosen": "Angle/thermal: warning ≤ critical enforced; amps/volts/heat_diss: no ordering; add comment in Step 1.5",
      "reverting_this_requires": "Reviewer confusion only"
    }
  ],

  "files_state": {
    "created": [
      ".cursor/explore/plan-critique-fixes-exploration.md"
    ],
    "modified": [],
    "deleted": []
  },

  "contracts_established": [
    "invalidate_cache() holds _load_lock for full duration of _cache_loaded = False write",
    "Backend PUT /api/thresholds/:weld_type rejects angle_target_degrees=0 with 422",
    "isCompleteForm requires angle_target_degrees > 0 and no parseFloat fallback to 0",
    "session_factory: SessionModel via generate_expert_session, model_copy(update={\"process_type\":\"mig\"}), 10+ frames",
    "Step 4.2 useMemo deps: [sessionData?.frames, primaryScore] — both required for micro-feedback"
  ],

  "assumptions_now_baked_in": [
    "generate_expert_session exists in test harness and produces valid session with frames",
    "WeldThresholdModel imported before Base.metadata.create_all in test fixtures",
    "alembic down_revision 003_add_score_total; migration 004 follows",
    "Step 4.5 demo fallback hardcoded MIG spec — low risk if admin UI changes seed"
  ],

  "open_risks_for_next_phase": [
    {
      "risk": "Multi-worker: invalidate_cache only clears one process; other workers serve stale thresholds",
      "trigger": "Gunicorn/uvicorn multi-worker deployment",
      "owner": "Document; backlog Redis"
    },
    {
      "risk": "Large-table migration: single UPDATE locks table; timeout on 100k+ rows",
      "trigger": "alembic upgrade on staging/prod with many sessions",
      "owner": "Pre-flight row count; batched UPDATE for >1000"
    },
    {
      "risk": "Micro-feedback flicker if gating not implemented",
      "trigger": "Score loads after frames on Replay page",
      "owner": "Step 4.2 must gate on both; add verification"
    }
  ],

  "broken_or_incomplete": [
    "Plan has typos: nule→nullable, thold→threshold, threshos→thresholds, hat_diss→heat_diss, AngleArcDiagram angleTargetDees→angleTargetDegrees — fix during implementation",
    "session_factory fixture code not in plan body; use exploration doc",
    "Step 4.6 test_put_threshold_invalidates_cache requires session with frames; fixture must produce scorable session"
  ],

  "next_phase_must_not": [
    "Omit _load_lock in invalidate_cache — race with _load_all",
    "Use parseFloat(x) || 0 or allow angle_target_degrees=0",
    "Compute micro-feedback before primaryScore loads",
    "Run Step 1.2 migration on >1000-row sessions without manual UPDATE and migration_preview.sql"
  ],

  "next_phase_starting_state": "Plan fixes validated via prototypes. Apply edits to plan or implement directly with fixes. Exploration doc at .cursor/explore/plan-critique-fixes-exploration.md. No code changes yet."
}
```
