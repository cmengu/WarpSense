# create-issue-autonomous

You are a senior engineer capturing a development issue. Your job is to produce a complete, structured issue specification that gives the exploration phase everything it needs — with zero follow-up questions required.

---

## Your Output Contract

Produce exactly these sections. No extras, no omissions. Be dense and specific — no filler.

---

### 1. Title
`[Type] Specific description under 80 chars`

Types: `Bug` `Feature` `Improvement` `Refactor` `Performance` `Security` `A11y`

---

### 2. TL;DR
3–5 sentences. Cover: what, why it matters, root cause, desired outcome, effort size. No jargon. Quantify impact if possible (% users affected, time saved, revenue impact).

---

### 3. Root Cause Analysis
Answer "why" 5 levels deep. State the surface problem, then each underlying cause. End with the true root cause in one sentence.

---

### 4. Current State

**What exists today:**
List every relevant file, component, API endpoint, and data model with one-line descriptions. Include file paths. Be exhaustive — if it touches this issue, list it.

**What's broken or missing:**
For each gap: what the user wants to do, what currently happens, and why it fails. Include workarounds users are using now.

---

### 5. Desired Outcome

**User flow after fix:**
Step-by-step: what the user does → what the system does → what the user sees. Cover the primary flow and at least 2 edge flows (error state, empty state).

**Acceptance criteria:**
List 8–12 specific, testable criteria. Format: `User can [action]` or `System does [behaviour]`. Each must be verifiable — no vague criteria like "works correctly."

**Out of scope:**
List at least 3 things explicitly excluded, with a one-line reason each.

---

### 6. Constraints

Cover all that apply:
- Tech stack requirements (must use / must not use)
- Performance targets (response time, bundle size, memory)
- Browser / device / environment support
- Accessibility standard (WCAG level)
- Timeline or deadline
- Blocked by / blocks (other issues)
- Approval required from

---

### 7. Risks
List 5–8 risks. For each: description, probability (Low/Med/High), impact (Low/Med/High), mitigation. Focus on risks that could block execution or cause rework.

---

### 8. Open Questions
List every unresolved decision. For each: the question, your current assumption, confidence in that assumption (Low/Med/High), and who can resolve it. Mark blockers with 🔴.

---

### 9. Classification
- **Type:** bug / feature / improvement / refactor / performance / security / a11y
- **Priority:** P0 (production broken) / P1 (high impact) / P2 (standard) / P3 (nice-to-have)
- **Effort:** XS (<4h) / S (4–8h) / M (8–16h) / L (16–40h) / XL (40–80h)
- **Effort breakdown:** Frontend _h + Backend _h + Testing _h + Review _h = Total _h

---

## Output Rules

- Write every section. If a section has nothing, write "N/A — [brief reason]" rather than omitting it.
- File paths must be real (explore the codebase if needed to confirm them).
- Acceptance criteria must be testable — someone should be able to check each one without asking you.
- Risks must be specific to this issue, not generic boilerplate.
- Do not include planning or implementation details — that is the exploration phase's job.
- Target length: 600–900 words of substantive content. No padding.

---
# Task Description:

10 Mock Welders With Skill Arcs
The Goal
A supervisor opens the dashboard and sees 10 welders. Three are improving. Two are declining. One is a star. One just started. That's a sales demo, not a prototype.
Step 1: Define the Archetypes (backend/data/mock_welders.py)
Create a new file with 10 welder profiles. Each has an arc_type that drives score generation:
pythonWELDER_ARCHETYPES = [
  {"welder_id": "mike-chen",       "name": "Mike Chen",        "arc": "fast_learner",      "sessions": 5, "base": 58, "delta": +4},
  {"welder_id": "sara-okafor",     "name": "Sara Okafor",      "arc": "consistent_expert", "sessions": 5, "base": 88, "delta": +1},
  {"welder_id": "james-park",      "name": "James Park",       "arc": "plateaued",         "sessions": 5, "base": 71, "delta": 0},
  {"welder_id": "lucia-reyes",     "name": "Lucia Reyes",      "arc": "declining",         "sessions": 5, "base": 76, "delta": -4},
  {"welder_id": "tom-bradley",     "name": "Tom Bradley",      "arc": "new_hire",          "sessions": 3, "base": 42, "delta": +6},
  {"welder_id": "ana-silva",       "name": "Ana Silva",        "arc": "volatile",          "sessions": 5, "base": 65, "delta":  0},  # swings ±12
  {"welder_id": "derek-kwon",      "name": "Derek Kwon",       "arc": "fast_learner",      "sessions": 5, "base": 61, "delta": +5},
  {"welder_id": "priya-nair",      "name": "Priya Nair",       "arc": "consistent_expert", "sessions": 5, "base": 91, "delta": 0},
  {"welder_id": "marcus-bell",     "name": "Marcus Bell",      "arc": "declining",         "sessions": 5, "base": 80, "delta": -5},
  {"welder_id": "expert-benchmark","name": "Expert Benchmark", "arc": "consistent_expert", "sessions": 5, "base": 93, "delta": +0.5},
]
Step 2: Score Generator Function (same file)
pythonimport random

def generate_score_arc(base, delta, sessions, arc_type):
    scores = []
    for i in range(sessions):
        if arc_type == "volatile":
            score = base + random.choice([-12, -6, 0, 8, 14])
        else:
            noise = random.uniform(-2, 2)
            score = bae + (delta * i) + noise
        scores.append(round(min(max(score, 0), 100), 1))
    return scores
Step 3: Frame Data Per Arc
Each welder's frames should reflect their arc — don't just fake the score, fake the underlying rule data that produces the score. That way when you click through to a welder report, the heatmap and micro-feedback tell the same story.
For declining arcs: increase angle_degrees variance in later sessions (frames drift from 45° toward 60°+). For fast_learner: angle variance tightens session over session. For volatile: thermal asymmetry spikes randomly.
Create a generate_frames_for_arc(arc_type, session_index) function in mock_sessions.py that adjusts:

angle_degrees standard deviation (tight=2°, loose=8°, drifting=2°→8° over sessions)
thermal N/S delta (symmetric=<10°C, asymmetric=20–40°C)
amps stability (stable=±2A, unstable=±8A)

Step 4: Seed Script Update
Extend your existing POST /api/dev/seed-mock-sessions to loop over all 10 archetypes, generate 3–5 sessions each with correct timestampsnd store welder_id on each session.
Total sessions seeded: ~45. Total new code: ~150 lines of Python, zero new API endpoints.
Step 5: Dashboard Update (WELDER_MAP + WELDERS array)
In the Seagull dashboard, replace the 2-welder WELDERS array with all 10. Add coaching status derived from arc: if last 2 scores declining → show amber "Needs attention" badge. If improving → green "On track". This is 3 lines of logic per card, all derived from history you're already fetching.
The supervisor sees at a glance: 3 green, 5 neutral, 2 amber. That's the enterprise demo.
