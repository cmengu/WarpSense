# Refine Plan (Intelligent Improvement with Deep Analysis)

You are a **principal engineer** conducting a targeted refinement of an implementation plan based on a comprehensive critique. This requires strategic thinking about what to preserve, what to fix, and how to integrate improvements seamlessly.

---

## CRITICAL: Your Mission

You have received:
1. **Current Plan** - The implementation plan being refined
2. **Critique** - A detailed analysis with scores, issues, and recommendations
3. **History** - Previous iterations and what was attempted

Your task: **Output a COMPLETE, IMPROVED plan** that:
- ✅ Fixes ALL critical issues completely
- ✅ Addresses minor issues where they improve quality
- ✅ Preserves ALL strengths and working sections
- ✅ Maintains logical structure and flow
- ✅ Remains executable and actionable

**DO NOT:**
- ❌ Just list changes or provide diffs
- ❌ Explain what you're doing or why
- ❌ Remove good working content
- ❌ Add unnecessary complexity
- ❌ Output anything except the refined plan

---

## Phase 1: Critique Analysis & Strategy Formation

### 1.1 Parse the Critique

First, extract from the critique JSON:
- **Score:** What's the current quality level?
- **Critical Issues:** What MUST be fixed? (execution-blocking)
- **Minor Issues:** What SHOULD be fixed? (quality improvements)
- **Strengths:** What MUST be preserved?
- **Recommended Action:** Was this flagged for deep revision or polish?

### 1.2 Issue Classification & Prioritization

Organize issues by type:

**Type A: Missing Core Steps**
- Example: "No database backup before migration"
- Fix: Add complete new step with verification
- Impact: Could cause data loss
- Priority: P0 - Must fix

**Type B: Insufficient Error Handling**
- Example: "No null check for API response"
- Fix: Add try-catch, null guards, fallback behavior
- Impact: Runtime errors, crashes
- Priority: P0 - Must fix

**Type C: Unclear/Vague Instructions**
- Example: "Update the component" → needs specifics
- Fix: Add file paths, function names, code examples
- Impact: Execution ambiguity
- Priority: P1 - High priority

**Type D: Missing Verification**
- Example: "Test manually" without test cases
- Fix: Add specific test cases, expected outputs, validation commands
- Impact: Can't verify success
- Priority: P1 - High priority

**Type E: Dependency/Sequencing Issues**
- Example: Step 5 depends on Step 8
- Fix: Reorder steps, add explicit prerequisites
- Impact: Execution failure
- Priority: P0 - Must fix

**Type F: Missing Edge Cases**
- Example: "Doesn't handle empty array"
- Fix: Add edge case handling logic
- Impact: Failures on boundary conditions
- Priority: P1-P2 depending on likelihood

**Type G: Polish/Optimization**
- Example: "Could add TypeScript types"
- Fix: Add if improves clarity significantly
- Impact: Code quality, maintainability
- Priority: P2 - Nice to have

---

## Phase 2: Refinement Strategy by Issue Type

### Strategy A: Adding Missing Steps

**When to use:** Critical issue indicates entire step is missing

**Process:**
1. Identify where in sequence the step belongs
2. Draft the complete step with:
   - Clear action description
   - Code example or command
   - Expected outcome
   - Verification method
   - Dependencies
   - Error handling
3. Integrate seamlessly into existing flow
4. Update step numbering if needed
5. Cross-reference with related steps

**Example:**

**Critique says:** "Missing database backup before migration in Step 4"

**Bad refinement:**
```
3.5. Backup database
```

**Good refinement:**
```
## Step 3.5: Create Pre-Migration Database Backup

**Action:**
Create a timestamped backup of the production database before running migrations.

**Implementation:**
```bash
# Create backup directory if it doesn't exist
mkdir -p ./backups

# Generate timestamped backup
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
pg_dump -h localhost -U postgres -d production_db > ./backups/backup_${TIMESTAMP}.sql

# Verify backup created successfully
if [ $? -eq 0 ]; then
    echo "✅ Backup created: backup_${TIMESTAMP}.sql"
    echo "Size: $(du -h ./backups/backup_${TIMESTAMP}.sql | cut -f1)"
else
    echo "❌ Backup failed - ABORT MIGRATION"
    exit 1
fi
```

**Verification:**
- Backup file exists in `./backups/` directory
- File size > 0 bytes
- Can open file and see SQL commands

**Rollback (if migration fails):**
```bash
# Restore from backup
psql -h localhost -U postgres -d production_db < ./backups/backup_${TIMESTAMP}.sql
```

**Time estimate:** 2-5 minutes (depending on database size)

**Dependencies:** Requires `pg_dump` installed, database credentials configured
```

---

### Strategy B: Adding Error Handling

**When to use:** Critical issue about missing error handling or null checks

**Process:**
1. Identify the risky operation
2. Determine failure modes (what could go wrong?)
3. Add defensive checks before operation
4. Wrap in try-catch where applicable
5. Provide fallback behavior
6. Log errors for debugging

**Example:**

**Critique says:** "Step 7 doesn't handle null heatmapData"

**Current (problematic):**
```javascript
function renderHeatmap() {
  heatmapData.forEach(point => {
    // render point
  });
}
```

**Refined (with error handling):**
```javascript
function renderHeatmap() {
  // Guard: Check if data exists
  if (!heatmapData) {
    console.warn('⚠️ Heatmap data not loaded yet');
    return; // Exit gracefully
  }

  // Guard: Check if array is empty
  if (heatmapData.length === 0) {
    console.warn('⚠️ Heatmap data is empty');
    // Show "No data" UI instead of blank screen
    showNoDataMessage();
    return;
  }

  // Process data with error handling
  try {
    heatmapData.forEach(point => {
      // Validate each point
      if (!point || typeof point.temperature !== 'number') {
        console.error('Invalid point data:', point);
        return; // Skip this point
      }
      
      // Render point
      renderPoint(point);
    });
  } catch (error) {
    console.error('Error rendering heatmap:', error);
    // Fallback: show error state
    showErrorMessage('Failed to render heatmap');
  }
}
```

---

### Strategy C: Improving Clarity & Specificity

**When to use:** Minor issue about vague instructions

**Process:**
1. Identify what's ambiguous
2. Add concrete details:
   - File paths
   - Function names
   - Import statements
   - Exact commands
   - Expected outputs
3. Provide code examples
4. Show before/after if modifying existing code

**Example:**

**Critique says:** "Step 2 'Import required libraries' is too vague"

**Current (vague):**
```
## Step 2: Import Libraries

Import the necessary libraries for 3D rendering and heatmap visualization.
```

**Refined (specific):**
```
## Step 2: Import Required Dependencies

**Action:**
Add the following imports to the top of `src/components/ReplayViewer.tsx`:

**Code:**
```typescript
// Three.js core and loaders
import * as THREE from 'three';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

// Custom shaders
import { HeatmapShader } from '@/shaders/HeatmapShader';

// React and state management
import { useEffect, useRef, useState } from 'react';
import { useReplayStore } from '@/stores/replayStore';

// Utils
import { temperatureToColor } from '@/utils/colorMapping';
import { interpolateHeatData } from '@/utils/heatInterpolation';
```

**Verification:**
- No TypeScript errors in file
- Imports resolve (check with `npm run typecheck`)
- Auto-complete works for imported functions

**Dependencies:**
Ensure these packages are installed:
```bash
npm install three@0.160.0
npm install @types/three@0.160.0 --save-dev
```

**Time estimate:** 2-3 minutes
```

---

### Strategy D: Adding Verification Steps

**When to use:** Issue about missing or weak verification

**Process:**
1. For each step, define "done" criteria
2. Provide specific test cases
3. Include expected outputs
4. Add commands to run tests
5. Include negative test cases (what should fail)

**Example:**

**Critique says:** "Step 5 verification just says 'check colors' - not specific"

**Current (weak):**
```
**Verification:**
- Check that colors change with temperature
```

**Refined (specific):**
```
**Verification:**

**Manual Visual Test:**
1. Open browser DevTools (F12)
2. Go to Console tab
3. Run these test scenarios:

```javascript
// Test 1: Cold temperature (0°F) - should be deep blue
testTemperature(0);
// Expected: RGB(0, 0, 139) - dark blue

// Test 2: Warm temperature (250°F) - should be yellow-orange  
testTemperature(250);
// Expected: RGB(255, 165, 0) - orange

// Test 3: Hot temperature (500°F) - should be bright red/white
testTemperature(500);
// Expected: RGB(255, 255, 255) - white-hot

// Test 4: 5°F increments - should see visible color change
for (let temp = 0; temp <= 50; temp += 5) {
  console.log(`${temp}°F:`, temperatureToColor(temp));
}
// Expected: Should see gradual transition from blue → cyan

// Test 5: Negative temperature - should clamp to 0°F color
testTemperature(-50);
// Expected: Same as 0°F (deep blue), no error

// Test 6: Above max temperature - should clamp to 500°F color  
testTemperature(1000);
// Expected: Same as 500°F (white), no error
```

**Automated Test:**
```typescript
// Add to src/__tests__/colorMapping.test.ts
describe('temperatureToColor', () => {
  test('visible change every 5°F', () => {
    for (let temp = 0; temp < 500; temp += 5) {
      const color1 = temperatureToColor(temp);
      const color2 = temperatureToColor(temp + 5);
      
      // Calculate color difference
      const diff = Math.abs(color1.r - color2.r) + 
                   Math.abs(color1.g - color2.g) + 
                   Math.abs(color1.b - color2.b);
      
      // Expect visible difference (threshold: 10 on 0-765 scale)
      expect(diff).toBeGreaterThan(10);
    }
  });

  test('handles edge cases', () => {
    expect(() => temperatureToColor(-100)).not.toThrow();
    expect(() => temperatureToColor(9999)).not.toThrow();
    expect(temperatureToColor(null)).toEqual({ r: 0, g: 0, b: 0 }); // fallback
  });
});
```

**Success Criteria:**
- All manual tests show expected colors
- Automated tests pass (`npm test`)
- Color transitions are smooth and visible
- No console errors
```

---

### Strategy E: Fixing Dependencies & Sequencing

**When to use:** Critical issue about step ordering or missing prerequisites

**Process:**
1. Map dependencies between steps
2. Identify circular dependencies or out-of-order steps
3. Reorder steps logically
4. Add explicit prerequisite sections
5. Update step references throughout plan

**Example:**

**Critique says:** "Step 5 uses heatmapShader but it's not created until Step 8"

**Current (broken sequence):**
```
## Step 5: Apply Shader to Metal
Use heatmapShader on material...

## Step 8: Create Heatmap Shader
Define the shader code...
```

**Refined (correct sequence):**
```
## Step 5: Create Heatmap Shader

**Prerequisites:** None (this step must come first)

**Action:**
Define the custom shader for heatmap rendering.

**Implementation:**
```typescript
// src/shaders/HeatmapShader.ts
export const HeatmapShader = {
  uniforms: {
    temperatureMap: { value: null },
    minTemp: { value: 0.0 },
    maxTemp: { value: 500.0 }
  },
  vertexShader: `
    varying vec2 vUv;
    void main() {
      vUv = uv;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
  `,
  fragmentShader: `
    uniform sampler2D temperatureMap;
    uniform float minTemp;
    uniform float maxTemp;
    varying vec2 vUv;
    
    vec3 temperatureToColor(float temp) {
      // Normalize temp to 0-1 range
      float t = clamp((temp - minTemp) / (maxTemp - minTemp), 0.0, 1.0);
      
      // Color gradient: blue → cyan → green → yellow → red → white
      if (t < 0.2) {
        return mix(vec3(0.0, 0.0, 0.545), vec3(0.0, 1.0, 1.0), t * 5.0);
      } else if (t < 0.4) {
        return mix(vec3(0.0, 1.0, 1.0), vec3(0.0, 1.0, 0.0), (t - 0.2) * 5.0);
      } else if (t < 0.6) {
        return mix(vec3(0.0, 1.0, 0.0), vec3(1.0, 1.0, 0.0), (t - 0.4) * 5.0);
      } else if (t < 0.8) {
        return mix(vec3(1.0, 1.0, 0.0), vec3(1.0, 0.0, 0.0), (t - 0.6) * 5.0);
      } else {
        return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 1.0), (t - 0.8) * 5.0);
      }
    }
    
    void main() {
      float temp = texture2D(temperatureMap, vUv).r;
      vec3 color = temperatureToColor(temp);
      gl_FragColor = vec4(color, 1.0);
    }
  `
};
```

**Verification:**
- File created at `src/shaders/HeatmapShader.ts`
- No TypeScript errors
- Can import in other files: `import { HeatmapShader } from '@/shaders/HeatmapShader'`

**Time estimate:** 15-20 minutes

---

## Step 6: Apply Shader to Metal Material

**Prerequisites:** 
- Step 5 completed (HeatmapShader created)
- Metal model loaded in scene

**Action:**
Replace metal's standard material with shader material using HeatmapShader.

**Implementation:**
```typescript
// In ReplayViewer.tsx
import { HeatmapShader } from '@/shaders/HeatmapShader';

// Find metal mesh
const metalMesh = scene.getObjectByName('metal_surface');

if (metalMesh) {
  // Create shader material
  const shaderMaterial = new THREE.ShaderMaterial({
    uniforms: THREE.UniformsUtils.clone(HeatmapShader.uniforms),
    vertexShader: HeatmapShader.vertexShader,
    fragmentShader: HeatmapShader.fragmentShader
  });
  
  // Apply to mesh
  metalMesh.material = shaderMaterial;
  
  console.log('✅ Heatmap shader applied to metal');
} else {
  console.error('❌ Metal mesh not found in scene');
}
```

**Verification:**
- Metal surface visible in viewport
- No console errors
- Material shows as ShaderMaterial in Three.js DevTools
```

---

### Strategy F: Adding Edge Case Handling

**When to use:** Issue about missing edge cases or boundary conditions

**Process:**
1. Enumerate edge cases for the operation
2. Add handling code for each case
3. Provide examples
4. Add tests for edge cases

**Example:**

**Critique says:** "Doesn't handle case when replay has no temperature data"

**Refined (with edge cases):**
```
## Step 9: Render Heatmap Frame

**Action:**
Render heatmap for current replay frame, handling edge cases.

**Implementation:**
```typescript
function renderHeatmapFrame(frameIndex: number) {
  // Edge Case 1: Invalid frame index
  if (frameIndex < 0 || frameIndex >= totalFrames) {
    console.warn(`⚠️ Frame ${frameIndex} out of range (0-${totalFrames-1})`);
    return;
  }

  // Edge Case 2: No temperature data for this replay
  const temperatureData = replayStore.getTemperatureData();
  if (!temperatureData || temperatureData.length === 0) {
    console.warn('⚠️ No temperature data available');
    // Show default/neutral color
    setUniformTemperature(metalMesh, 70); // Room temperature
    return;
  }

  // Edge Case 3: Frame exists but has no data points
  const frameData = temperatureData[frameIndex];
  if (!frameData || frameData.points.length === 0) {
    console.warn(`⚠️ Frame ${frameIndex} has no data points`);
    // Interpolate from adjacent frames
    const interpolated = interpolateFromAdjacentFrames(frameIndex);
    applyTemperatureData(interpolated);
    return;
  }

  // Edge Case 4: Sparse data (missing measurements at some points)
  if (frameData.points.length < metalMesh.geometry.vertices.length * 0.5) {
    console.warn(`⚠️ Frame ${frameIndex} has sparse data, interpolating...`);
    const filled = fillSparsedData(frameData);
    applyTemperatureData(filled);
    return;
  }

  // Normal case: Full data available
  applyTemperatureData(frameData);
}

// Helper: Interpolate when frame has no data
function interpolateFromAdjacentFrames(frameIndex: number) {
  const prevFrame = findPreviousValidFrame(frameIndex);
  const nextFrame = findNextValidFrame(frameIndex);
  
  if (prevFrame && nextFrame) {
    // Linear interpolation between frames
    const t = (frameIndex - prevFrame.index) / (nextFrame.index - prevFrame.index);
    return interpolateFrames(prevFrame.data, nextFrame.data, t);
  } else if (prevFrame) {
    // Use previous frame
    return prevFrame.data;
  } else if (nextFrame) {
    // Use next frame
    return nextFrame.data;
  } else {
    // No data at all - use defaults
    return getDefaultTemperatureData();
  }
}
```

**Edge Cases Covered:**
1. ✅ Invalid frame index (negative or beyond range)
2. ✅ No temperature data loaded
3. ✅ Frame with zero data points
4. ✅ Sparse/incomplete data points
5. ✅ First frame (no previous frame to interpolate from)
6. ✅ Last frame (no next frame to interpolate from)

**Test Cases:**
```typescript
describe('renderHeatmapFrame edge cases', () => {
  test('handles negative frame index', () => {
    expect(() => renderHeatmapFrame(-1)).not.toThrow();
  });

  test('handles frame beyond range', () => {
    expect(() => renderHeatmapFrame(9999)).not.toThrow();
  });

  test('handles missing temperature data', () => {
    replayStore.temperatureData = null;
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });

  test('handles empty data points', () => {
    replayStore.temperatureData = [{ points: [] }];
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });
});
```
```

---

## Phase 3: Execution - Building the Refined Plan

### 3.1 Structure Preservation

**Preserve the original structure unless critique explicitly says to reorganize.**

**Keep:**
- Main section headings
- Step numbering scheme
- Overall flow and narrative
- Introduction and overview sections

**Modify only:**
- Content that has issues
- Steps that need reordering (if dependency issue)
- Sections flagged in critique

---

### 3.2 Integration Approach

**For each issue in the critique:**

1. **Locate** the affected section in current plan
2. **Apply** the appropriate refinement strategy (A-G above)
3. **Verify** the fix doesn't break other sections
4. **Update** any cross-references
5. **Maintain** consistent formatting and style

---

### 3.3 Quality Checks Before Output

Before outputting the refined plan, verify:

**Completeness:**
- [ ] All critical issues addressed
- [ ] All minor issues addressed (or consciously skipped)
- [ ] All strengths preserved
- [ ] No sections accidentally deleted

**Consistency:**
- [ ] Step numbers sequential
- [ ] Cross-references updated
- [ ] Consistent terminology
- [ ] Uniform formatting

**Executability:**
- [ ] Each step has clear action
- [ ] Code examples are complete and runnable
- [ ] Verification criteria are specific
- [ ] Dependencies are explicit

**Clarity:**
- [ ] No ambiguous instructions
- [ ] File paths and names provided
- [ ] Expected outputs specified
- [ ] Time estimates included

---

## Phase 4: Output Format

### 4.1 What to Output

**Output ONLY the complete refined plan as markdown.**

**DO NOT include:**
- ❌ This meta-instruction document
- ❌ Explanations of what you changed
- ❌ Diffs or change summaries
- ❌ Commentary about the critique
- ❌ Preamble or conclusion about the refinement

**DO include:**
- ✅ Complete refined plan from beginning to end
- ✅ All sections (even unchanged ones)
- ✅ All fixes integrated seamlessly
- ✅ Proper markdown formatting

---

### 4.2 Output Template Structure

Your output should follow this general structure (adapt to specific plan):

```markdown
# [Plan Title]

## Overview
[High-level description of what will be implemented]

## Prerequisites
- [Dependency 1]
- [Dependency 2]
- [etc.]

## Implementation Steps

### Phase 1: [Phase Name]

#### Step 1.1: [Step Name]

**Action:**
[What to do]

**Implementation:**
```[language]
[Complete code]
```

**Verification:**
- [Specific test 1]
- [Specific test 2]

**Expected Outcome:**
[What success looks like]

**Time Estimate:** [X minutes/hours]

**Dependencies:** [What must be done first]

---

#### Step 1.2: [Step Name]

[... repeat pattern ...]

---

### Phase 2: [Phase Name]

[... continue ...]

---

## Testing & Validation

[Comprehensive testing section]

---

## Rollback Procedure

[How to undo if things go wrong]

---

## Known Issues & Limitations

[Any caveats or edge cases still not handled]

---

## Success Criteria

[How to know the entire implementation is complete]
```

---

## Phase 5: Special Cases & Advanced Scenarios

### Scenario A: Score 8-9 (Minor Polish)

**What it means:** Plan is mostly good, just needs small improvements

**Approach:**
- Address minor issues without disrupting working sections
- Focus on clarity and specificity
- Add verification where weak
- Don't over-refine

**Example fixes:**
- Add missing import statement
- Clarify vague file path
- Add specific test case
- Fix typo in command

---

### Scenario B: Score 5-7 (Significant Revision)

**What it means:** Plan has critical flaws, needs substantial work

**Approach:**
- Fix all critical issues first (execution-blocking)
- Reorder steps if sequencing is broken
- Add missing core steps
- Beef up error handling significantly
- May need to restructure sections

**Example fixes:**
- Add entire missing phase
- Reorder 5+ steps for correct dependencies
- Add comprehensive error handling throughout
- Replace vague steps with specific implementations

---

### Scenario C: Score <5 (Major Overhaul)

**What it means:** Plan is fundamentally flawed

**Approach:**
- Consider if plan addresses the original task at all
- May need to restructure entire plan
- Add multiple missing phases
- Complete rewrite of vague sections
- Extensive verification added throughout

**Example fixes:**
- Add 3 new major phases
- Complete rewrite of implementation approach
- Add all error handling, verification, testing
- Fix fundamental technical errors

---

### Scenario D: Conflicting Critique Points

**What to do when critique has contradictory suggestions:**

**Example:** 
- Issue #1: "Too vague, add more detail"
- Issue #5: "Too verbose, simplify Step 3"

**Resolution:**
1. Look at which is critical vs minor
2. Critical issues take precedence
3. For same priority: Balance both concerns
   - Add detail where execution requires it
   - Simplify prose without losing critical information

---

### Scenario E: Preserving Existing Good Code

**When critique says:** "Add error handling to Step 7" but Step 7 already has working code

**Approach:**
```typescript
// EXISTING (keep this):
function processData(data) {
  const result = transform(data);
  return result;
}

// ADD (integrate with existing):
function processData(data) {
  // NEW: Validation
  if (!data) {
    console.error('Data is null or undefined');
    return null;
  }

  // NEW: Error handling
  try {
    // EXISTING: Keep original logic
    const result = transform(data);
    return result;
  } catch (error) {
    // NEW: Error recovery
    console.error('Transform failed:', error);
    return getDefaultData();
  }
}
```

**Don't replace working code entirely - wrap it with improvements.**

---

## Phase 6: Final Checklist

Before submitting the refined plan, verify:

### Critical Requirements
- [ ] **All critical issues fixed** - None remain unaddressed
- [ ] **Plan is executable** - Someone could follow it step-by-step
- [ ] **Strengths preserved** - Good sections untouched
- [ ] **Complete output** - No placeholders like "TODO" or "[add details]"

### Quality Requirements
- [ ] **Specific, not vague** - File paths, function names, exact commands
- [ ] **Verifiable** - Clear success criteria for each step
- [ ] **Error handling** - Defensive code where needed
- [ ] **Edge cases** - Boundary conditions handled

### Formatting Requirements
- [ ] **Valid markdown** - Proper headers, code blocks, lists
- [ ] **Consistent style** - Uniform formatting throughout
- [ ] **Code blocks tagged** - Language specified (```typescript, ```bash, etc.)
- [ ] **No meta-content** - Only the plan itself, no explanations

---

## Remember

**You are outputting THE REFINED PLAN, not a report about the plan.**

**The refined plan should be:**
- ✅ Complete (beginning to end)
- ✅ Improved (all issues fixed)
- ✅ Executable (ready to implement)
- ✅ Clear (no ambiguity)

**The refined plan should NOT be:**
- ❌ A diff or changelog
- ❌ A summary of changes
- ❌ Commentary about the critique
- ❌ Partial or incomplete

---

**Now, examine the critique, apply the appropriate strategies, and output the complete refined plan.**

---
# Current Plan:


Summary of the implementation plan:

---

## Plan Summary

Implementation plan is in **`.cursor/taskspace/20260218_130707/plan-10-mock-welders.md`**.

### Phase Breakdown (5 phases, 17 steps, ~16h)

| Phase | Goal | Est. |
|-------|------|------|
| **Phase 1** — Backend mock data layer | 10 archetypes, `generate_score_arc`, `generate_frames_for_arc`, `generate_session_for_welder` | 5h |
| **Phase 2** — Seed & wipe | ~45 sessions seeded, wipe removes all, seed_demo_data aligned | 3h |
| **Phase 3** — Dashboard | 10 welder cards, coaching badges (On track / Needs attention) | 3h |
| **Phase 4** — Welder report | Historical scores from fetch, `sess_expert-benchmark_005` as expert baseline | 2h |
| **Phase 5** — Tests | `test_mock_welders.py`, updates to `test_dev_routes.py` | 3h |

### Main implementation details

1. **`mock_welders.py`** — `WELDER_ARCHETYPES` (10 entries) and `generate_score_arc` for validation only (correct `base` vs spec typo).
2. **`mock_sessions.py`** — `generate_frames_for_arc(arc_type, session_index)` and `generate_session_for_welder(welder_id, arc_type, session_index, session_id)`; volatile/declining use `disable_sensor_continuity_checks=True`.
3. **Seed/wipe** — IDs derived from archetypes (single source of truth), `random.seed(42)` before generation.
4. **Dashboard** — `WELDERS` with `sessionCount`; `latestSessionId`/`secondLatestSessionId`; badge from last vs second-last score.
5. **Welder report** — `MOCK_HISTORICAL` replaced with fetched scores; expert comparison uses `sess_expert-benchmark_005`.

### Risk mitigations

- Frame tuning: accept ±5–10 variance; tune angle/amps/volts.
- Volatile reproducibility: `random.seed(42)` in seed route.
- Continuity failures: `disable_sensor_continuity_checks=True` for volatile and declining.
- Wipe coverage: IDs derived from `WELDER_ARCHETYPES`.

The plan includes per-step verification, CRITICAL steps with full code, and a pre-flight checklist and risk heatmap.


---
# Standard Critique (issues to fix):


{"overall_strategy_score": 8.2, "top_3_avg_priority": 8.30, "context_type": "demo", "goal_alignment_score": 8.5, "task_scores": [{"task_number": 1, "task_title": "Phase 1 — Backend mock data layer (mock_welders, generate_frames_for_arc, generate_session_for_welder)", "strategic_value": 9.0, "strategic_value_after_penalty": 9.0, "context_alignment": 9.0, "leverage_optionality": 8.0, "resource_efficiency": 6.0, "risk_balance": 6.0, "dependencies": 10.0, "execution_quality": 9.0, "overall_score": 8.38, "feedback_penalty_applied": false, "ships_wrong_risk": "If frame params (angle/amps/thermal) produce scores outside ±15 of target, extract_features→score_session yields wrong bands and badges show incorrect On track/Needs attention.", "higher_leverage_alternative": "Prototype validation (Step 1.4) with explicit pass bands—run earlier to fail fast before Phase 2.", "reasoning": "Core dependency for all other phases; unblocks demo data and scoring."}, {"task_number": 2, "task_title": "Phase 2 — Seed & wipe (~45 sessions, derive IDs from WELDER_ARCHETYPES, seed_demo_data alignment)", "strategic_value": 8.0, "strategic_value_after_penalty": 8.0, "context_alignment": 9.0, "leverage_optionality": 7.0, "resource_efficiency": 7.0, "risk_balance": 8.0, "dependencies": 5.0, "execution_quality": 9.0, "overall_score": 7.88, "feedback_penalty_applied": false, "ships_wrong_risk": "If session ID derivation diverges between seed and wipe, wipe misses sessions and orphan mock data breaks demo reset.", "higher_leverage_alternative": null, "reasoning": "Single source of truth for IDs is correct; enables dashboard and report."}, {"task_number": 3, "task_title": "Phase 3 — Dashboard (10 welder cards, latest/second score fetch, On track/Needs attention badges)", "strategic_value": 9.0, "strategic_value_after_penalty": 9.0, "context_alignment": 10.0, "leverage_optionality": 7.0, "resource_efficiency": 7.0, "risk_balance": 9.0, "dependencies": 5.0, "execution_quality": 8.0, "overall_score": 8.51, "feedback_penalty_applied": false, "ships_wrong_risk": "If 20 fetchScore calls block or timeout, dashboard hangs; if byWelder grouping is wrong, badges appear on wrong welders.", "higher_leverage_alternative": "Add batch GET /api/scores?ids=... to reduce round-trips; plan marks this OOS so current approach is acceptable.", "reasoning": "Primary demo surface; 10 cards and badges are the main outcome for supervisor."}, {"task_number": 4, "task_title": "Phase 4 — Welder report (fetched historical scores, sess_expert-benchmark_005 baseline, trend chart)", "strategic_value": 8.0, "strategic_value_after_penalty": 8.0, "context_alignment": 9.0, "leverage_optionality": 7.0, "resource_efficiency": 8.0, "risk_balance": 9.0, "dependencies": 5.0, "execution_quality": 8.0, "overall_score": 8.01, "feedback_penalty_applied": false, "ships_wrong_risk": "If expert-benchmark not seeded before demo, fetchSession 404 breaks comparison; if Promise.all order mismatches historicalSessionIds, chart and AI feedback get wrong scores.", "higher_leverage_alternative": null, "reasoning": "Drill-down with trend chart and expert comparison completes the demo story."}, {"task_number": 5, "task_title": "Phase 5 — Tests (test_mock_welders.py, test_dev_routes updates for ~45 sessions)", "strategic_value": 6.0, "strategic_value_after_penalty": 6.0, "context_alignment": 7.0, "leverage_optionality": 6.0, "resource_efficiency": 5.0, "risk_balance": 10.0, "dependencies": 3.0, "execution_quality": 9.0, "overall_score": 6.22, "feedback_penalty_applied": false, "ships_wrong_risk": "If score range assertions are too tight (±15), test_arc_scores_in_reasonable_range flakes when extract_features/thresholds change.", "higher_leverage_alternative": "Run tests after Phase 2 only; defer volatile repro test until seed route is stable.", "reasoning": "Essential for correctness per project rules but not directly visible to stakeholder."}], "critical_issues": ["Step 1.2: Frame params in generate_frames_for_arc may not produce scores in expected bands; prototype (Step 1.4) has soft pass criteria and no automated gate.", "Step 3.2: 20 sequential fetchScore calls on dashboard; no timeout or retry handling; slow network could block or hang the page.", "WELDER_ARCHETYPES and sessionCount duplicated in seagull/page.tsx and welder/[id]/page.tsx — drift risk if archetypes change."], "minor_issues": ["random.seed(42) in seed route but prototype_arc_scoring.py may not seed; volatile validation could differ between prototype and seed runs.", "test_dev_routes expects >=40 sessions; exact count (45) varies with archetype sessions sum—brittle if archetypes change."], "suggestions": ["Add explicit prototype pass bands (e.g. fast_learner s0 in 43–73, s4 in 70–98) and fail Step 1.4 if out of band.", "Document batch scores endpoint as follow-up; add 5s timeout to fetchScore or use AbortController to avoid indefinite hang.", "Consider GET /api/dev/welders returning archetypes + sessionCount so frontend uses single source of truth."], "dependency_order": "Phase 1 before Phase 2 (generate_session_for_welder required). Phase 2 before Phase 3 and 4 (seed data required). Phase 3 and 4 can run in parallel. Phase 5 after Phases 1–4.", "premortem_top_risk": "Frame generation params produce scores outside expected bands, so declining welders show On track or fast learners show Needs attention, and the demo story fails.", "goal_alignment_notes": "All phases serve the enterprise demo goal: 10 welders with skill arcs, badges, and drill-down. Phases 1–4 deliver visible outcomes within 48h. Phase 5 supports data integrity per project rules. Plan aligns with acceptance criteria in the issue.", "context_aware_feedback": {"current_context_type": "demo", "penalties_applied": [], "penalties_skipped": []}}

---
# Adversarial Findings (also must fix):


```json
{
  "score": 5.2,
  "adversarial_verdict": "This plan will pass verification in a clean dev environment but contains a critical prototype/implementation split that invalidates Phase 1.4 validation, plus multiple silent-failure vectors at the frontend/backend boundary and in partial execution paths.",
  "sabotage_vectors": [
    {
      "step": "1.4",
      "attack": "Step 1.4 runs prototype_arc_scoring.py to validate score ranges. The prototype uses its OWN make_session_for_arc - it does NOT import generate_frames_for_arc or generate_session_for_welder from mock_sessions. Implement mock_sessions.generate_frames_for_arc with subtly different angle/amps params. Prototype passes. Seed (Phase 2) uses generate_session_for_welder which calls mock_sessions. Real scores diverge from prototype bands. Badges and reports show wrong data.",
      "detection_gap": "Verification runs the prototype script, not the mock_sessions implementation. No step asserts that mock_sessions.generate_frames_for_arc produces scores within +/-15.",
      "blast_radius": "Phase 2 seed produces sessions with wrong score bands; Phase 3 badges incorrect for declining/fast_learner; Phase 4 welder report trend chart misleading; success criteria 3-4 fail"
    },
    {
      "step": "2.4",
      "attack": "seed_demo_data uses existing > 0 to skip. Seed 44 sessions; process crashes on session 45. Re-run: 44 exist, skip. Database has incomplete demo. Dashboard shows broken links; welder report 404s.",
      "detection_gap": "Verification expects Demo data seeded or skip. Second run prints skip, which is a pass. No assertion that count equals sum(arch['sessions']).",
      "blast_radius": "Deploy uses seed_demo_data; demo has partial data; success criteria 1-7 fail silently"
    },
    {
      "step": "2.1",
      "attack": "Wipe deletes only IDs derived from current WELDER_ARCHETYPES. Add welder then remove from archetypes and re-seed. Orphan sess_old-demo_001 sessions remain. Wipe never removes them.",
      "detection_gap": "Wipe verification checks deleted matches seeded count. Orphan sessions from prior schema are not in derived list. No step verifies zero sess_* sessions remain for all historical IDs.",
      "blast_radius": "Schema evolution leaves orphan sessions"
    },
    {
      "step": "3.1",
      "attack": "WELDERS and sessionCount hardcoded in page.tsx. Add 11th archetype to backend. Seed creates 49 sessions. Dashboard still shows 10 cards; 11th welder invisible.",
      "detection_gap": "Verification counts exactly 10 cards. Adding an 11th to backend would make 11 seeded welders; frontend would still show 10.",
      "blast_radius": "Backend/frontend schema drift; new welders never appear"
    },
    {
      "step": "4.4",
      "attack": "Step 4.4 uses filter(fulfilled).map(r => r.value.total) for historicalScores. If sess_mike-chen_003 fetch fails, filtered historicalScores loses index. generateAIFeedback receives wrong order. AI feedback trend is wrong.",
      "detection_gap": "Verification says chart renders with partial data. Chart uses histResults[i] so position is correct. But historicalScores passed to generateAIFeedback is the filtered array - order is wrong when some fetches fail.",
      "blast_radius": "AI feedback trend wrong when any historical fetch fails"
    },
    {
      "step": "4.3",
      "attack": "Promise.all - any single rejection aborts. Expert session 404 causes full page error. User sees Session not found with no path to recover.",
      "detection_gap": "Verification assumes seed ran. No step verifies graceful degradation when expert fetch fails.",
      "blast_radius": "Welder report page unusable if expert-benchmark not seeded"
    },
    {
      "step": "1.2",
      "attack": "Prototype sets disable_sensor_continuity_checks only for volatile. Plan says declining needs it. If declining frames exceed 20% amps jump, Session validation fails during seed.",
      "detection_gap": "Prototype does not build declining sessions. Declining validation path untested.",
      "blast_radius": "Seed fails on declining archetypes; Phase 2 blocks"
    }
  ],
  "precondition_gaps": [
    "Step 1.4 assumes prototype will be updated to import from mock_sessions. Plan says modify if needed but provides no code change. Prototype currently uses its own make_session_for_arc.",
    "Step 2.1 assumes database connection and migrations applied. No step asserts DB is reachable before seed.",
    "Step 3.1 depends on Backend seeded but Phase 3 has no step that runs seed. Phase boundary handoff is implicit.",
    "Step 4.2 assumes sess_expert-benchmark_005 exists. No Phase 4 step seeds data.",
    "Step 5.2 test_seed_creates expects SessionModel.frame_count > 0. No step verifies frame persistence."
  ],
  "untestable_verifications": [
    "Step 3.3 Visual check for badges - no DOM selector, no assertion. getBadge with swapped args would show wrong badges; visual pass.",
    "Step 1.4 All arc types produce scores in expected bands - bands are +/-15, manually observed. No automated assertion.",
    "Step 4.1 All 10 welder IDs resolve to correct latest session - no automated check. Manual click-through."
  ],
  "cascade_points": [
    "Step 1.2 generate_frames_for_arc is single point of failure for 1.3, 1.4, 2.1, 2.4, 5.1. Wrong frame params propagate to every seeded session.",
    "WELDER_ARCHETYPES typo in one place - seed creates sess_mike-chen_001, frontend looks for sess_mike_chen_001. 404. Cascade: dashboard Score unavailable."
  ],
  "phase_boundary_gaps": [
    "Phase 1 to Phase 2: Phase 1.4 runs prototype, not mock_sessions. Phase 2 uses mock_sessions. Handoff is invalid.",
    "Phase 2 to Phase 3: No step ensures seed ran before opening /seagull. Executor may open dashboard with empty DB.",
    "Phase 2 to Phase 4: seed_demo_data and seed route are separate. If seed_demo_data has idempotent bug, Phase 4 fails for some welders.",
    "Phase 3 to Phase 4: Dashboard WELDERS and welder report WELDER_SESSION_COUNT are duplicated. Changing one without the other causes wrong session link."
  ],
  "pre_mortem": {
    "partial_execution": "We got through Phase 1 and Phase 2 but stopped at Step 2.4. seed_demo_data first run crashed on session 40. Second run hit existing > 0 and skipped. Database has 39 sessions. Phase 3 dashboard loads; some welders show Score unavailable. Rollback requires manual DELETE; no documented down path.",
    "silent_corruption": "Everything appeared to work. Users reported wrong coaching badges. Root cause: Step 1.4 validated prototype; seed used mock_sessions with different angle drift. Frame params produced different scores. Prototype passed; production data wrong.",
    "prod_vs_local": "Staging revealed dashboard load timeouts. 10 welders = 20 fetchScore calls. Under latency, 3 timeout. Cards show Score unavailable. Local dev has low latency; all 20 complete.",
    "dependency_drift": "When SQLAlchemy 2.x updated, delete with IN clause for 45 session_ids caused too many SQL variables in SQLite. Wipe started failing. Tests use in-memory SQLite."
  },
  "critical_issues": [
    "Step 1.4 validates the wrong implementation. Prototype must be updated to call mock_sessions.generate_session_for_welder and assert score bands.",
    "Step 2.4 idempotent check existing > 0 is wrong. Should skip only when existing == len(session_ids).",
    "Step 4.4 historicalScores order is corrupted when filtering fulfilled results. Must preserve index for failed fetches."
  ],
  "latent_issues": [
    "Step 2.1 and 2.2: Wipe does not handle schema evolution. Orphan sess_* from removed archetypes persist.",
    "Step 3.2: 20 fetchScore calls with no batching. Under load, timeouts cause Score unavailable.",
    "Step 5.2: test_wipe uses like(sess_*). If another test left sess_* rows, remaining > 0."
  ],
  "blind_spots": [
    "Plan assumes executor will run seed before viewing dashboard or welder report. No step in Phase 3 or 4 runs seed.",
    "Plan assumes WELDER_ARCHETYPES will never change. Frontend hardcodes 10 welders. No single source of truth across stack.",
    "Plan assumes generate_session_for_welder never calls generate_score_arc. Unvalidated."
  ],
  "minor_issues": [
    "Step 1.3 What says generate_session_for_welder without arc_type but signature includes it.",
    "Step 3.3 verification At least 2 green badges - volatile may randomly show either. Test could be flaky.",
    "Pre-flight grep for generate_frames does not verify params."
  ],
  "what_the_author_cant_see": "The plan mentally merged prototype and mock_sessions. Step 1.4 runs the prototype. The plan never migrates that validation to mock_sessions. The author believes Phase 1 validates the production path. It does not.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Add assertion that mock_sessions.generate_session_for_welder produces scores in band for fast_learner session 0",
    "Change Step 2.4 idempotent check from existing > 0 to existing == len(session_ids)",
    "Fix Step 4.4: Build historicalScores preserving order when some fetches fail (map over indices, use 0 for rejected)",
    "Document in Phase 3 and 4 prerequisites: Run seed before opening dashboard or welder report"
  ]
}
```
