
```json
{
  "score": 7.4,
  "dimension_scores": {
    "completeness": 7.5,
    "clarity": 7.5,
    "verification": 7.5,
    "dependencies": 8.5,
    "risk_management": 7.5,
    "edge_cases": 6.5,
    "production_ready": 7.5
  },
  "critical_issues": [
    "Phase 3 omits seagull routes. my-app/src/app/seagull/welder/[id]/page.tsx uses red-* for error state (lines 132-136) and my-app/src/app/seagull/page.tsx uses text-amber-600 (line 99). These will persist as rainbow colors after rebrand. Add Step 3.x: Update seagull welder error (red→violet), seagull dashboard amber→violet.",
    "Step 1.1 theme.ts THERMAL_ANCHOR_COLORS_0_1 comments say positions 0, 0.125, 0.25, 0.375, 0.5, 0.625, 0.75, 1.0 but heatmapShaderUtils and GLSL use [0, 0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 1.0]. Implementers may misalign. Add explicit doc: 'Array index i maps to shader anchorPos[i]; positions are [0, 0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 1.0]'."
  ],
  "minor_issues": [
    "Step 1.2: Code snippet uses '// ... rest same' — specify full tempToColor loop and return logic for clarity.",
    "Step 1.4: No complete GLSL anchorCol block — provide full vec3 anchorCol[8] replacement to avoid copy errors.",
    "Step 1.7: heatmapData.test tempToColorRange expects atMin #3b82f6, atMax #ef4444 — plan says update but should specify exact new expected hex (tempToColor(0) and tempToColor(500)) for 400–550 range.",
    "Step 2.8: 'Use actual hex from tempToColor' — add explicit expected hex for 0°C (#1e3a8a), 250°C (indigo hex), 500°C (#a855f7) to reduce trial-and-error.",
    "Range change [20,600]→[0,500]: Plan does not document impact if real sensor data exceeds 500°C. Add note: welding temps >500°C will clamp to max purple; verify against typical sensor range.",
    "Step 4.5 grep: Exclude .cursor, node_modules, *.test.ts during migration (tests assert old behavior until updated); plan says exclude tests — clarify if tests are updated in Phase 1/2/3 before Phase 4 grep.",
    "Phase 4.6 Visual QA: Add seagull /seagull and /seagull/welder/[id] to checklist if seagull is in scope."
  ],
  "strengths": [
    "Excellent dependency graph and critical path; theme.ts as single source of truth is correctly identified.",
    "Step 1.5 thermal sync test with sample temps and ±1 channel tolerance prevents heatmapData vs heatmapShaderUtils drift.",
    "Risk table with probability/impact and mitigation for thermal drift, missed colors, test brittleness.",
    "Full theme.ts implementation with 8 anchors, CHART_PALETTE, semantic hex — copy-paste ready.",
    "Step 1.6 deltaHeatmapData code block is complete with correct purple math.",
    "Step 2.6 getWeldPoolColor full implementation; Phase 3 Tailwind mappings are explicit.",
    "Pre-flight checklist, success criteria (15 items), and visual QA checklist.",
    "Test update mapping is explicit: heatmapData, heatmapShaderUtils, deltaHeatmapData, HeatMap, demo."
  ],
  "reasoning": "The plan demonstrates strong understanding of the thermal pipeline (heatmapData, heatmapShaderUtils, GLSL shader) and correctly sequences theme.ts as the foundation. The core approach is sound. Two critical gaps would cause execution failure or incomplete rebrand: (1) Seagull routes use red/amber and are not in Phase 3 — grep in Phase 4 would find them, but the plan does not include fixing them, so the rebrand would be incomplete. (2) THERMAL_ANCHOR_COLORS_0_1 position comments conflict with actual shader positions [0, 0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 1.0]; a copy-paste implementer might misalign the 8 values. Minor issues are clarity refinements (exact hex for test assertions, full GLSL block, range-impact note). The weighted score 7.4 reflects a solid plan with 2 critical fixes needed; with those, it would reach 8.5+. Recommended action: refine.",
  "improvement_priority": [
    "CRITICAL: Add seagull routes to Phase 3 — welder error (red→violet), seagull dashboard amber→violet.",
    "CRITICAL: Fix theme.ts THERMAL_ANCHOR_COLORS_0_1 documentation — state array index maps to shader positions [0, 0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 1.0].",
    "Minor: Provide full GLSL anchorCol[8] replacement in Step 1.4.",
    "Minor: Specify exact expected hex for heatmapData.test and HeatMap.test assertions.",
    "Minor: Document [0,500] clamp impact on sensor data >500°C."
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 7.4 indicates core approach is correct but has 2 critical gaps. Seagull omission would leave rainbow colors in production. Theme position comment mismatch could cause thermal sync test failures or visual inconsistency between 2D and 3D heatmaps. Use refine-plan.md to add seagull to Phase 3 and fix theme documentation without replanning."
}
```
