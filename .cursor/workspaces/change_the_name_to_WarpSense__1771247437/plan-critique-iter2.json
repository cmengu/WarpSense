
```json
{
  "score": 8.1,
  "dimension_scores": {
    "completeness": 8.5,
    "clarity": 8.5,
    "verification": 8.0,
    "dependencies": 8.5,
    "risk_management": 8.0,
    "edge_cases": 7.5,
    "production_ready": 8.0
  },
  "critical_issues": [],
  "minor_issues": [
    "Step 1.7 heatmapShaderUtils.test: Plan updates 'returns bright red at 500°C' but omits 'returns cool blue at 0°C' (lines 37-41). Current test expects r≈0.05, g≈0.05, b>0.3; new THERMAL_ANCHOR_COLORS_0_1[0] is (0.12, 0.23, 0.54). Test will fail. Add: expect(r).toBeCloseTo(0.12, 2); expect(g).toBeCloseTo(0.23, 2); expect(b).toBeGreaterThan(0.5);",
    "Step 1.7 heatmapData.test: Plan lists main tempToColor assertions but omits 'expert ~490°C yields yellow-ish' and 'novice spike ~520°C yields red-ish' (lines 296-318). These assert rgb semantics (g>50, r>b for yellow; r>150, b<100 for red). After rebrand, 490°C and 520°C map to purple (b>r). Either update to purple-ish assertions (e.g. b>r, r>100) or remove if testing obsolete semantics.",
    "Step 1.7 heatmapData.test: 'clamps temps below 20 to 20' test (lines 289-291) must change to 'clamps temps below 0 to 0' — new range is [0,500], so tempToColor(-50) should equal tempToColor(0) = #1e3a8a. Plan implies this but does not explicitly call out renaming the test.",
    "Step 4.5 grep: Command uses --type-add and -t '!exclude' which may not work on all ripgrep versions. Simpler: rg 'green|red|cyan|amber|yellow|orange|pink|emerald|teal|lime' my-app/src --glob '!*.test.*' --glob '!*.spec.*'. Excluding .cursor and node_modules is implicit when grepping my-app/src.",
    "Step 2.3 mockData: 'Replace #10b981, any green/amber' is vague. Actual file has color: '#10b981' at line 79. Add: Change to '#2563eb' or CHART_PALETTE[0]. Verify no other green/amber hex in file.",
    "Step 1.5 thermal sync: 'convert rgb to hex' — heatmapShaderUtils returns [r,g,b] in 0-1; multiply by 255, round, then format. Add explicit formula: const toHex = (c:number) => Math.round(c*255).toString(16).padStart(2,'0'); hex = '#'+toHex(r)+toHex(g)+toHex(b);"
  ],
  "strengths": [
    "Phase 1 thermal pipeline is well-structured: theme.ts as single source of truth, sync test prevents drift, dependency DAG is correct",
    "Step 1.2, 1.4, 1.7 provide complete code blocks — no '// ... rest same' gaps; tempToColor loop and GLSL anchorCol block are copy-paste ready",
    "Step 3.5 seagull: Exact file paths, line numbers (~132-136, line 99), and replacement code blocks — verified against actual codebase",
    "THERMAL_ANCHOR_COLORS_0_1 documentation: Index-to-shader-position mapping [0, 0.1, 0.2, 0.3, 0.5, 0.7, 0.9, 1.0] prevents anchor/shader drift",
    "Risk heatmap and failure modes explicitly include seagull routes; Step 4.6 Visual QA checklist includes /seagull and /seagull/welder/[id]",
    "Range [0,500]°C impact documented in synthesis, Known Issues, and risk #6 — sensor data >500°C clamp is acknowledged",
    "Pre-Flight checklist, Success Criteria (16 items), and Progress Dashboard provide execution scaffolding",
    "tempToColorRange span<=0 guard already exists in heatmapData.ts; plan's remap change preserves this edge-case handling"
  ],
  "reasoning": "The refined plan addresses the previous critique well: seagull routes are fully specified with paths and code, theme.ts position docs are clear, and the dependency graph includes seagull. The plan is execution-ready for a competent engineer. No critical execution-blocking issues remain. The 6 minor issues are targeted refinements: (1) heatmapShaderUtils 0°C test will fail with new anchors — the plan updates the 500°C test but not the 0°C one. (2) heatmapData expert/novice semantics tests assert yellow/red color properties that won't hold after rebrand; plan's catch-all 'fix assertions' may miss these. (3) Clamp test rename is implicit. (4) grep command could be simplified for portability. (5) mockData lacks specific line/key. (6) thermal sync rgb-to-hex conversion could be explicit. These are polish items; an engineer could infer and fix them during execution. Weighted score: (8.5×0.20)+(8.5×0.15)+(8.0×0.20)+(8.5×0.15)+(8.0×0.15)+(7.5×0.10)+(8.0×0.05) = 8.1. Score 8.0-8.9 indicates minor polish needed.",
  "improvement_priority": [
    "Minor: Add heatmapShaderUtils 0°C test update to Step 1.7 (r≈0.12, g≈0.23, b>0.5)",
    "Minor: Add heatmapData expert/novice tests handling to Step 1.7 — update to purple-ish or remove",
    "Minor: In Step 1.7, explicitly rename 'clamps temps below 20' → 'clamps temps below 0'",
    "Minor: Simplify Step 4.5 grep command for portability",
    "Minor: Step 2.3: Specify mockData.ts line 79, color key, and replacement hex",
    "Minor: Step 1.5: Add explicit rgb-to-hex conversion for sync test"
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 8.1 falls in 8.0-8.9 range. Zero critical issues; 6 minor clarity/completeness gaps. Core approach is solid, seagull and theme docs are addressed, dependency order is correct. Use refine-plan.md to add the heatmapShaderUtils 0°C test update, heatmapData expert/novice test handling, and the other minor specifics. With these refinements, the plan would reach 8.8-9.0 and be fully ready for execution."
}
```
