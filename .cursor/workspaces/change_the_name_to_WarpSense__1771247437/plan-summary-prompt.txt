# Compress Plan to Execution Summary

Extract ONLY the actionable steps - no explanations, rationale, or background.

## Format:
```
## Steps
1. File: path/to/file.tsx
   - Action: What to do
   - Key code: One-line reference

2. File: path/to/other.ts
   - Action: What to do
   - Key code: One-line reference

## Critical Details
- Constraint 1
- Constraint 2
```

Keep under 50 lines. Maximum brevity.

---

# Full Plan to Compress:


All five fixes from the critique are applied. Summary of edits:

1. **Step 1.1** — Added note: "If `constants/index.ts` barrel exists, add theme export there."

2. **Step 1.2** — Added line reference for `tempToColorRange`: "In `heatmapData.ts` near line 118, change remap from `const t = 20 + p * (600 - 20)` to `const t = p * 500`..."

3. **Step 1.6** — Added NaN note plus optional handling: guard with `Number.isNaN(d)` or optional test `expect(deltaTempToColor(NaN)).toBeDefined()`.

4. **Step 2.3** — Added a copy-pasteable `rg` command:
```bash
rg '#10b981|#f59e0b' my-app/src/data/mockData.ts
```

5. **Step 2.6** — Added an optional `rg` helper to find Tailwind classes:
```bash
rg 'cyan|green|amber' my-app/src/components/welding/TorchWithHeatmap3D.tsx
```

The refined plan is updated with these changes; it should be closer to a 9.0 score and ready for execution.
