# Fix Implementation Issues - Round 1

Apply ALL fixes identified in the review.

## Review Findings:

Here’s a concise summary of the Round 1 code review:

## Code review summary

The full report is in `.cursor/review-round1-output.md`. Summary:

### Findings

| Severity | Count | Highlights |
|----------|-------|------------|
| **CRITICAL** | 1 | WebGL resources (DataTexture, ShaderMaterial) created in `useMemo` in `ThermalPlate.tsx` — side effects in a pure memo, risks leaks and Strict Mode issues |
| **HIGH** | 6 | DataTexture update robustness, hardcoded comparison session ID, font loading, global test pollution, flat metal `planeGeometry` hardcoded to 3 |
| **MEDIUM** | 8 | Duplicate uniform updates, duplicate constants across files, repeated magic numbers, duplicate prop types |
| **LOW** | 6 | Hardcoded legend text, JSDoc clarity, comment correctness, color format usage |

### Critical fix

In `ThermalPlate.tsx`, move texture and material creation from `useMemo` into `useEffect`. `useMemo` is for pure computations; allocating and updating GPU-backed resources there breaks React’s expectations and can cause leaks in Strict Mode.

### What’s working well

- Clear separation of thermal extraction, interpolation, and rendering
- Context-loss handling with appropriate ARIA attributes
- Pure `temperatureToColor` with good edge-case handling
- Consistent use of `filterThermalFrames` and `extractCenterTemperatureWithCarryForward` in replay/demo
- Updated WebGL docs and ESLint rules

### Next steps

1. Fix the **CRITICAL** ThermalPlate `useMemo` usage (move resource creation to `useEffect`).
2. Address all **HIGH** issues.
3. Tackle **MEDIUM** items where practical.
4. Re-run the testing checklist, then request Round 2 review.

---
# Current Implementation:


# Implementation Complete

Unified torch + 3D heatmap on metal is implemented. Summary of changes:

---

## Step 1: Color Sensitivity — 5–10°C Visible Variations

### Step 1.1–1.3

**Created:** `my-app/src/utils/heatmapShaderUtils.ts`  
- TypeScript mirror of the shader temperature→color mapping  
- `temperatureToColor(minTemp, maxTemp, stepCelsius, temp)` returns `[r,g,b]` in 0–1  
- 8 anchors: dark blue → cyan → teal → green → yellow → orange → red  

**Created:** `my-app/src/__tests__/utils/heatmapShaderUtils.test.ts`  
- Tests for 10°C and 5°C color differences, edge cases, clamping  

**Modified:** `my-app/src/components/welding/shaders/heatmapFragment.glsl.ts`  
- Added `uMinTemp`, `uMaxTemp`, `uStepCelsius`  
- Stepped gradient with 8 anchors (0°–500°C, ~10° per step)  
- NaN guard for missing texture data  

**Modified:** `my-app/src/components/welding/HeatmapPlate3D.tsx`  
- Added `minTemp`, `colorSensitivity` props (defaults: 0, 10)  
- `maxTemp` default set to 500  

---

## Step 2.0: Extract `extractFivePointFromFrame`

**Modified:** `my-app/src/utils/frameUtils.ts`  
- `extractFivePointFromFrame(frame)`  
- `DEFAULT_AMBIENT_CELSIUS = 20`  

**Modified:** `my-app/src/components/welding/HeatmapPlate3D.tsx`  
- Imports and uses `extractFivePointFromFrame` from `frameUtils`  

---

## Step 2.1 / 2.2a–2.2b: ThermalPlate and TorchWithHeatmap3D

**Created:** `my-app/src/components/welding/ThermalPlate.tsx`  
- Reusable thermal workpiece mesh (100×100 grid, vertex displacement, stepped gradient)  
- Props: `frame`, `maxTemp`, `minTemp`, `plateSize`, `colorSensitivity`  

**Modified:** `my-app/src/components/welding/HeatmapPlate3D.tsx`  
- Uses `ThermalPlate` instead of internal `Plate`  

**Created:** `my-app/src/components/welding/TorchWithHeatmap3D.tsx`  
- Unified torch + thermal metal  
- Thermal workpiece when `frames` has data, flat metal when `frames` is empty  
- Same scene structure as TorchViz3D (PBR, shadows, OrbitControls)  
- Context-loss handling and HUD  

---

## Step 2.2–2.3: Replay and Demo Integration

**Modified:** `my-app/src/app/replay/[sessionId]/page.tsx`  
- Replaced `TorchViz3D` and `HeatmapPlate3D` with `TorchWithHeatmap3D`  
- Left/right: `TorchWithHeatmap3D` with `frames={thermal_frames}`  
- Removed separate HeatmapPlate3D; thermal data shown on metal in 3D  
- Non‑thermal sessions: HeatMap + “Thermal data shown in 3D view above” when thermal  

**Modified:** `my-app/src/app/demo/page.tsx`  
- Replaced `TorchViz3D` and `HeatmapPlate3D` with `TorchWithHeatmap3D`  
- Added `noviceThermalFrames` memo  
- Both expert and novice use `TorchWithHeatmap3D` with thermal frames  
- HeatMap only when thermal frames are absent  

---

## Step 2.4 & Phase 4: WebGL, ESLint, Tests, Docs

**Modified:** `my-app/src/constants/webgl.ts`  
- `MAX_CANVAS_PER_PAGE` set to 2  

**Modified:** `my-app/eslint-rules/max-torchviz3d-per-page.cjs`  
- Counts both `TorchViz3D` and `TorchWithHeatmap3D`  

**Modified:** `documentation/WEBGL_CONTEXT_LOSS.md`  
- Documented TorchWithHeatmap3D  

**Modified:** `my-app/src/__tests__/app/replay/[sessionId]/page.test.tsx`  
- Mocks and expectations updated for `TorchWithHeatmap3D`  

**Modified:** `my-app/src/__tests__/app/demo/page.test.tsx`  
- Mocks updated for `TorchWithHeatmap3D`  

**Created:** `my-app/src/__tests__/components/welding/TorchWithHeatmap3D.test.tsx`  
- Basic render, HUD, context-loss behavior  

**Modified:** `CONTEXT.md`, `LEARNING_LOG.md`  
- Documented TorchWithHeatmap3D and color sensitivity  

---

## Files Created

1. `my-app/src/utils/heatmapShaderUtils.ts`
2. `my-app/src/__tests__/utils/heatmapShaderUtils.test.ts`
3. `my-app/src/components/welding/ThermalPlate.tsx`
4. `my-app/src/components/welding/TorchWithHeatmap3D.tsx`
5. `my-app/src/__tests__/components/welding/TorchWithHeatmap3D.test.tsx`

## Files Modified

1. `my-app/src/components/welding/shaders/heatmapFragment.glsl.ts`
2. `my-app/src/components/welding/HeatmapPlate3D.tsx`
3. `my-app/src/utils/frameUtils.ts`
4. `my-app/src/app/replay/[sessionId]/page.tsx`
5. `my-app/src/app/demo/page.tsx`
6. `my-app/src/constants/webgl.ts`
7. `my-app/eslint-rules/max-torchviz3d-per-page.cjs`
8. `documentation/WEBGL_CONTEXT_LOSS.md`
9. `my-app/src/__tests__/app/replay/[sessionId]/page.test.tsx`
10. `my-app/src/__tests__/app/demo/page.test.tsx`
11. `CONTEXT.md`
12. `LEARNING_LOG.md`

---

## Feature Completeness Checklist

- [x] Color sensitivity (5–10°C) with stepped gradient  
- [x] `extractFivePointFromFrame` in `frameUtils`  
- [x] `ThermalPlate` and `TorchWithHeatmap3D`  
- [x] Replay and demo use `TorchWithHeatmap3D` instead of separate components  
- [x] HeatmapPlate3D removed from replay/demo; ThermalPlate kept for dev  
- [x] Canvas count reduced from 3 to 2  
- [x] WebGL constants and ESLint rule updated  
- [x] Tests and docs updated  

---

## Manual Verification

1. `cd my-app && npm run dev`
2. Open `http://localhost:3000/demo`
3. Confirm torch + thermal metal in both expert and novice columns
4. Confirm 0–500°C scale, color changes between steps
5. Open `http://localhost:3000/replay/[sessionId]` (session with thermal data)
6. Confirm 2 unified 3D views with heat on metal
7. Run `npm test`
8. Run `cd my-app && npx eslint src/app/replay/ src/app/demo/`

---

# Implementation Status: COMPLETE

All planned steps are implemented and wired to the app. Ready for manual testing and review.

## Instructions

1. Fix ALL critical issues completely
2. Fix ALL minor issues
3. Don't break working code
4. Verify each fix
5. Update documentation

Output the COMPLETE fixed implementation (not diffs).
