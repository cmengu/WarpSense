
Here is the critique in the requested JSON format:

```json
{
  "score": 7.8,
  "dimension_scores": {
    "completeness": 8.0,
    "clarity": 8.0,
    "verification": 7.5,
    "dependencies": 8.5,
    "risk_management": 7.5,
    "edge_cases": 7.0,
    "production_ready": 7.0
  },
  "critical_issues": [
    "Step 2.3 layout ambiguity: The current replay grid has row 1 (2× TorchViz3D) and row 2 (HeatmapPlate3D | TorchAngleGraph in 2 cols). Plan says 'Remove HeatmapPlate3D block' but does not specify the resulting grid structure. After removal, does TorchAngleGraph span full width, or remain in a 2-col grid with an empty cell? Unclear grid structure could cause layout regressions. Add explicit layout spec: e.g. 'grid-cols-2 for 3D block; below, single full-width TorchAngleGraph when thermal; when no thermal, HeatMap full-width then TorchAngleGraph.'",
    "Step 2.2a thermal/workpiece logic drift: Plan says 'Copy Plate component from HeatmapPlate3D' for ThermalWorkpiece. Copying creates two code paths (HeatmapPlate3D Plate + TorchWithHeatmap3D ThermalWorkpiece) that will diverge over time. Red Team #2 identified this. Mitigation: Extract shared ThermalPlate (or ThermalWorkpiece) to a single file used by both HeatmapPlate3D and TorchWithHeatmap3D, or document a sync contract so future gradient/sensor changes are applied in both places."
  ],
  "minor_issues": [
    "Step 1.1b and 1.3 both wire Plate uniforms (uMinTemp, uStepCelsius). Red Team #1: Merge 1.1b into 1.3 to avoid duplicate wiring and confusion.",
    "Step 2.2d (dynamic import) is redundant with 2.3/2.4 which add TorchWithHeatmap3D — roll 2.2d into 2.3 and 2.4 when integrating.",
    "Step 2.2b: 'Copy SceneContent from TorchViz3D' — add file path and function name: TorchViz3D.tsx SceneContent (lines ~63–130). Improves actionability.",
    "Step 4.2/4.3: Plan references 'my-app/src/__tests__/app/demo/page.test.tsx (or equivalent)'. Verify exact path; demo test exists at src/__tests__/app/demo/page.test.tsx.",
    "ESLint: Plan says 'ESLint/constants updated if needed'. ESLint max-torchviz3d-per-page.cjs only counts TorchViz3D. After change, pages will have 0 TorchViz3D and 2 TorchWithHeatmap3D — rule would pass but no longer enforce Canvas limit. Make ESLint update required, not optional.",
    "Phase 3 verification is 100% manual. Consider adding a lightweight automated smoke: 'TorchWithHeatmap3D with thermal frames renders without error' (Jest + R3F test-utils or similar) to catch regressions.",
    "LUT fallback: Gap 1 says 'need concrete 512×1 texture generation'. Plan defers LUT to Phase 4 'if user reports banding'. Add one sentence: 'LUT fallback: create 512×1 DataTexture, fill with temperatureToColor at 500/512 intervals, sample in shader by t.'"
  ],
  "strengths": [
    "Phase 0 pre-planning is exceptional: synthesis, dependency graph, risk planning, Red Team. Rare to see this depth.",
    "Steps 1.2, 1.4, 2.1 include full, copy-pasteable code (GLSL, TS mirror, extractFivePointFromFrame). Reduces ambiguity.",
    "10 failure modes with mitigations; 10 Red Team problems with severity and fixes. Strong risk awareness.",
    "12 success criteria with P0/P1 priority; 10 'junior engineer' Q&A pre-answered. High implementability.",
    "Dependency graph is a valid DAG; critical path and bottlenecks identified. Ordering is correct.",
    "Demo-data inspection correct: both generateExpertSession and generateNoviceSession produce thermal (buildFrames uses THERMAL_INTERVAL_MS). noviceThermalFrames memo is needed and plan includes it.",
    "getFrameAtTimestamp(thermal_frames, ts) for carry-forward is correct; frameUtils and replay useFrameData patterns validated."
  ],
  "reasoning": "The plan demonstrates strong engineering discipline: thorough pre-planning, explicit dependency ordering, full code for critical steps, and comprehensive risk coverage. The core approach (TorchWithHeatmap3D facade, stepped gradient, extractFivePointFromFrame to frameUtils) is sound and matches the codebase. Two issues rise to critical: (1) Replay layout — removing HeatmapPlate3D changes the grid structure; the plan does not specify the resulting layout, which could cause responsive breakage or misaligned elements. (2) ThermalWorkpiece/Plate copy — maintaining two copies of thermal rendering logic creates drift risk; extracting a shared component or documenting a sync contract would prevent future bugs. The minor issues are mostly polish: merging redundant steps (1.1b into 1.3, 2.2d into 2.3/2.4), clarifying file/line refs, making ESLint update required, and adding a brief LUT fallback spec. Verification is solid per-step but Phase 3 is fully manual; an automated smoke for TorchWithHeatmap3D would improve regression safety. Score 7.8 reflects a high-quality plan that needs two targeted fixes (layout spec, thermal logic sharing) before execution. With those addressed, this would rate 8.5–9.0.",
  "improvement_priority": [
    "CRITICAL: Add explicit replay grid layout spec to Step 2.3 (TorchAngleGraph/HeatMap positioning after HeatmapPlate3D removal)",
    "CRITICAL: Extract shared ThermalPlate/ThermalWorkpiece or document sync contract; avoid copying Plate logic",
    "Minor: Merge Step 1.1b into 1.3 (wire all Plate props at once)",
    "Minor: Roll Step 2.2d into 2.3 and 2.4",
    "Minor: Add file path and line ref for SceneContent in Step 2.2b",
    "Minor: Make ESLint update required (not optional) in Step 4.5",
    "Minor: Add one-sentence LUT fallback spec in Step 1.2 or Part 0 Gap 1"
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score of 7.8 indicates the core approach is sound but has 2 critical gaps (layout spec, thermal logic sharing). These are targeted fixes that do not require rethinking the plan. Use refine-plan.md to address the critical issues while preserving the strong foundation. No need to revisit exploration or replan."
}
```
