
```json
{
  "score": 8.2,
  "dimension_scores": {
    "completeness": 8.5,
    "clarity": 8.5,
    "verification": 7.5,
    "dependencies": 9.0,
    "risk_management": 8.0,
    "edge_cases": 8.5,
    "production_ready": 7.0
  },
  "critical_issues": [
    "Step 2.1 — Implementation/test contract violation: The unit test specifies 'Frame with thermal_snapshots[0].readings empty → returns null', but the provided extractFivePointFromFrame implementation returns an object { center: 20, north: 20, ... } when readings is [] (get() returns DEFAULT_AMBIENT_CELSIUS for each). Add guard before the get loop: `const readings = frame.thermal_snapshots[0].readings ?? []; if (readings.length === 0) return null;` to align implementation with test spec."
  ],
  "minor_issues": [
    "Step 2.2b: Line reference 'lines ~63–130' for TorchViz3D SceneContent is inaccurate — actual SceneContent spans lines 64–189. Update to 'lines ~64–189' to avoid wrong copy range.",
    "Step 4.1: Overlaps with 2.3/2.4 — both 2.3 and 2.4 explicitly remove HeatmapPlate3D. Clarify that 4.1 is a verification/audit step (grep to confirm no remnants) rather than duplicate removal.",
    "Step 1.5: Pass criteria has two formulations — 'per-channel diff > 0.06' and '|ΔR|+|ΔG|+|ΔB| > 0.18'. The latter is redundant if any per-channel exceeds 0.06. Unify to: per-channel > 0.06 for at least one channel (R, G, or B).",
    "ThermalPlate extraction: Step 2.2a moves DataTexture, ShaderMaterial, textureRef, materialRef logic. Add explicit note to preserve the useEffect-for-DataTexture-update pattern (avoids GPU read race) — this is subtle and could regress if extraction changes render timing.",
    "Step 2.1: extractFivePointFromFrame code block omits import for ThermalDirection. frameUtils.ts already has `import type { ThermalDirection } from '@/types/thermal'` — add one-line note: 'Ensure ThermalDirection import exists in frameUtils.ts.'"
  ],
  "strengths": [
    "Part 0 pre-planning: core approach, data flow, risk analysis, dependency brainstorm, and Gaps 1–3 with concrete specs (LUT, demo novice, ESLint).",
    "Full copy-pasteable code for Steps 1.2 (GLSL stepped gradient), 1.4 (TS mirror), 2.1 (extractFivePointFromFrame), and 2.2a (ThermalPlateProps interface).",
    "Explicit Replay layout spec (Step 2.3): Row 1/Row 2 table and Row 2 rule (primary session thermal_frames only) removes ambiguity.",
    "Step 3.3/4.4 sequencing clarified: TorchWithHeatmap3D.test.tsx created in 3.3, expanded in 4.4 — dependency table updated.",
    "Dependency graph (Section 9) is a valid DAG with no cycles; critical path and bottlenecks identified.",
    "10 failure modes in Part 0 C with explicit handling (null frame → ambient, frames empty → flat metal, etc.).",
    "extractFivePointFromFrame unit tests (Step 2.1) cover 6 edge cases including null, has_thermal_data: false, empty snapshots/readings, missing directions, full 5-point.",
    "Red Team (Section 11) identifies 10 potential problems with mitigations; most addressed in refinements.",
    "Common Failures & Fixes (Section 7) and Implementability Test (10 Q&A) make the plan auditable and debuggable.",
    "Shared ThermalPlate extraction prevents drift; sync contract documented in 2.2a."
  ],
  "reasoning": "The plan has been refined five times and now reflects production-grade structure: explicit ThermalPlateProps, Row 2 layout rule, extractFivePointFromFrame unit tests, 3.3/4.4 sequencing, and webgl constant comments. Codebase verification confirms HeatmapPlate3D has extractFivePointFromFrame at lines 37–51; frameUtils has getFrameAtTimestamp, ThermalDirection import; demo-data's buildFrames produces thermal for both expert and novice; heatmapFragment has only uMaxTemp; ESLint rule counts TorchViz3D. There is one critical issue: Step 2.1's unit test expects null for empty readings, but the implementation returns an object with DEFAULT_AMBIENT_CELSIUS — a contract violation that would cause the test to fail. Adding the empty-readings guard fixes this. Minor issues are clarity and precision: line references, step overlap clarification, pass-criteria unification, extraction-pattern note, and import reminder. The weighted score of 8.2 reflects a solid plan with one targeted fix needed. Completeness and dependencies are strong (8.5, 9.0). Verification is good but Phase 3 remains largely manual. Production readiness is adequate for a frontend feature (no new monitoring required).",
  "improvement_priority": [
    "CRITICAL: Add empty-readings guard to extractFivePointFromFrame in Step 2.1: `if (readings.length === 0) return null;` after `const readings = ...`.",
    "Minor: Update Step 2.2b SceneContent line reference to 64–189.",
    "Minor: Clarify Step 4.1 as verification step; 2.3/2.4 perform removal.",
    "Minor: Unify Step 1.5 pass criteria.",
    "Minor: Add note about preserving useEffect/DataTexture pattern in ThermalPlate extraction."
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 8.2 falls in 8.0–8.9 range. One critical fix (empty-readings guard) is required; otherwise the plan is production-ready. Use refine-plan.md to add the guard to Step 2.1 and address minor clarity issues. No replan or re-exploration needed — the approach is sound and codebase-aligned."
}
```
