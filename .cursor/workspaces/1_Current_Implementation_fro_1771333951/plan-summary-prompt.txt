# Compress Plan to Execution Summary

Extract ONLY the actionable steps - no explanations, rationale, or background.

## Format:
```
## Steps
1. File: path/to/file.tsx
   - Action: What to do
   - Key code: One-line reference

2. File: path/to/other.ts
   - Action: What to do
   - Key code: One-line reference

## Critical Details
- Constraint 1
- Constraint 2
```

Keep under 50 lines. Maximum brevity.

---

# Full Plan to Compress:


Summary of the refinements in `.cursor/plans/wwad-macro-analytics-implementation-plan.md`:

**Phase 2 – Backend**
- **Division by zero:** Added explicit `len(scored) == 0` check; `avg_score` is only computed when `len(scored) > 0`.
- **Sessions truncation:** Limited session list to 1000, added `sessions_truncated` flag to the API response.
- **Tests:** Added `test_aggregate_kpis_zero_sessions_all_null_score` and `test_aggregate_invalid_date_returns_400`.

**Phase 3 – Frontend**
- **API_BASE_URL:** Documented environment switching for local/dev/prod via `NEXT_PUBLIC_API_URL`.
- **CalendarHeatmap:** Switched to UTC (`setUTCDate`, `Date.UTC`) for date handling.
- **aggregateToDashboardData:** Guards for null/missing `trend` and `calendar`, with `[]` fallbacks.
- **aggregate-transform tests:** Edge cases for empty trend, null trend, all-zero trend, malformed response.
- **Orthogonality:** ESLint `no-restricted-imports` required in CI.

**Phase 4 – Export**
- **Truncation alert:** Mandatory, clearly visible UI alert when `sessions_truncated` is true.
- **E2E test (Step 4.8):** Date filter and CSV export covered in Playwright tests.

**Phase 1**
- **Backfill:** Automated `verify_backfill.py` to confirm all COMPLETE sessions have `score_total` (exit 1 if not).

**Definition of Done**
- **Observability:** Required logging of aggregate requests (and truncation when present).
- **Frontend:** Log export failures; recommended Sentry/LogRocket for production.

**New section 7.1 – Known issues & limitations**
- Implicit API contract (runtime guards; optional contract tests).
- No pagination (1000-session cap).
- Frontend error logging (console now; Sentry later).
