
```json
{
  "score": 7.8,
  "dimension_scores": {
    "completeness": 8.0,
    "clarity": 8.5,
    "verification": 7.5,
    "dependencies": 8.0,
    "risk_management": 7.5,
    "edge_cases": 7.5,
    "production_ready": 7.5
  },
  "critical_issues": [
    "Step 1.4 'Implement lazy persistence in get_session_score': Verification is manual ('GET /api/sessions/sess_expert_001/score', 'DB shows score_total updated'). Project rules (.cursorrules) require 'Verification to be done by adding and running automated tests (no manual browser checks)'. Add an automated test (e.g. in test_get_session_score.py): (1) seed a COMPLETE session with frames and score_total=NULL, (2) call GET /api/sessions/{id}/score, (3) re-query SessionModel from the test db_session and assert score_total is now set. Without this, persistence is not verified per project mandate.",
    "Step 1.5 'Create backfill_score_total.py': If extract_features or score_session raises for a single session (e.g. corrupted frames), the whole batch fails with an unhandled exception and db.commit() never runs. Add per-session try/except: wrap extract_features + score_session + assignment in try/except, log failed session_id, continue. Commit the batch only when all sessions in the batch succeed; optionally collect failures for a final report."
  ],
  "minor_issues": [
    "Step 2.6: No exact EXPLAIN command for index verification. Add a concrete example that runs the query with EXPLAIN and asserts 'Index Scan' or 'idx_sessions_start_time' in the output.",
    "Step 2.2 aggregate_service: sessions = q.all() loads all sessions in range. Section 7.1 mentions this; add an explicit note in Step 2.2 that for production with >2000 sessions in range, consider chunked aggregation.",
    "Step 4.8 E2E test: Uses page.waitForTimeout(500). Replace with deterministic waits (e.g. waitForSelector with timeout).",
    "Definition of Done requires aggregate endpoint logging, but no dedicated step. Add an explicit step or expand Step 2.3 with the exact logger.info call and fields.",
    "Step 4.1 generateCSV: undefined values become 'undefined' string. Add handling for null/undefined (e.g. use '' for null/undefined)."
  ],
  "strengths": [
    "Phase 0 pre-planning covers dependency graph, risks, and failure modes well.",
    "Step 1.2: Pre-migration backup script with DATABASE_URL extraction and exit 1 on failure.",
    "Step 2.2: Division-by-zero guard (len(scored)==0 → avg_score=None); covered by test_aggregate_kpis_zero_sessions_all_null_score.",
    "Step 3.4: Runtime guards for null kpis/trend/calendar; solid transform tests.",
    "Step 4.3: Mandatory truncation alert when sessions_truncated.",
    "Step 2.4: Route ordering documented (aggregate_router before sessions_router).",
    "Many steps include full code examples.",
    "CalendarHeatmap uses setUTCDate/Date.UTC; aggregate_service uses start_time < (date_end + 1 day) for inclusive end.",
    "verify_backfill.py with exit 1 on failure.",
    "ESLint no-restricted-imports for orthogonality."
  ],
  "reasoning": "The plan is thorough and well-structured with 34 steps across four phases. Most steps have full code examples and verification tests. Two critical gaps block verification and robustness: (1) Step 1.4 persistence is verified manually, which violates the rule that verification must use automated tests. (2) The backfill script has no per-session error handling, so one bad session stops the entire run. Migration down_revision matches 002_disable_sensor_checks. The weighted score of 7.8 reflects a strong plan that needs these two fixes before meeting the project’s verification mandate and production robustness bar.",
  "improvement_priority": [
    "CRITICAL: Add automated test for get_session_score persistence (Step 1.4).",
    "CRITICAL: Add per-session try/except in backfill script (Step 1.5).",
    "Minor: Add an explicit logging step for the aggregate route.",
    "Minor: Replace page.waitForTimeout in Step 4.8 with deterministic waits.",
    "Minor: Add null/undefined handling in generateCSV."
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 7.8 is in 7.0–7.9 (Has Critical Flaws). The approach is sound; the main fixes are an automated persistence test and backfill error handling. Use refine-plan.md to update Steps 1.4 and 1.5 accordingly."
}
```
