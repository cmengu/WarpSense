# Refine Plan (Intelligent Improvement with Deep Analysis)

You are a **principal engineer** conducting a targeted refinement of an implementation plan based on a comprehensive critique. This requires strategic thinking about what to preserve, what to fix, and how to integrate improvements seamlessly.

---

## CRITICAL: Your Mission

You have received:
1. **Current Plan** - The implementation plan being refined
2. **Critique** - A detailed analysis with scores, issues, and recommendations
3. **History** - Previous iterations and what was attempted

Your task: **Output a COMPLETE, IMPROVED plan** that:
- ✅ Fixes ALL critical issues completely
- ✅ Addresses minor issues where they improve quality
- ✅ Preserves ALL strengths and working sections
- ✅ Maintains logical structure and flow
- ✅ Remains executable and actionable

**DO NOT:**
- ❌ Just list changes or provide diffs
- ❌ Explain what you're doing or why
- ❌ Remove good working content
- ❌ Add unnecessary complexity
- ❌ Output anything except the refined plan

---

## Phase 1: Critique Analysis & Strategy Formation

### 1.1 Parse the Critique

First, extract from the critique JSON:
- **Score:** What's the current quality level?
- **Critical Issues:** What MUST be fixed? (execution-blocking)
- **Minor Issues:** What SHOULD be fixed? (quality improvements)
- **Strengths:** What MUST be preserved?
- **Recommended Action:** Was this flagged for deep revision or polish?

### 1.2 Issue Classification & Prioritization

Organize issues by type:

**Type A: Missing Core Steps**
- Example: "No database backup before migration"
- Fix: Add complete new step with verification
- Impact: Could cause data loss
- Priority: P0 - Must fix

**Type B: Insufficient Error Handling**
- Example: "No null check for API response"
- Fix: Add try-catch, null guards, fallback behavior
- Impact: Runtime errors, crashes
- Priority: P0 - Must fix

**Type C: Unclear/Vague Instructions**
- Example: "Update the component" → needs specifics
- Fix: Add file paths, function names, code examples
- Impact: Execution ambiguity
- Priority: P1 - High priority

**Type D: Missing Verification**
- Example: "Test manually" without test cases
- Fix: Add specific test cases, expected outputs, validation commands
- Impact: Can't verify success
- Priority: P1 - High priority

**Type E: Dependency/Sequencing Issues**
- Example: Step 5 depends on Step 8
- Fix: Reorder steps, add explicit prerequisites
- Impact: Execution failure
- Priority: P0 - Must fix

**Type F: Missing Edge Cases**
- Example: "Doesn't handle empty array"
- Fix: Add edge case handling logic
- Impact: Failures on boundary conditions
- Priority: P1-P2 depending on likelihood

**Type G: Polish/Optimization**
- Example: "Could add TypeScript types"
- Fix: Add if improves clarity significantly
- Impact: Code quality, maintainability
- Priority: P2 - Nice to have

---

## Phase 2: Refinement Strategy by Issue Type

### Strategy A: Adding Missing Steps

**When to use:** Critical issue indicates entire step is missing

**Process:**
1. Identify where in sequence the step belongs
2. Draft the complete step with:
   - Clear action description
   - Code example or command
   - Expected outcome
   - Verification method
   - Dependencies
   - Error handling
3. Integrate seamlessly into existing flow
4. Update step numbering if needed
5. Cross-reference with related steps

**Example:**

**Critique says:** "Missing database backup before migration in Step 4"

**Bad refinement:**
```
3.5. Backup database
```

**Good refinement:**
```
## Step 3.5: Create Pre-Migration Database Backup

**Action:**
Create a timestamped backup of the production database before running migrations.

**Implementation:**
```bash
# Create backup directory if it doesn't exist
mkdir -p ./backups

# Generate timestamped backup
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
pg_dump -h localhost -U postgres -d production_db > ./backups/backup_${TIMESTAMP}.sql

# Verify backup created successfully
if [ $? -eq 0 ]; then
    echo "✅ Backup created: backup_${TIMESTAMP}.sql"
    echo "Size: $(du -h ./backups/backup_${TIMESTAMP}.sql | cut -f1)"
else
    echo "❌ Backup failed - ABORT MIGRATION"
    exit 1
fi
```

**Verification:**
- Backup file exists in `./backups/` directory
- File size > 0 bytes
- Can open file and see SQL commands

**Rollback (if migration fails):**
```bash
# Restore from backup
psql -h localhost -U postgres -d production_db < ./backups/backup_${TIMESTAMP}.sql
```

**Time estimate:** 2-5 minutes (depending on database size)

**Dependencies:** Requires `pg_dump` installed, database credentials configured
```

---

### Strategy B: Adding Error Handling

**When to use:** Critical issue about missing error handling or null checks

**Process:**
1. Identify the risky operation
2. Determine failure modes (what could go wrong?)
3. Add defensive checks before operation
4. Wrap in try-catch where applicable
5. Provide fallback behavior
6. Log errors for debugging

**Example:**

**Critique says:** "Step 7 doesn't handle null heatmapData"

**Current (problematic):**
```javascript
function renderHeatmap() {
  heatmapData.forEach(point => {
    // render point
  });
}
```

**Refined (with error handling):**
```javascript
function renderHeatmap() {
  // Guard: Check if data exists
  if (!heatmapData) {
    console.warn('⚠️ Heatmap data not loaded yet');
    return; // Exit gracefully
  }

  // Guard: Check if array is empty
  if (heatmapData.length === 0) {
    console.warn('⚠️ Heatmap data is empty');
    // Show "No data" UI instead of blank screen
    showNoDataMessage();
    return;
  }

  // Process data with error handling
  try {
    heatmapData.forEach(point => {
      // Validate each point
      if (!point || typeof point.temperature !== 'number') {
        console.error('Invalid point data:', point);
        return; // Skip this point
      }
      
      // Render point
      renderPoint(point);
    });
  } catch (error) {
    console.error('Error rendering heatmap:', error);
    // Fallback: show error state
    showErrorMessage('Failed to render heatmap');
  }
}
```

---

### Strategy C: Improving Clarity & Specificity

**When to use:** Minor issue about vague instructions

**Process:**
1. Identify what's ambiguous
2. Add concrete details:
   - File paths
   - Function names
   - Import statements
   - Exact commands
   - Expected outputs
3. Provide code examples
4. Show before/after if modifying existing code

**Example:**

**Critique says:** "Step 2 'Import required libraries' is too vague"

**Current (vague):**
```
## Step 2: Import Libraries

Import the necessary libraries for 3D rendering and heatmap visualization.
```

**Refined (specific):**
```
## Step 2: Import Required Dependencies

**Action:**
Add the following imports to the top of `src/components/ReplayViewer.tsx`:

**Code:**
```typescript
// Three.js core and loaders
import * as THREE from 'three';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

// Custom shaders
import { HeatmapShader } from '@/shaders/HeatmapShader';

// React and state management
import { useEffect, useRef, useState } from 'react';
import { useReplayStore } from '@/stores/replayStore';

// Utils
import { temperatureToColor } from '@/utils/colorMapping';
import { interpolateHeatData } from '@/utils/heatInterpolation';
```

**Verification:**
- No TypeScript errors in file
- Imports resolve (check with `npm run typecheck`)
- Auto-complete works for imported functions

**Dependencies:**
Ensure these packages are installed:
```bash
npm install three@0.160.0
npm install @types/three@0.160.0 --save-dev
```

**Time estimate:** 2-3 minutes
```

---

### Strategy D: Adding Verification Steps

**When to use:** Issue about missing or weak verification

**Process:**
1. For each step, define "done" criteria
2. Provide specific test cases
3. Include expected outputs
4. Add commands to run tests
5. Include negative test cases (what should fail)

**Example:**

**Critique says:** "Step 5 verification just says 'check colors' - not specific"

**Current (weak):**
```
**Verification:**
- Check that colors change with temperature
```

**Refined (specific):**
```
**Verification:**

**Manual Visual Test:**
1. Open browser DevTools (F12)
2. Go to Console tab
3. Run these test scenarios:

```javascript
// Test 1: Cold temperature (0°F) - should be deep blue
testTemperature(0);
// Expected: RGB(0, 0, 139) - dark blue

// Test 2: Warm temperature (250°F) - should be yellow-orange  
testTemperature(250);
// Expected: RGB(255, 165, 0) - orange

// Test 3: Hot temperature (500°F) - should be bright red/white
testTemperature(500);
// Expected: RGB(255, 255, 255) - white-hot

// Test 4: 5°F increments - should see visible color change
for (let temp = 0; temp <= 50; temp += 5) {
  console.log(`${temp}°F:`, temperatureToColor(temp));
}
// Expected: Should see gradual transition from blue → cyan

// Test 5: Negative temperature - should clamp to 0°F color
testTemperature(-50);
// Expected: Same as 0°F (deep blue), no error

// Test 6: Above max temperature - should clamp to 500°F color  
testTemperature(1000);
// Expected: Same as 500°F (white), no error
```

**Automated Test:**
```typescript
// Add to src/__tests__/colorMapping.test.ts
describe('temperatureToColor', () => {
  test('visible change every 5°F', () => {
    for (let temp = 0; temp < 500; temp += 5) {
      const color1 = temperatureToColor(temp);
      const color2 = temperatureToColor(temp + 5);
      
      // Calculate color difference
      const diff = Math.abs(color1.r - color2.r) + 
                   Math.abs(color1.g - color2.g) + 
                   Math.abs(color1.b - color2.b);
      
      // Expect visible difference (threshold: 10 on 0-765 scale)
      expect(diff).toBeGreaterThan(10);
    }
  });

  test('handles edge cases', () => {
    expect(() => temperatureToColor(-100)).not.toThrow();
    expect(() => temperatureToColor(9999)).not.toThrow();
    expect(temperatureToColor(null)).toEqual({ r: 0, g: 0, b: 0 }); // fallback
  });
});
```

**Success Criteria:**
- All manual tests show expected colors
- Automated tests pass (`npm test`)
- Color transitions are smooth and visible
- No console errors
```

---

### Strategy E: Fixing Dependencies & Sequencing

**When to use:** Critical issue about step ordering or missing prerequisites

**Process:**
1. Map dependencies between steps
2. Identify circular dependencies or out-of-order steps
3. Reorder steps logically
4. Add explicit prerequisite sections
5. Update step references throughout plan

**Example:**

**Critique says:** "Step 5 uses heatmapShader but it's not created until Step 8"

**Current (broken sequence):**
```
## Step 5: Apply Shader to Metal
Use heatmapShader on material...

## Step 8: Create Heatmap Shader
Define the shader code...
```

**Refined (correct sequence):**
```
## Step 5: Create Heatmap Shader

**Prerequisites:** None (this step must come first)

**Action:**
Define the custom shader for heatmap rendering.

**Implementation:**
```typescript
// src/shaders/HeatmapShader.ts
export const HeatmapShader = {
  uniforms: {
    temperatureMap: { value: null },
    minTemp: { value: 0.0 },
    maxTemp: { value: 500.0 }
  },
  vertexShader: `
    varying vec2 vUv;
    void main() {
      vUv = uv;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
  `,
  fragmentShader: `
    uniform sampler2D temperatureMap;
    uniform float minTemp;
    uniform float maxTemp;
    varying vec2 vUv;
    
    vec3 temperatureToColor(float temp) {
      // Normalize temp to 0-1 range
      float t = clamp((temp - minTemp) / (maxTemp - minTemp), 0.0, 1.0);
      
      // Color gradient: blue → cyan → green → yellow → red → white
      if (t < 0.2) {
        return mix(vec3(0.0, 0.0, 0.545), vec3(0.0, 1.0, 1.0), t * 5.0);
      } else if (t < 0.4) {
        return mix(vec3(0.0, 1.0, 1.0), vec3(0.0, 1.0, 0.0), (t - 0.2) * 5.0);
      } else if (t < 0.6) {
        return mix(vec3(0.0, 1.0, 0.0), vec3(1.0, 1.0, 0.0), (t - 0.4) * 5.0);
      } else if (t < 0.8) {
        return mix(vec3(1.0, 1.0, 0.0), vec3(1.0, 0.0, 0.0), (t - 0.6) * 5.0);
      } else {
        return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 1.0), (t - 0.8) * 5.0);
      }
    }
    
    void main() {
      float temp = texture2D(temperatureMap, vUv).r;
      vec3 color = temperatureToColor(temp);
      gl_FragColor = vec4(color, 1.0);
    }
  `
};
```

**Verification:**
- File created at `src/shaders/HeatmapShader.ts`
- No TypeScript errors
- Can import in other files: `import { HeatmapShader } from '@/shaders/HeatmapShader'`

**Time estimate:** 15-20 minutes

---

## Step 6: Apply Shader to Metal Material

**Prerequisites:** 
- Step 5 completed (HeatmapShader created)
- Metal model loaded in scene

**Action:**
Replace metal's standard material with shader material using HeatmapShader.

**Implementation:**
```typescript
// In ReplayViewer.tsx
import { HeatmapShader } from '@/shaders/HeatmapShader';

// Find metal mesh
const metalMesh = scene.getObjectByName('metal_surface');

if (metalMesh) {
  // Create shader material
  const shaderMaterial = new THREE.ShaderMaterial({
    uniforms: THREE.UniformsUtils.clone(HeatmapShader.uniforms),
    vertexShader: HeatmapShader.vertexShader,
    fragmentShader: HeatmapShader.fragmentShader
  });
  
  // Apply to mesh
  metalMesh.material = shaderMaterial;
  
  console.log('✅ Heatmap shader applied to metal');
} else {
  console.error('❌ Metal mesh not found in scene');
}
```

**Verification:**
- Metal surface visible in viewport
- No console errors
- Material shows as ShaderMaterial in Three.js DevTools
```

---

### Strategy F: Adding Edge Case Handling

**When to use:** Issue about missing edge cases or boundary conditions

**Process:**
1. Enumerate edge cases for the operation
2. Add handling code for each case
3. Provide examples
4. Add tests for edge cases

**Example:**

**Critique says:** "Doesn't handle case when replay has no temperature data"

**Refined (with edge cases):**
```
## Step 9: Render Heatmap Frame

**Action:**
Render heatmap for current replay frame, handling edge cases.

**Implementation:**
```typescript
function renderHeatmapFrame(frameIndex: number) {
  // Edge Case 1: Invalid frame index
  if (frameIndex < 0 || frameIndex >= totalFrames) {
    console.warn(`⚠️ Frame ${frameIndex} out of range (0-${totalFrames-1})`);
    return;
  }

  // Edge Case 2: No temperature data for this replay
  const temperatureData = replayStore.getTemperatureData();
  if (!temperatureData || temperatureData.length === 0) {
    console.warn('⚠️ No temperature data available');
    // Show default/neutral color
    setUniformTemperature(metalMesh, 70); // Room temperature
    return;
  }

  // Edge Case 3: Frame exists but has no data points
  const frameData = temperatureData[frameIndex];
  if (!frameData || frameData.points.length === 0) {
    console.warn(`⚠️ Frame ${frameIndex} has no data points`);
    // Interpolate from adjacent frames
    const interpolated = interpolateFromAdjacentFrames(frameIndex);
    applyTemperatureData(interpolated);
    return;
  }

  // Edge Case 4: Sparse data (missing measurements at some points)
  if (frameData.points.length < metalMesh.geometry.vertices.length * 0.5) {
    console.warn(`⚠️ Frame ${frameIndex} has sparse data, interpolating...`);
    const filled = fillSparsedData(frameData);
    applyTemperatureData(filled);
    return;
  }

  // Normal case: Full data available
  applyTemperatureData(frameData);
}

// Helper: Interpolate when frame has no data
function interpolateFromAdjacentFrames(frameIndex: number) {
  const prevFrame = findPreviousValidFrame(frameIndex);
  const nextFrame = findNextValidFrame(frameIndex);
  
  if (prevFrame && nextFrame) {
    // Linear interpolation between frames
    const t = (frameIndex - prevFrame.index) / (nextFrame.index - prevFrame.index);
    return interpolateFrames(prevFrame.data, nextFrame.data, t);
  } else if (prevFrame) {
    // Use previous frame
    return prevFrame.data;
  } else if (nextFrame) {
    // Use next frame
    return nextFrame.data;
  } else {
    // No data at all - use defaults
    return getDefaultTemperatureData();
  }
}
```

**Edge Cases Covered:**
1. ✅ Invalid frame index (negative or beyond range)
2. ✅ No temperature data loaded
3. ✅ Frame with zero data points
4. ✅ Sparse/incomplete data points
5. ✅ First frame (no previous frame to interpolate from)
6. ✅ Last frame (no next frame to interpolate from)

**Test Cases:**
```typescript
describe('renderHeatmapFrame edge cases', () => {
  test('handles negative frame index', () => {
    expect(() => renderHeatmapFrame(-1)).not.toThrow();
  });

  test('handles frame beyond range', () => {
    expect(() => renderHeatmapFrame(9999)).not.toThrow();
  });

  test('handles missing temperature data', () => {
    replayStore.temperatureData = null;
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });

  test('handles empty data points', () => {
    replayStore.temperatureData = [{ points: [] }];
    expect(() => renderHeatmapFrame(0)).not.toThrow();
  });
});
```
```

---

## Phase 3: Execution - Building the Refined Plan

### 3.1 Structure Preservation

**Preserve the original structure unless critique explicitly says to reorganize.**

**Keep:**
- Main section headings
- Step numbering scheme
- Overall flow and narrative
- Introduction and overview sections

**Modify only:**
- Content that has issues
- Steps that need reordering (if dependency issue)
- Sections flagged in critique

---

### 3.2 Integration Approach

**For each issue in the critique:**

1. **Locate** the affected section in current plan
2. **Apply** the appropriate refinement strategy (A-G above)
3. **Verify** the fix doesn't break other sections
4. **Update** any cross-references
5. **Maintain** consistent formatting and style

---

### 3.3 Quality Checks Before Output

Before outputting the refined plan, verify:

**Completeness:**
- [ ] All critical issues addressed
- [ ] All minor issues addressed (or consciously skipped)
- [ ] All strengths preserved
- [ ] No sections accidentally deleted

**Consistency:**
- [ ] Step numbers sequential
- [ ] Cross-references updated
- [ ] Consistent terminology
- [ ] Uniform formatting

**Executability:**
- [ ] Each step has clear action
- [ ] Code examples are complete and runnable
- [ ] Verification criteria are specific
- [ ] Dependencies are explicit

**Clarity:**
- [ ] No ambiguous instructions
- [ ] File paths and names provided
- [ ] Expected outputs specified
- [ ] Time estimates included

---

## Phase 4: Output Format

### 4.1 What to Output

**Output ONLY the complete refined plan as markdown.**

**DO NOT include:**
- ❌ This meta-instruction document
- ❌ Explanations of what you changed
- ❌ Diffs or change summaries
- ❌ Commentary about the critique
- ❌ Preamble or conclusion about the refinement

**DO include:**
- ✅ Complete refined plan from beginning to end
- ✅ All sections (even unchanged ones)
- ✅ All fixes integrated seamlessly
- ✅ Proper markdown formatting

---

### 4.2 Output Template Structure

Your output should follow this general structure (adapt to specific plan):

```markdown
# [Plan Title]

## Overview
[High-level description of what will be implemented]

## Prerequisites
- [Dependency 1]
- [Dependency 2]
- [etc.]

## Implementation Steps

### Phase 1: [Phase Name]

#### Step 1.1: [Step Name]

**Action:**
[What to do]

**Implementation:**
```[language]
[Complete code]
```

**Verification:**
- [Specific test 1]
- [Specific test 2]

**Expected Outcome:**
[What success looks like]

**Time Estimate:** [X minutes/hours]

**Dependencies:** [What must be done first]

---

#### Step 1.2: [Step Name]

[... repeat pattern ...]

---

### Phase 2: [Phase Name]

[... continue ...]

---

## Testing & Validation

[Comprehensive testing section]

---

## Rollback Procedure

[How to undo if things go wrong]

---

## Known Issues & Limitations

[Any caveats or edge cases still not handled]

---

## Success Criteria

[How to know the entire implementation is complete]
```

---

## Phase 5: Special Cases & Advanced Scenarios

### Scenario A: Score 8-9 (Minor Polish)

**What it means:** Plan is mostly good, just needs small improvements

**Approach:**
- Address minor issues without disrupting working sections
- Focus on clarity and specificity
- Add verification where weak
- Don't over-refine

**Example fixes:**
- Add missing import statement
- Clarify vague file path
- Add specific test case
- Fix typo in command

---

### Scenario B: Score 5-7 (Significant Revision)

**What it means:** Plan has critical flaws, needs substantial work

**Approach:**
- Fix all critical issues first (execution-blocking)
- Reorder steps if sequencing is broken
- Add missing core steps
- Beef up error handling significantly
- May need to restructure sections

**Example fixes:**
- Add entire missing phase
- Reorder 5+ steps for correct dependencies
- Add comprehensive error handling throughout
- Replace vague steps with specific implementations

---

### Scenario C: Score <5 (Major Overhaul)

**What it means:** Plan is fundamentally flawed

**Approach:**
- Consider if plan addresses the original task at all
- May need to restructure entire plan
- Add multiple missing phases
- Complete rewrite of vague sections
- Extensive verification added throughout

**Example fixes:**
- Add 3 new major phases
- Complete rewrite of implementation approach
- Add all error handling, verification, testing
- Fix fundamental technical errors

---

### Scenario D: Conflicting Critique Points

**What to do when critique has contradictory suggestions:**

**Example:** 
- Issue #1: "Too vague, add more detail"
- Issue #5: "Too verbose, simplify Step 3"

**Resolution:**
1. Look at which is critical vs minor
2. Critical issues take precedence
3. For same priority: Balance both concerns
   - Add detail where execution requires it
   - Simplify prose without losing critical information

---

### Scenario E: Preserving Existing Good Code

**When critique says:** "Add error handling to Step 7" but Step 7 already has working code

**Approach:**
```typescript
// EXISTING (keep this):
function processData(data) {
  const result = transform(data);
  return result;
}

// ADD (integrate with existing):
function processData(data) {
  // NEW: Validation
  if (!data) {
    console.error('Data is null or undefined');
    return null;
  }

  // NEW: Error handling
  try {
    // EXISTING: Keep original logic
    const result = transform(data);
    return result;
  } catch (error) {
    // NEW: Error recovery
    console.error('Transform failed:', error);
    return getDefaultData();
  }
}
```

**Don't replace working code entirely - wrap it with improvements.**

---

## Phase 6: Final Checklist

Before submitting the refined plan, verify:

### Critical Requirements
- [ ] **All critical issues fixed** - None remain unaddressed
- [ ] **Plan is executable** - Someone could follow it step-by-step
- [ ] **Strengths preserved** - Good sections untouched
- [ ] **Complete output** - No placeholders like "TODO" or "[add details]"

### Quality Requirements
- [ ] **Specific, not vague** - File paths, function names, exact commands
- [ ] **Verifiable** - Clear success criteria for each step
- [ ] **Error handling** - Defensive code where needed
- [ ] **Edge cases** - Boundary conditions handled

### Formatting Requirements
- [ ] **Valid markdown** - Proper headers, code blocks, lists
- [ ] **Consistent style** - Uniform formatting throughout
- [ ] **Code blocks tagged** - Language specified (```typescript, ```bash, etc.)
- [ ] **No meta-content** - Only the plan itself, no explanations

---

## Remember

**You are outputting THE REFINED PLAN, not a report about the plan.**

**The refined plan should be:**
- ✅ Complete (beginning to end)
- ✅ Improved (all issues fixed)
- ✅ Executable (ready to implement)
- ✅ Clear (no ambiguity)

**The refined plan should NOT be:**
- ❌ A diff or changelog
- ❌ A summary of changes
- ❌ Commentary about the critique
- ❌ Partial or incomplete

---

**Now, examine the critique, apply the appropriate strategies, and output the complete refined plan.**

---
# Current Plan:


Refined implementation plan is in `.cursor/issues/beautiful-pdf-report-plan.md`. Summary of changes:

## Critique and adversarial fixes applied

### Adversarial fixes
1. **Step 3.2 — Orphaned `toPng` promise**  
   Added `.catch(() => {})` on the `toPng` promise to avoid `unhandledrejection` when the timeout fires first.

2. **Step 2.1 — Request body size limit**  
   Reject requests with `Content-Length > 5MB` with 413. Reduces OOM risk from large bodies.

3. **Step 2.1 — `chartDataUrl` type coercion**  
   Use `typeof chartDataUrl === "string"` before length checks and only accept PNG (`startsWith("data:image/png")`).

4. **Step 1.2 — Non-PNG rejection**  
   Only render `Image` when `chartDataUrl` is a valid PNG data URL. Reject SVG/webp and other formats.

5. **Step 4.3 — `toPng` mock isolation**  
   Use `try/finally` to restore the default `toPng` mock after the chart fallback test.

### Critique fixes
6. **Pre-Flight — `npm ci`**  
   Use `npm ci` after install to avoid lockfile drift.

7. **Step 3.3 — basePath handling**  
   Moved `getApiBase` into `lib/api-base.ts` and added unit tests (Step 4.5).

8. **Step 4.4 — Trend-chart assertion**  
   Added a test that checks the `trend-chart` element exists with `data-testid`.

9. **Step 2.2 — JSON parsing**  
   Use `jq` where available for response parsing.

10. **Step 2.3 — Oversized tests**  
   Use `node -e` to generate large payloads instead of shell brace expansion to reduce OOM risk on CI.

### Other updates
- **Step 4.6** — API tests now include the 413 body-size case.
- **Step 4.5** — New tests for `getApiBase()`.
- **Risk heatmap** — Updated with adversarial risks and mitigations.
- **Known issues** — Documented Content-Length caveats and `getApiBase` usage.

Total steps: 16 across 4 phases (est. ~17.5h).


---
# Standard Critique (issues to fix):


{"overall_strategy_score":8.1,"top_3_avg_priority":8.27,"context_type":"feature","goal_alignment_score":8.7,"task_scores":[{"task_number":1,"task_title":"Phase 1 — Dependencies and PDF Component (Steps 1.1–1.3)","strategic_value":8.0,"strategic_value_after_penalty":8.0,"context_alignment":9.0,"leverage_optionality":8.0,"resource_efficiency":6.5,"risk_balance":9.0,"dependencies":10.0,"execution_quality":9.0,"overall_score":8.3,"feedback_penalty_applied":false,"ships_wrong_risk":"If WelderReportPDF renders SVG/webp chartDataUrl, React PDF Image throws and API returns 500 instead of gracefully omitting chart.","higher_leverage_alternative":"null","reasoning":"Foundation task; exact paths and defensive isPngDataUrl; no blockers; directly enables forwardable PDF."},{"task_number":2,"task_title":"Phase 2 — API Route and Validation (Steps 2.1–2.3)","strategic_value":9.0,"strategic_value_after_penalty":9.0,"context_alignment":9.0,"leverage_optionality":7.5,"resource_efficiency":6.0,"risk_balance":7.0,"dependencies":5.0,"execution_quality":9.0,"overall_score":8.1,"feedback_penalty_applied":false,"ships_wrong_risk":"If Content-Length check is omitted or chartDataUrl type coercion is loose, oversized POST or malformed chart triggers OOM or 500.","higher_leverage_alternative":"null","reasoning":"Critical path for PDF generation; adversarial fixes (413, typeof, PNG-only) addressed; blocked by Phase 1."},{"task_number":3,"task_title":"Phase 3 — Client Integration and Download Flow (Steps 3.1–3.3)","strategic_value":9.0,"strategic_value_after_penalty":9.0,"context_alignment":10.0,"leverage_optionality":8.0,"resource_efficiency":6.0,"risk_balance":7.0,"dependencies":6.0,"execution_quality":9.0,"overall_score":8.4,"feedback_penalty_applied":false,"ships_wrong_risk":"If getApiBase is wrong when basePath is configured, fetch 404s in production and investor cannot download PDF during demo.","higher_leverage_alternative":"null","reasoning":"User-facing outcome; toPng orphan fix and basePath extraction address critique; dual-audience deliverable."},{"task_number":4,"task_title":"Phase 4 — Tests and Edge Cases (Steps 4.1–4.7)","strategic_value":7.5,"strategic_value_after_penalty":7.5,"context_alignment":8.0,"leverage_optionality":7.0,"resource_efficiency":5.5,"risk_balance":9.0,"dependencies":6.0,"execution_quality":9.0,"overall_score":7.3,"feedback_penalty_applied":false,"ships_wrong_risk":"If API test runs in jsdom instead of node, renderToBuffer fails and 4.6 is silently skipped or flaky.","higher_leverage_alternative":"Prioritize 4.6 (API tests) and 4.1 (POST assertion) first; defer 4.5 getApiBase test if basePath not yet used.","reasoning":"Required by .cursorrules; 413, trend-chart, chart fallback and mock isolation address adversarial/critique gaps; lower strategic value than ship path."}],"critical_issues":["Step 4.5 — getApiBase reads process.env; in browser build NEXT_PUBLIC_* is inlined at compile time. The test mutates process.env in Node — getApiBase returns '' when window undefined. Test may not validate real browser behavior when basePath is set."],"minor_issues":["Step 4.6 413 test allocates 6MB string in Node — may stress CI on low-memory runners; consider smaller pad or skip on CI if OOM observed."],"suggestions":["Add Step 3.3 acceptance: Remove id=trend-chart via DevTools, click Download — verify PDF downloads without chart. Document as manual smoke before demo.","Pre-flight: Add curl with basePath prefix when next.config basePath is set, to catch getApiBase bugs before deploy."],"dependency_order":"T1 (Phase 1) before T2 — API route depends on WelderReportPDF. T2 before T3 — client POSTs to API. T3 before T4 — integration tests need handleDownloadPDF wired.","premortem_top_risk":"html-to-image fails on Recharts in production browser (e.g. Safari, older Chrome) and chartDataUrl is always null — PDF ships without chart, reducing investor demo impact, with no early warning until first live demo.","goal_alignment_notes":"The four phases together deliver the dual-audience PDF report: investors get something forwardable, operators/managers get actionable session data. Plan explicitly addresses product vision (PDF session report in dual-audience filter) and labor replacement (Shift Manager report). Adversarial and critique fixes close OOM, unhandledrejection, and basePath gaps. All tasks produce visible or testable output within 48h of phase completion.","context_aware_feedback":{"current_context_type":"feature","penalties_applied":[],"penalties_skipped":[]}}

---
# Adversarial Findings (also must fix):


```json
{
  "score": 6.2,
  "adversarial_verdict": "This plan will execute to completion in a clean dev environment. It will fail silently in production when clients use chunked encoding to bypass the body-size limit, and the getApiBase tests may pass for the wrong reason due to Next.js env inlining. Two precondition gaps and one verification/code mismatch create latent risk.",
  "sabotage_vectors": [
    {
      "step": "2.1",
      "attack": "Client sends Transfer-Encoding: chunked instead of Content-Length. The 5MB check only runs when Content-Length is present. Server reads full body (e.g. 50MB) into memory before request.json(), causing OOM.",
      "detection_gap": "413 test uses Content-Length header. No test for chunked transfer with oversized payload.",
      "blast_radius": "Server crash; production instability under malicious or misconfigured clients"
    },
    {
      "step": "2.1",
      "attack": "body.welder.name is an object or array. Route passes it through; sanitizeFilename does String(name) → '[object Object]'; WelderReportPDF receives it. PDF contains '[object Object]', filename is mangled.",
      "detection_gap": "No validation that welder.name is string. 2.1 verification uses valid payload only. 4.6 VALID has welder.name as string.",
      "blast_radius": "Confusing PDF content and filenames; no data corruption but UX and auditability impact"
    },
    {
      "step": "1.3",
      "attack": "Verification expects 'Large PNG (~130KB) PDF: OK' in output. The provided code does not include a large-PNG test (plan marks it optional). Running verification as written fails because the code never prints that string.",
      "detection_gap": "Verification pass criteria are inconsistent with provided code. Implementer may skip the check or add code that wasn't reviewed.",
      "blast_radius": "Step 1.3 verification is unexecutable as written; large-PNG render path remains untested; React PDF may fail on 100KB+ images in production"
    },
    {
      "step": "4.5",
      "attack": "Next.js inlines process.env.NEXT_PUBLIC_BASE_PATH at build time. In Jest, the compiled code may already have the literal value. Mutating process.env in the test does not change the inlined value. Tests pass because getApiBase always returns '' when var wasn't set at build, not because the logic works.",
      "detection_gap": "Test asserts env branches but cannot simulate them if Next.js has already replaced process.env at transform time. A wrong implementation that ignores env would still pass when NEXT_PUBLIC_BASE_PATH is unset.",
      "blast_radius": "basePath deployment fails with 404; test gave false confidence"
    },
    {
      "step": "4.6",
      "attack": "API tests create 6MB (413 test) and 2MB+ (oversized chartDataUrl test) in-memory strings. In CI with limited memory (e.g. 2GB), running these together can cause OOM or severe slowdown. Tests may pass locally and flake in CI.",
      "detection_gap": "No memory constraint or CI-specific verification. Plan does not document minimum RAM for test suite.",
      "blast_radius": "Flaky CI; engineers disable tests or add --testPathIgnorePatterns; regressions slip through"
    }
  ],
  "precondition_gaps": [
    "Phase 2 verification (Steps 2.1–2.3) assumes dev server is running on localhost:3000. No Pre-Flight step starts the server; no step verifies it before curl. First curl fails with connection refused; implementer may assume API is broken.",
    "Step 1.3 verification pass criteria mention 'Large PNG (~130KB) PDF: OK' but the provided code omits this. Precondition for passing verification is undefined.",
    "getApiBase reads process.env.NEXT_PUBLIC_BASE_PATH. If next.config adds basePath, NEXT_PUBLIC_BASE_PATH must be set at build. Plan documents this in Known Issues but no Pre-Flight step verifies the two match when basePath is configured."
  ],
  "untestable_verifications": [
    "Step 3.3: 'Remove id=trend-chart via DevTools; click; PDF downloads without chart' — manual only. Step 4.3 asserts chartDataUrl is null in fetch body but does not verify the returned PDF omits the chart. A wrong implementation could return a PDF with an error placeholder instead of omitting the chart.",
    "Step 2.1: 'head -c 5 /tmp/test.pdf | xxd' — requires manual inspection. No automated assertion that the binary PDF header is correct; a 200 response with wrong Content-Type could pass if file type is guessed."
  ],
  "cascade_points": [
    "Step 2.1 is single point of failure for Steps 2.2, 2.3, 3.3, and 4.6. If the route has a subtle bug (e.g. wrong MIME, corrupted buffer), no downstream verification validates the PDF bytestream end-to-end.",
    "Step 1.2 WelderReportPDF: If React PDF throws on a specific chartDataUrl format (e.g. PNG with unusual encoding), Step 1.3 smoke uses 1x1 PNG only. Bug surfaces only when a real Recharts capture is sent in Step 3."
  ],
  "phase_boundary_gaps": [
    "Phase 1 to Phase 2: Pre-Flight requires Step 1.3 smoke passed but does not require prototype/pdf-route.mts to have been updated to use WelderReportPDF (it currently uses a minimal Doc). If implementer runs the existing prototype, it tests the wrong component.",
    "Phase 2 to Phase 3: Phase 3 assumes API returns PDF. If Phase 2 was verified with curl against a different port or basePath, handleDownloadPDF may call the wrong URL and get 404. No explicit handoff verification of fetch URL."
  ],
  "pre_mortem": {
    "partial_execution": "We got through Phase 1 and Phase 2 but had to stop. The state of the codebase was: WelderReportPDF exists, API route exists and passes curl, but handleDownloadPDF was not wired. The Download PDF button still shows the alert. Phase 3 was never started. Rollback is trivial (revert), but the prototype pdf-route.mts was overwritten in Step 1.3 — the original minimal Doc is lost unless restored from git.",
    "silent_corruption": "Everything appeared to work. Users reported issues 3 days later. The root cause was: welder names containing special objects (from a buggy upstream) were passed through. PDFs showed '[object Object]' and filenames were mangled. The validation in Step 2.1 never enforced welder.name as string. No test covered non-string welder.name.",
    "prod_vs_local": "The staging deployment revealed: the app is served under /app basePath. NEXT_PUBLIC_BASE_PATH was not set at build. getApiBase() returns ''. The fetch goes to /api/welder-report-pdf instead of /app/api/welder-report-pdf. All PDF downloads return 404. The Step 4.5 test passed because it runs in Jest with no basePath, so getApiBase returning '' was the expected case.",
    "dependency_drift": "Three months later, when @react-pdf/renderer updated to 3.5.x, the breakage was: renderToBuffer signature or React PDF Image handling for base64 changed. The 1x1 PNG smoke still passed. Production charts (larger, different encoding) caused 'Invalid image' or crash. The pinned 3.4.0 in package.json was overridden by a transitive update when a different package brought in a newer react-pdf."
  },
  "critical_issues": [
    "Step 2.1: Content-Length check is bypassed by chunked encoding. Malicious or chunked clients can send oversized bodies and cause OOM. No mitigation in plan.",
    "Step 1.3: Verification criteria reference 'Large PNG (~130KB) PDF: OK' but the code does not produce it. Verification is unexecutable as written; implementer cannot satisfy pass criteria with the provided code."
  ],
  "latent_issues": [
    "Step 2.1: welder.name is not validated as string. Objects/arrays produce '[object Object]' in PDF and filename. Documented in Known Issues for Content-Length; welder.name gap is not.",
    "Step 4.6: 2MB+ and 6MB payloads in tests may cause CI OOM. No benchmark or memory limit documented. Latent under resource-constrained CI.",
    "Step 4.5: getApiBase unit test may pass for wrong reason due to Next.js env inlining. basePath deployment could fail with 404 despite green tests."
  ],
  "blind_spots": [
    "Plan assumes request bodies use Content-Length. Chunked encoding is common for large uploads and some proxies. The 5MB protection does not apply there.",
    "Plan assumes npm ci is run after npm install. Pre-Flight says 'Use npm ci' but Step 1.1 only does npm install. Package-lock must be committed. No explicit step to commit lockfile.",
    "Plan assumes html-to-image toPng works on Recharts LineChart in all target browsers. Known Issues documents 'best-effort' but there is no fallback UX (e.g. 'Chart unavailable' message). User gets PDF without chart with no indication why."
  ],
  "minor_issues": [
    "Step 2.2 verification script uses two curl calls (one for body, one for status). Could be simplified to one call with -w for status.",
    "Step 3.1 Depends on: none — could note that it is independent and can be done in parallel with Phase 2.",
    "Step 4.3 code uses try/finally to restore toPng; plan text says 'beforeEach/afterEach'. Slight documentation mismatch; implementation is correct."
  ],
  "what_the_author_cant_see": "The plan was written with a mental model of clients that send Content-Length. Production traffic often uses chunked encoding for POST bodies. The body-size safeguard is effectively optional. Likewise, the author ran verification in an environment where the prototype was already updated — the Phase 1 to 2 boundary assumes prototype uses WelderReportPDF, but the current prototype in-repo uses a minimal Doc. Step 1.3 says 'modify' but the delta is a full replacement; the original prototype is gone. The author's 'modify' implies incremental change; the actual change is wholesale.",
  "recommended_action": "refine",
  "minimum_fixes_before_execution": [
    "Step 2.1: Add streaming body-size check or reject requests with Transfer-Encoding: chunked when body is expected to be large. Document that 5MB limit applies only when Content-Length is present; or implement a streaming parser with size cap.",
    "Step 2.1: Validate welder.name is string (and non-empty for display). Reject or coerce before passing to component.",
    "Step 1.3: Align verification with code — either add the large-PNG smoke to the prototype or remove 'Large PNG (~130KB) PDF: OK' from pass criteria.",
    "Phase 2 Pre-Flight: Add step to verify dev server is running before curl (e.g. curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/... or document that server must be started manually).",
    "Step 4.5: Document that getApiBase test may need special setup (e.g. unmocked module or build-time env) to reliably test basePath. Or add an integration test that fetches with basePath in jsdom."
  ]
}
```
