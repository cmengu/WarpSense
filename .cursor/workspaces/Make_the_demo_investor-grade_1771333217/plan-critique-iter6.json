
Here is the critique output in the required JSON format:

```json
{
  "score": 7.9,
  "dimension_scores": {
    "completeness": 8.5,
    "clarity": 7.5,
    "verification": 7.5,
    "dependencies": 8.5,
    "risk_management": 8.0,
    "edge_cases": 7.0,
    "production_ready": 7.5
  },
  "critical_issues": [
    "Step 2.2 DemoTour: Requirements explicitly call for 'highlight fallback when highlightSelector fails' and 'If highlightSelector fails, use default element or center overlay; animate transitions'. The provided code (lines 548-624) implements only a centered overlay — no highlightSelector usage, no document.querySelector(step.highlightSelector), no spotlight positioning, no fallback logic. If DEMO_TOUR_STEPS later add highlightSelector, the feature will not work. Add either: (a) logic to query and position overlay near the element, with fallback to center when null, plus transition-all; or (b) explicit note that Phase 2 ships without element highlighting (centered overlay only)."
  ],
  "minor_issues": [
    "Step 1.4: hasThermalData is declared but never used. Remove or use for a conditional (e.g. unified placeholder when both empty).",
    "Step 1.4: Skeleton uses {/* ... header, AI summary ... */} and {/* FeedbackPanel, LineChart ... */}. Specify how report.summary, report.feedback_items, and MOCK_HISTORICAL_CHART wire into FeedbackPanel and LineChart.",
    "Step 2.2: Verification says 'assert focus trap (Tab stays inside)' but does not describe how. Add: render DemoTour, query FOCUSABLE_SELECTOR, simulate Tab N times, assert document.activeElement stays within overlayRef.",
    "Step 2.3: onStepEnter calls setCurrentTimestamp and setPlaying — document that these come from DemoPageContent state (already present).",
    "Step 3.4: Optional feature flags ('if time') — unclear when to include. Either make it a concrete step or remove.",
    "DEMO_WELDERS empty: Team dashboard would render an empty grid. Add early return or empty-state message when DEMO_WELDERS.length === 0."
  ],
  "strengths": [
    "Comprehensive pre-planning: exploration synthesis, dependency DAG, risk heatmap, failure modes, and contingency path.",
    "Full code for demo-config, heatmapTempRange, seagull-demo-data, demo-tour-config, DemoTour, team pages — copy-pasteable.",
    "Rollback section (Section 7) with explicit revert steps.",
    "Phase success criteria with checkboxes; clear Phase 1 / 2 / 3 boundaries.",
    "Dependencies verified: THERMAL_*, extractHeatmapData, tempToColorRange, generateExpertSession, ai-feedback mockScore pattern exist in codebase.",
    "PlaceholderHeatMap for empty frames; invalid welderId handled; frame validation in Common Failures.",
    "Debounced scrub (150ms) to reduce UI lag; dismiss overlay before navigate."
  ],
  "reasoning": "The plan is strong and mostly production-ready. Completeness (8.5) is high: phases, rollback, pre-flight, and verification strategy are clear. Dependencies (8.5) form a valid DAG and match the codebase (constants, utils, demo-data). Risk management (8.0) covers failure modes, mitigations, and contingency paths. The main gap is Step 2.2: requirements call for highlightSelector and a fallback, but the DemoTour sample implements only a centered overlay. An implementer copying the code would not get element highlighting or fallback behavior, which contradicts Success Criteria #4 (P1). This is a requirements–implementation mismatch and should be resolved before execution. Minor issues are mostly clarity (unused variable, test assertions, FeedbackPanel/LineChart wiring, empty DEMO_WELDERS). The score of 7.9 reflects a solid plan that needs this one critical fix plus small polish. With the highlight clarification, this would land in 8.2–8.5.",
  "improvement_priority": [
    "CRITICAL: Resolve Step 2.2 highlightSelector / fallback — add code or document that Phase 2 ships centered-only.",
    "Minor: Remove or use hasThermalData in Step 1.4.",
    "Minor: Add FeedbackPanel and LineChart wiring in Step 1.4 skeleton.",
    "Minor: Add concrete focus-trap test steps in Step 2.2 verification.",
    "Minor: Handle DEMO_WELDERS.length === 0 in team dashboard."
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 7.9 is in the 7.0–7.9 band (Has Critical Flaws). The core plan is correct and matches the codebase; the main gap is the DemoTour highlightSelector/fallback requirements vs. the provided implementation. Use refine-plan.md to address the critical mismatch and the minor items. No replan or re-exploration is needed."
}
```
