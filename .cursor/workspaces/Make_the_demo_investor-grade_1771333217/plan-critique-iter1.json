
```json
{
  "score": 7.2,
  "dimension_scores": {
    "completeness": 7.5,
    "clarity": 7.5,
    "verification": 6.0,
    "dependencies": 8.0,
    "risk_management": 8.0,
    "edge_cases": 6.5,
    "production_ready": 7.0
  },
  "critical_issues": [
    "Project rules (.cursorrules) explicitly require 'Verification to be done by adding and running automated tests (no manual browser checks)'. The plan specifies manual verification for 9 of 11 steps. Only Step 1.4 (seagull-demo-data) has unit tests. Steps 1.2, 1.3, 1.5, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3 all use manual checks. Add Jest/RTL tests for: demo team dashboard (render, 2 cards, links), welder report (render with mocked getDemoTeamData), DemoTour (steps advance, skip dismisses), invalid welderId fallback. Existing seagull/page.test.tsx and welder page tests provide patterns.",
    "Step 2.3: onStepEnter callback must be wrapped in useCallback to prevent useEffect dependency churn. DemoTour's useEffect depends on onStepEnter. If the parent passes an inline callback, it is recreated every render, causing useEffect to run every render and potentially trigger render loops. Add to Step 2.3: 'Wrap onStepEnter in useCallback with [setCurrentTimestamp, setPlaying] deps.' The Red team identified this but Common Failures do not document the fix.",
    "Step 1.2: Team dashboard has only subtasks, no code. Plan claims 'another developer could implement without clarification.' Seagull dashboard (app/seagull/page.tsx) exists as reference but implementer must infer layout, adapt hrefs, and remove fetch logic. Add a code skeleton mirroring Seagull structure (grid, DEMO_WELDERS, Link to /demo/team/[welderId]) to remove ambiguity."
  ],
  "minor_issues": [
    "Step 3.1: CTA placement 'below header or in fixed position' is ambiguous. Specify: either 'in header div after title' or 'fixed top-right' with exact className pattern.",
    "Step 2.3: State placement unspecified. Add: 'In DemoPageContent, add const [showTour, setShowTour] = useState(true) alongside existing playing/currentTimestamp state.'",
    "Phase time inconsistency: Phase 1 header says 8–12h, Phase 1 total says 7.5h; Phase 2 header 8–12h, total 5h. Align headers with totals or document buffer rationale.",
    "AppNav modification (Step 3.2): Plan says 'Add Link href=\"/seagull\" label \"Team\"'. AppNav currently has Home, Dashboard, Demo. Specify insertion point: 'Add Team link after Demo, before closing div.'",
    "Step 1.3 welder report: HeatMap receives data with optional colorFn. When extractHeatmapData returns empty points, allTemps=[], minT=0, maxT=600 — handled. But plan does not verify HeatMap's empty-state behavior. HeatMap.tsx already returns fallback when point_count===0. Document in Common Failures: 'If HeatMap shows nothing, verify extractHeatmapData returns non-empty points; generateNoviceSession frames must have thermal_snapshots.'"
  ],
  "strengths": [
    "Excellent pre-planning: exploration synthesis, dependency graph, risk heatmap with P/I, detection signals, and mitigations. 16 risks documented.",
    "Steps 1.1, 1.3, 2.1, 2.2 provide full, copy-pasteable code. createMockScore, getDemoTeamData, DEMO_TOUR_STEPS, DemoTour component are implementation-ready.",
    "Common Failures & Fixes for every step — e.g. 'generateAIFeedback returns No scoring rules' → use exact RULE_IDS; 'Tour hidden behind 3D' → z-[200] isolate.",
    "Clear dependency DAG: seagull-demo-data → team pages → tour config → DemoTour → integration. Phase 1 and 2 parallelizable.",
    "Phase success criteria are explicit checklists. Definition of Done with P0/P1 priorities.",
    "Red team identified 5 real problems (useCallback, getDemoTeamData re-renders, tour flash, CTA placement, expert-benchmark narrative).",
    "Pre-flight checklist with verification commands (node --version, npm install) per phase."
  ],
  "reasoning": "The plan demonstrates strong engineering rigor: full code for critical data and component steps, comprehensive risk documentation, and logical phase boundaries. The core approach (browser-only team path + custom tour overlay) is sound and aligns with exploration findings. generateExpertSession, generateNoviceSession, useFrameData, extractHeatmapData, and HeatMap contracts were verified against the codebase. Three critical gaps block full execution: (1) .cursorrules require automated tests; the plan relies on manual verification for 9 steps. The project uses Jest + RTL (seagull/page.test.tsx pattern); the plan must add analogous tests for demo team pages and DemoTour. (2) onStepEnter inline callback causes useEffect dependency churn in DemoTour — Red team flagged this; the fix (useCallback) belongs in Step 2.3. (3) Step 1.2 lacks code; Seagull dashboard is the template but 'same layout' requires inference. Minor issues are clarity improvements: CTA placement, state placement, time alignment, AppNav insertion point. Score 7.2 reflects a fundamentally solid plan (good structure, full code for 4 critical steps) that needs three targeted fixes. With those, it would reach 8.5–9.0.",
  "improvement_priority": [
    "CRITICAL: Add automated test strategy per .cursorrules — Jest/RTL tests for Steps 1.2, 1.3, 1.5, 2.2. Specify test file paths and key assertions.",
    "CRITICAL: Step 2.3 — Add 'Wrap onStepEnter in useCallback' and document in Common Failures.",
    "CRITICAL: Step 1.2 — Add code skeleton for team dashboard (mirror Seagull grid + DEMO_WELDERS).",
    "Minor: Step 3.1 — Specify CTA placement (header vs fixed) with className.",
    "Minor: Step 2.3 — Specify showTour state location in DemoPageContent.",
    "Minor: Align phase time headers with step totals or add buffer rationale."
  ],
  "recommended_action": "refine",
  "rationale_for_action": "Score 7.2 falls in 7.0–7.9 range. Core approach is correct; phases and dependencies are well-structured; full code exists for data layer and DemoTour. The three critical issues are targeted fixes: automated tests (project compliance), useCallback (prevents render loop), and team dashboard code (removes implementation ambiguity). These do not require re-exploration or replanning. Use refine-plan.md to address the critical issues. If score were <7.0, would recommend re-explore or replan."
}
```
