{
  "name": "warpsense-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "WarpSense welding MVP monorepo",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\"",
    "dev:frontend": "cd my-app && npm run dev",
    "dev:backend": "cd backend && ENV=development bash start.sh",
    "build": "cd my-app && npm run build",
    "test": "npm run test:frontend && npm run test:backend",
    "test:frontend": "cd my-app && npm test",
    "test:backend": "cd backend && source venv/bin/activate && pytest tests/ -v",
    "type-check": "cd my-app && npx tsc --noEmit",
    "install:all": "cd my-app && npm install --legacy-peer-deps && cd ../ipad_app && npm install --legacy-peer-deps && cd ../backend && pip install -r requirements.txt",
    "setup": "npm install && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": []
}
