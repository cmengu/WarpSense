/**
 * Root page at / — re-exports landing from (marketing).
 * Per plan: landing at /, dashboard at /dashboard.
 *
 * NOTE: If you see both this file and (marketing)/page.tsx, delete this file
 * so (marketing)/page.tsx solely serves /. This file exists only because
 * automated delete was rejected; manual removal is recommended.
 */
export { default } from '@/app/(marketing)/page';
