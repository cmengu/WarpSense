/**
 * Dashboard Component
 * Replay + metrics visualization for welding sessions
 * 
 * Placeholder component - implementation will be added later
 */

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function Dashboard() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Dashboard Component - Coming soon</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  text: {
    fontSize: 16,
    color: '#666',
  },
});
